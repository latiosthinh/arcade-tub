---
phase: 11-player-view
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/views/EmbedView.ts
  - src/styles/components/embed.css
  - src/main.ts
  - index.html
  - test/views/embed.test.ts
  - test/main.test.ts
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02
  - PLAY-03
  - PLAY-04

must_haves:
  truths:
    - "Application boots via src/main.ts initializing Store, HashRouter, AppHeader, AppSidebar, BottomNav, and ViewContainer"
    - "Navigating to #/ mounts CatalogView, navigating to #/game/:id mounts GameView, and navigating to #/embed mounts EmbedView"
    - "EmbedView provides live interactive embed preview sandbox with game selector tabs and integration code examples"
    - "Switching between views triggers proper destroy() lifecycle cleanup on active views, preventing zombie iframes and memory leaks"
    - "index.html imports src/main.ts and all component stylesheets"
  artifacts:
    - path: "src/views/EmbedView.ts"
      provides: "Interactive embed kit demo view with game tabs, live embed player, and code snippets"
      min_lines: 60
    - path: "src/styles/components/embed.css"
      provides: "Styles for embed sandbox, code snippets, copy buttons, and tab bar"
      min_lines: 40
    - path: "src/main.ts"
      provides: "Main app bootstrapper initializing reactive Store, HashRouter with View Transitions, and view lifecycle management"
      min_lines: 70
    - path: "index.html"
      provides: "Refactored index.html entry point loading main.ts and modular component stylesheets"
      contains: "/src/main.ts"
    - path: "test/views/embed.test.ts"
      provides: "Unit tests for EmbedView component"
      min_lines: 40
    - path: "test/main.test.ts"
      provides: "Unit tests for application shell and router integration"
      min_lines: 50
  key_links:
    - from: "src/main.ts"
      to: "src/core/Router.ts"
      via: "router.on('/', ...).on('/game/:id', ...).on('/embed', ...)"
    - from: "src/main.ts"
      to: "src/views/CatalogView.ts"
      via: "new CatalogView(store)"
    - from: "src/main.ts"
      to: "src/views/GameView.ts"
      via: "new GameView(store, id)"
    - from: "src/main.ts"
      to: "src/views/EmbedView.ts"
      via: "new EmbedView(store)"
---

<objective>
Implement `EmbedView` (`src/views/EmbedView.ts`), `src/styles/components/embed.css`, and the central application entry point `src/main.ts` connecting `CatalogView`, `GameView`, and `EmbedView` via `HashRouter` and `Store`. Update `index.html` to complete the Milestone v2.0 SPA refactor.

Purpose: Connect the complete reactive architecture, enabling hash-based routing between Catalog, Game Player, and Embed Kit views with seamless transitions and automatic lifecycle teardown.
Output: `src/views/EmbedView.ts`, `src/styles/components/embed.css`, `src/main.ts`, `index.html`, `test/views/embed.test.ts`, `test/main.test.ts`
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-player-view/11-CONTEXT.md
@.planning/phases/11-player-view/11-01-PLAN.md
@src/core/Component.ts
@src/core/Store.ts
@src/core/Router.ts
@src/core/types.ts
@src/components/AppHeader.ts
@src/components/AppSidebar.ts
@src/components/BottomNav.ts
@src/views/CatalogView.ts
@src/views/GameView.ts
@embed.html
@index.html

<interfaces>
From src/core/types.ts:
```typescript
export interface AppState {
  route: RouteInfo;
  activeFilter: string;
  searchQuery: string;
  isMuted: boolean;
  isTheaterMode: boolean;
  highScores: Record<string, number>;
}
```

From src/core/Router.ts:
```typescript
export class HashRouter {
  public on(path: string, handler: RouteHandler): this;
  public notFound(handler: RouteHandler): this;
  public navigate(hashOrPath: string): void;
  public start(useTransitions?: boolean): void;
  public stop(): void;
  public resolve(hash?: string): void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement EmbedView component, styles, and unit tests</name>
  <files>src/views/EmbedView.ts, src/styles/components/embed.css, test/views/embed.test.ts</files>
  <behavior>
    - Test 1: Mounts EmbedView, renders live preview container, game switcher tabs, and integration code samples.
    - Test 2: Switching game tab updates live preview iframe and active tab styling.
    - Test 3: Updates score banner when embed emits score message.
    - Test 4: Cleans up embed instance on destroy without memory leaks.
  </behavior>
  <action>
    1. Create `src/styles/components/embed.css` for grid layout, card containers, code blocks, tab bar, and score banner.
    2. Write `test/views/embed.test.ts` testing mount, tab switching, and destroy cleanup.
    3. Implement `src/views/EmbedView.ts` extending `BaseComponent<AppState>`.
       - Renders interactive arcade embed kit preview with tabs for each game in `GAMES`.
       - Embeds selected game using `ArcadeEmbed` or standard iframe lifecycle.
       - Displays integration code snippets for HTML Web Component, JavaScript SDK, and raw Iframe.
       - Cleans up active iframe and listeners on `destroy()`.
  </action>
  <verify>
    <automated>pnpm test test/views/embed.test.ts</automated>
  </verify>
  <done>EmbedView implemented, styled, and passing unit tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build main app bootstrapper, update index.html, and write integration tests</name>
  <files>src/main.ts, index.html, test/main.test.ts</files>
  <behavior>
    - Test 1: Initializes AppShell with Store, AppHeader, AppSidebar, BottomNav, and #view-container.
    - Test 2: Routes to CatalogView on #/, GameView on #/game/:id, and EmbedView on #/embed.
    - Test 3: Correctly destroys previous view when navigating to a new route.
    - Test 4: Subscribes components to store state changes (search, mute, route).
  </behavior>
  <action>
    1. Write `test/main.test.ts` verifying router initialization, view switching, and lifecycle teardown.
    2. Create `src/main.ts`:
       - Initialize `Store<AppState>` with initial state (route: { path: '/', params: {} }, activeFilter: 'all', searchQuery: '', isMuted: false, isTheaterMode: false, highScores: loadInitialScores()).
       - Instantiate and mount `AppHeader`, `AppSidebar`, `BottomNav`, and `#view-container`.
       - Initialize `HashRouter` with routes:
         - `/` -> Unmount current view (if any), mount `new CatalogView(store)`, update `store.setState({ route: { path: '/', params: {} } })`.
         - `/game/:id` -> Unmount current view, mount `new GameView(store, params.id)`, update `store.setState({ route: { path: `/game/${params.id}`, params } })`.
         - `/embed` -> Unmount current view, mount `new EmbedView(store)`, update `store.setState({ route: { path: '/embed', params: {} } })`.
         - `notFound` -> redirect to `/` or render friendly 404 view.
       - Start router with view transitions: `router.start(true)`.
    3. Update `index.html`:
       - Replace `<script type="module" src="/src/hub.ts"></script>` with `<script type="module" src="/src/main.ts"></script>`.
       - Include component CSS files (`header.css`, `nav.css`, `chips.css`, `cards.css`, `catalog.css`, `player.css`, `embed.css`) or verify `@import` hierarchy.
  </action>
  <verify>
    <automated>pnpm test && pnpm typecheck</automated>
  </verify>
  <done>src/main.ts orchestrates full SPA with clean router lifecycle, index.html updated, all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser Hash Router to View Loader | User navigates to arbitrary hash string |
| Iframe Embed to Host | Sandbox interaction in EmbedView |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Elevation of Privilege | Router navigation | mitigate | Sanitize route params and fallback to CatalogView for unknown paths |
| T-11-05 | Resource Exhaustion | View switching | mitigate | Call `currentView.destroy()` before mounting new view to eliminate zombie event listeners and detached DOM trees |
</threat_model>

<verification>
`pnpm test` passes 100% across all suites.
`pnpm typecheck` passes with zero errors.
</verification>

<success_criteria>
App fully transitions from legacy monolithic hub.ts to modular main.ts architecture supporting Catalog, Game Player, and Embed views with automated lifecycle teardown and zero test regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/11-player-view/11-02-SUMMARY.md`
</output>
