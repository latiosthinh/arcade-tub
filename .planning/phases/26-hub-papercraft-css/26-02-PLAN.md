---
phase: 26-hub-papercraft-css
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/styles/components/header.css
  - src/styles/components/chips.css
  - src/styles/components/cards.css
  - src/styles/components/catalog.css
  - test/components/header.test.ts
  - test/components/chips.test.ts
  - test/components/cards.test.ts
  - test/views/catalog.test.ts
autonomous: true
requirements:
  - CRAFT-01
  - CRAFT-02
  - CRAFT-03
must_haves:
  truths:
    - "AppHeader displays papercraft styling with cardboard borders, craft buttons, and tape accents"
    - "Search input renders as a hand-drawn paper slot with shortcut badge"
    - "Filter chips render as colored construction paper tags with cardboard drop shadows and tape accents when active"
    - "Game cards render as layered cardboard / Polaroid paper cutouts with craft typography, thumbnail border, and tactile hover physics"
    - "Catalog view section headers and empty state display papercraft notebook aesthetic"
    - "All unit tests for header, chips, cards, and catalog view continue to pass"
  artifacts:
    - path: "src/styles/components/header.css"
      provides: "Papercraft styling for top navigation bar, search input, and action buttons"
      contains: ".ac-header"
    - path: "src/styles/components/chips.css"
      provides: "Paper tag styling with active tape strips for category filters"
      contains: ".ac-chip"
    - path: "src/styles/components/cards.css"
      provides: "Layered cardboard card frames, craft score badges, and papercut play icons"
      contains: ".ac-card"
    - path: "src/styles/components/catalog.css"
      provides: "Grid layout and paper notebook empty-state styling"
      contains: ".ac-catalog-view"
  key_links:
    - from: "src/styles/components/header.css"
      to: "src/styles/tokens.css"
      via: "CSS variable references"
      pattern: "var\\(--bg-surface|--shadow-cardboard|--paper-ink\\)"
    - from: "src/styles/components/cards.css"
      to: "src/styles/tokens.css"
      via: "CSS variable references"
      pattern: "var\\(--font-display|--border-subtle|--bg-card\\)"
---

<objective>
Restyle the hub discovery components (`header.css`, `chips.css`, `cards.css`, and `catalog.css`) using the 2D Papercraft design system.
1. `header.css`: Style top header as a clean cream paper bar with a 2.5px dark brown ink border, washi tape accent stickers on the logo badge, cut-out search input field, and square cardboard tactile icon buttons for Audio and CRT scanlines.
2. `chips.css`: Style filter chips as colored construction paper tags (kraft, ochre, cherry red, sky blue, sage green) with cardboard drop shadows (`2px 2px 0px #3E2723`), slight alternating rotational angles on hover, and tape strip highlights when active.
3. `cards.css`: Style game cards as layered cardboard plates with paper cutout thumbnail windows, hand-drawn dark borders, craft font headings (`Patrick Hand`), high score ribbons, and circular papercut play buttons.
4. `catalog.css`: Style the catalog view grid, section headers with craft fonts, and paper cutout empty-search state.
5. Verify that all component DOM classes and interactions remain 100% compliant with existing tests.

Purpose: Bring the papercraft visual identity to the primary browsing and discovery surfaces of the Arcade Carnival hub.
Output: Updated `header.css`, `chips.css`, `cards.css`, `catalog.css`, verified with Vitest test suites.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-hub-papercraft-css/26-01-PLAN.md
@src/styles/tokens.css
@src/styles/theme.css
@src/styles/components/header.css
@src/styles/components/chips.css
@src/styles/components/cards.css
@src/styles/components/catalog.css
@test/components/header.test.ts
@test/components/chips.test.ts
@test/components/cards.test.ts
@test/views/catalog.test.ts

<interfaces>
<!-- Essential class names in header.css -->
.ac-header
.ac-header-left, .ac-logo, .ac-logo-icon, .ac-logo-text, .ac-logo-badge
.ac-header-center, .ac-search-box, .ac-search-input, .ac-search-shortcut
.ac-header-right, .ac-icon-btn, .ac-audio-toggle, .ac-crt-toggle, .ac-embed-link

<!-- Essential class names in chips.css -->
.ac-chips-bar
.ac-chip, .ac-chip.active

<!-- Essential class names in cards.css -->
.ac-card, .ac-card:hover, .ac-card:focus-visible
.ac-card-thumb, .ac-card-screenshot-container, .ac-game-screenshot, .ac-card-icon
.ac-card-play-overlay, .ac-card-play-circle
.ac-card-meta, .ac-card-title-row, .ac-card-title, .ac-card-genre
.ac-card-score-row, .ac-card-score-badge, .ac-card-score-value, .ac-card-rating

<!-- Essential class names in catalog.css -->
.ac-catalog-view, .ac-section-header, .ac-section-title, .ac-section-sub
.ac-game-grid-container, .ac-game-grid, .ac-card.is-hidden
.ac-grid-empty, .ac-empty-icon, .ac-empty-title, .ac-empty-sub
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Overhaul header.css and chips.css with Papercraft aesthetics</name>
  <files>src/styles/components/header.css, src/styles/components/chips.css</files>
  <action>
1. In `src/styles/components/header.css`:
   - `.ac-header`: Background `#FFFDF8` (card stock), border-bottom `2.5px solid #3E2723`, box-shadow `0 3px 0px rgba(62, 39, 35, 0.15)`.
   - `.ac-logo-text`: Font `var(--font-display)` (Patrick Hand), font-size `1.5rem`, color `#3E2723`, letter-spacing `0.5px`, font-weight `700`.
   - `.ac-logo-badge`: Construction red background `#E11D48`, text `#FFFFFF`, border `1.5px solid #3E2723`, box-shadow `2px 2px 0px #3E2723`, transform `rotate(-2deg)`, font-family `var(--font-display)`.
   - `.ac-search-input`: Background `#F4EAD4`, border `2px solid #3E2723`, border-radius `var(--radius-full)`, box-shadow `inset 1px 1px 3px rgba(62, 39, 35, 0.1)`, font-family `var(--font-sans)`. On focus: border-color `#E11D48`, box-shadow `2px 2px 0px #3E2723`.
   - `.ac-search-shortcut`: Background `#FFFDF8`, border `1.5px solid #3E2723`, border-radius `var(--radius-sm)`, box-shadow `1px 1px 0px #3E2723`, font-family `var(--font-mono)`.
   - `.ac-icon-btn`: Cardboard cutout button `#FFFDF8`, border `2px solid #3E2723`, border-radius `var(--radius-md)`, box-shadow `2px 2px 0px #3E2723`, transition `transform 0.1s ease, box-shadow 0.1s ease`. On hover: background `#F59E0B` (construction yellow), transform `translateY(-1px)`, box-shadow `3px 3px 0px #3E2723`. On active: transform `translate(2px, 2px)`, box-shadow `0px 0px 0px`.
   - `.ac-embed-link`: Construction blue `#3B82F6` or green `#10B981`, border `2px solid #3E2723`, border-radius `var(--radius-md)`, box-shadow `2px 2px 0px #3E2723`, font-family `var(--font-display)`, font-size `var(--text-sm)`, font-weight `700`.
2. In `src/styles/components/chips.css`:
   - `.ac-chips-bar`: Spacing and smooth scrolling with craft paper styling.
   - `.ac-chip`: Construction paper tab `#FFFDF8`, border `2px solid #3E2723`, border-radius `var(--radius-full)`, box-shadow `2px 2px 0px #3E2723`, font-family `var(--font-display)`, font-size `var(--text-base)`, font-weight `700`, color `#3E2723`.
   - `.ac-chip:hover`: Background `#FDF6E2`, transform `translateY(-2px) rotate(1deg)`, box-shadow `3px 3px 0px #3E2723`.
   - `.ac-chip.active`: Construction yellow `#F59E0B`, border `2.5px solid #3E2723`, box-shadow `3px 3px 0px #3E2723`, transform `rotate(-1deg)`, position relative with subtle pseudo-element tape or staple accent.
  </action>
  <verify>
    <automated>pnpm test test/components/header.test.ts test/components/chips.test.ts</automated>
  </verify>
  <done>header.css and chips.css fully transformed to Papercraft design system; unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Overhaul cards.css and catalog.css with Papercraft aesthetics</name>
  <files>src/styles/components/cards.css, src/styles/components/catalog.css</files>
  <action>
1. In `src/styles/components/cards.css`:
   - `.ac-card`: Layered cardboard frame background `#FFFDF8`, border `2.5px solid #3E2723`, border-radius `var(--radius-lg)`, box-shadow `var(--shadow-paper-layer)` (`4px 4px 0px #3E2723, 7px 7px 0px rgba(62,39,35,0.15)`), overflow hidden, transition `transform 0.15s ease, box-shadow 0.15s ease`.
   - `.ac-card:hover`: `transform: translateY(-4px) rotate(0.5deg)`, `box-shadow: 6px 6px 0px #3E2723, 10px 10px 0px rgba(62,39,35,0.15)`.
   - `.ac-card-thumb`: Polaroid / cutout paper window aspect-ratio `16 / 9`, border-bottom `2px solid #3E2723`, background `#F4EAD4`.
   - `.ac-card-play-circle`: Construction cherry red `#E11D48`, color `#FFFFFF`, border `2px solid #3E2723`, box-shadow `2px 2px 0px #3E2723`, font-family `var(--font-display)`.
   - `.ac-card-title`: Font `var(--font-display)` (Patrick Hand), font-size `1.25rem`, font-weight `700`, color `#3E2723`.
   - `.ac-card-genre`: Font `var(--font-sans)` (Comfortaa), font-size `var(--text-xs)`, color `#795548`, font-weight `600`.
   - `.ac-card-score-row`: Border-top `1.5px dashed rgba(62, 39, 35, 0.25)`, padding-top `0.5rem`, color `#3E2723`.
   - `.ac-card-score-badge`: Color `#E11D48`, font-weight `700`, font-family `var(--font-display)`.
   - `.ac-card-rating`: Color `#D97706`, font-weight `700`.
2. In `src/styles/components/catalog.css`:
   - `.ac-section-title`: Font `var(--font-display)` (Patrick Hand), font-size `1.75rem`, color `#3E2723`, font-weight `700`, letter-spacing `0.5px`.
   - `.ac-section-sub`: Font `var(--font-sans)`, font-size `var(--text-sm)`, color `#795548`.
   - `.ac-grid-empty`: Background `#FFFDF8`, border `2px dashed #3E2723`, border-radius `var(--radius-lg)`, box-shadow `3px 3px 0px #3E2723`, padding `3rem 2rem`.
   - `.ac-empty-icon`: Font-size `3.5rem`, filter `drop-shadow(2px 2px 0px #3E2723)`.
   - `.ac-empty-title`: Font `var(--font-display)`, font-size `var(--text-2xl)`, color `#3E2723`.
   - `.ac-empty-sub`: Font `var(--font-sans)`, color `#795548`.
  </action>
  <verify>
    <automated>pnpm test test/components/cards.test.ts test/views/catalog.test.ts</automated>
  </verify>
  <done>cards.css and catalog.css fully transformed to Papercraft design system; unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User Search Input -> Component | Untrusted query string passed to DOM & store |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Cross-Site Scripting (XSS) | GameCard / Search Query rendering | mitigate | Use textContent / sanitized DOM node generation in Component.ts and GameCard.ts |
| T-26-04 | Denial of Service | Complex CSS box-shadows & hover transforms | mitigate | Keep box-shadows discrete (0 blur solid steps), hardware-accelerated transforms |
</threat_model>

<verification>
Automated verification via Vitest:
`pnpm test test/components/header.test.ts test/components/chips.test.ts test/components/cards.test.ts test/views/catalog.test.ts`
</verification>

<success_criteria>
- Header, Filter Chips, Game Cards, and Catalog grid styled in 2D Papercraft visual language.
- All associated unit tests pass.
</success_criteria>

<output>
Create `.planning/phases/26-hub-papercraft-css/26-02-SUMMARY.md` on completion.
</output>
