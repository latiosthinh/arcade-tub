---
phase: 26-hub-papercraft-css
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/styles/tokens.css
  - src/styles/theme.css
  - test/tokens.test.ts
autonomous: true
requirements:
  - CRAFT-01
  - CRAFT-02
must_haves:
  truths:
    - "index.html loads craft Google Fonts (Patrick Hand, Cabin Sketch, Comfortaa)"
    - "tokens.css defines papercraft palette (kraft brown, cardboard tan, cream, construction paper colors)"
    - "tokens.css defines papercraft shadows, borders, tape strips, and torn-edge clip-path primitives"
    - "theme.css implements papercraft background textures, cardboard card base, tape badge accents, and craft buttons"
    - "Vitest token test suite passes verifying all required papercraft design tokens"
  artifacts:
    - path: "index.html"
      provides: "Google font link tags for Patrick Hand, Cabin Sketch, and Comfortaa"
      contains: "Patrick+Hand"
    - path: "src/styles/tokens.css"
      provides: "CSS custom properties for craft paper palette, shadows, typography, and clip paths"
      contains: "--bg-kraft"
    - path: "src/styles/theme.css"
      provides: "Base resets, paper texture backgrounds, card primitives with layered shadow and tape styling"
      contains: "arcade-card"
    - path: "test/tokens.test.ts"
      provides: "Unit tests validating all required CSS variables"
      contains: "tokens.css design tokens"
  key_links:
    - from: "src/styles/theme.css"
      to: "src/styles/tokens.css"
      via: "CSS import"
      pattern: "@import.*tokens\\.css"
    - from: "index.html"
      to: "src/styles/tokens.css"
      via: "link stylesheet"
      pattern: "rel=\"stylesheet\".*tokens\\.css"
---

<objective>
Transform the core typography, design tokens, and base theme primitives from the vintage storybook style into the 2D Papercraft design system.
1. Update `index.html` to import craft-style Google Fonts: Patrick Hand (display handwriting), Cabin Sketch (sketchy display headings), and Comfortaa (soft geometric sans/body).
2. Rewrite `src/styles/tokens.css` with a craft paper palette (kraft brown `#C4A482`, cardboard tan `#D2B48C`, construction paper colors: craft red/pink `#D9534F`, craft blue/cyan `#4A90E2`, craft yellow `#F5B041`, craft green `#52BE80`, craft purple `#8E44AD`), layered cardboard shadows (`3px 3px 0px #3E2723`, `6px 6px 0px rgba(62,39,35,0.2)`), tape-strip colors (`rgba(255,255,255,0.7)` / semi-transparent washi tape), staple/pin accents, and hand-cut wobble radii.
3. Update `src/styles/theme.css` with a subtle graph/craft-paper grid background pattern, layered cardboard `.arcade-card` primitives, paper tape badges, and tactile tactile cut-out buttons with pressed state physics.
4. Update `test/tokens.test.ts` to assert all required tokens.

Purpose: Provide the foundational visual tokens, typography, and reusable primitives for all hub components and game renderers in Milestone v5.0.
Output: Updated `index.html`, `src/styles/tokens.css`, `src/styles/theme.css`, `test/tokens.test.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@index.html
@src/styles/tokens.css
@src/styles/theme.css
@test/tokens.test.ts

<interfaces>
<!-- Existing token contract required by components -->
:root {
  --bg-primary: string;
  --bg-surface: string;
  --bg-card: string;
  --bg-card-hover: string;
  --bg-overlay: string;
  --neon-cyan: string;
  --neon-pink: string;
  --neon-yellow: string;
  --neon-green: string;
  --neon-purple: string;
  --glow-cyan: string;
  --glow-pink: string;
  --glow-yellow: string;
  --glow-green: string;
  --glow-purple: string;
  --font-display: string;
  --font-mono: string;
  --font-sans: string;
  --font-handwriting: string;
  --text-xs: string;
  --text-sm: string;
  --text-base: string;
  --text-lg: string;
  --text-xl: string;
  --text-2xl: string;
  --text-3xl: string;
  --border-subtle: string;
  --border-accent: string;
  --border-pink: string;
  --radius-sm: string;
  --radius-md: string;
  --radius-lg: string;
  --radius-full: string;
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Google Fonts in index.html and update tokens test</name>
  <files>index.html, test/tokens.test.ts</files>
  <behavior>
    - index.html includes Google Fonts link for Patrick+Hand, Cabin+Sketch, and Comfortaa
    - test/tokens.test.ts verifies all semantic tokens plus new papercraft variables (--bg-kraft, --bg-cardboard, --tape-bg, --shadow-cardboard, --shadow-paper-layer)
  </behavior>
  <action>
1. In `index.html`, replace the existing font link with:
   `<link href="https://fonts.googleapis.com/css2?family=Cabin+Sketch:wght@700&family=Comfortaa:wght@500;700&family=Patrick+Hand&display=swap" rel="stylesheet">`
2. In `test/tokens.test.ts`, update assertions to verify that standard semantic tokens (--bg-primary, --bg-surface, --bg-card, --neon-cyan, --neon-pink, --neon-yellow, --neon-green, --neon-purple, --glow-cyan, --glow-pink, --font-display, --font-sans, --font-mono, --font-handwriting, --border-subtle, --border-accent) as well as new papercraft tokens exist.
  </action>
  <verify>
    <automated>pnpm test test/tokens.test.ts</automated>
  </verify>
  <done>index.html loads craft fonts and test/tokens.test.ts describes the complete papercraft token specification.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Papercraft Design Tokens in tokens.css and Base Primitives in theme.css</name>
  <files>src/styles/tokens.css, src/styles/theme.css</files>
  <action>
1. In `src/styles/tokens.css`:
   - Set `--bg-primary` to `#F4EAD4` (warm textured cream/parchment base).
   - Set `--bg-surface` to `#FFFDF8` (clean card paper cutout).
   - Set `--bg-card` to `#FFFDF8`.
   - Set `--bg-card-hover` to `#FDF6E2`.
   - Set `--bg-kraft` to `#D8C3A5`.
   - Set `--bg-cardboard` to `#C5A880`.
   - Set `--bg-overlay` to `rgba(62, 39, 35, 0.7)`.
   - Set construction paper accents:
     `--neon-cyan`: `#3B82F6` (construction blue)
     `--neon-pink`: `#E11D48` (construction cherry red)
     `--neon-yellow`: `#F59E0B` (construction warm yellow)
     `--neon-green`: `#10B981` (construction green)
     `--neon-purple`: `#8B5CF6` (construction violet)
     `--paper-ink`: `#3E2723` (dark brown ink line)
   - Set layered cardboard shadows (solid cardboard depth instead of neon light):
     `--glow-cyan`: `3px 3px 0px #2563EB, 5px 5px 0px rgba(62,39,35,0.3)`
     `--glow-pink`: `3px 3px 0px #BE123C, 5px 5px 0px rgba(62,39,35,0.3)`
     `--glow-yellow`: `3px 3px 0px #D97706, 5px 5px 0px rgba(62,39,35,0.3)`
     `--glow-green`: `3px 3px 0px #059669, 5px 5px 0px rgba(62,39,35,0.3)`
     `--glow-purple`: `3px 3px 0px #6D28D9, 5px 5px 0px rgba(62,39,35,0.3)`
     `--shadow-cardboard`: `3px 3px 0px #3E2723`
     `--shadow-paper-layer`: `4px 4px 0px #3E2723, 7px 7px 0px rgba(62, 39, 35, 0.2)`
   - Set craft fonts:
     `--font-display`: `'Patrick Hand', 'Cabin Sketch', cursive, sans-serif`
     `--font-sans`: `'Comfortaa', cursive, sans-serif`
     `--font-mono`: `'Comfortaa', monospace`
     `--font-handwriting`: `'Patrick Hand', cursive`
     `--font-sketch`: `'Cabin Sketch', cursive`
   - Set ink borders:
     `--border-subtle`: `2px solid #3E2723`
     `--border-accent`: `2.5px solid #3E2723`
     `--border-pink`: `2.5px solid #E11D48`
     `--border-dashed`: `2px dashed #3E2723`
     `--radius-sm`: `4px`
     `--radius-md`: `8px`
     `--radius-lg`: `12px`
     `--radius-full`: `9999px`
   - Add tape & staple tokens:
     `--tape-bg`: `rgba(255, 248, 220, 0.75)`
     `--tape-border`: `1px solid rgba(62, 39, 35, 0.15)`
     `--tape-shadow`: `1px 1px 3px rgba(0,0,0,0.15)`
2. In `src/styles/theme.css`:
   - Update body background with a subtle craft paper grid: `radial-gradient(#3E2723 0.65px, transparent 0.65px), radial-gradient(#3E2723 0.65px, #F4EAD4 0.65px)` with size `20px 20px`.
   - Update typography primitives to use `--paper-ink` (`#3E2723`) and `--font-display`.
   - Update `.arcade-card` to use `--shadow-paper-layer` with a subtle cardboard border and slight rotation hover effect (`transform: translateY(-2px) rotate(-0.5deg)`).
   - Update `.arcade-btn` with cardboard bevel shadow (`3px 3px 0px #3E2723`), solid dark ink borders, and active press physics (`transform: translate(2px, 2px); box-shadow: 1px 1px 0px #3E2723;`).
   - Add `.paper-tape` and `.paper-staple` utility classes for decorative headers and card accents.
  </action>
  <verify>
    <automated>pnpm test test/tokens.test.ts</automated>
  </verify>
  <done>tokens.css and theme.css define cohesive 2D Papercraft design system primitives; all token tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| External CDN -> HTML | Google Fonts stylesheet and font binaries loaded via HTTPS |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | index.html Google Fonts link | mitigate | Use official HTTPS fonts.googleapis.com / fonts.gstatic.com with preconnect |
| T-26-02 | Denial of Service | CSS animations & custom fonts | mitigate | Fallback system fonts provided in font-family stacks; hardware-accelerated transforms |
</threat_model>

<verification>
Automated verification via Vitest:
`pnpm test test/tokens.test.ts`
`pnpm test`
</verification>

<success_criteria>
- All tokens defined with correct papercraft color hex codes and font families.
- All 574 tests across repository pass.
- `index.html` links the 3 craft Google Fonts.
</success_criteria>

<output>
Create `.planning/phases/26-hub-papercraft-css/26-01-SUMMARY.md` on completion.
</output>
