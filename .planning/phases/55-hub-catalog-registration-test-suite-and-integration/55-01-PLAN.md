---
phase: 55-hub-catalog-registration-test-suite-and-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - games/tank-1990/index.html
  - games/tank-1990/src/main.ts
  - vite.config.ts
  - src/data/games.ts
  - src/data/screenshots.ts
autonomous: true
requirements:
  - INTEG-01
  - INTEG-02
  - INTEG-03

must_haves:
  truths:
    - "Standalone Tank 1990 game shell is accessible at games/tank-1990/index.html"
    - "Game loop in games/tank-1990/src/main.ts orchestrates all engine subsystems (GridMap, PlayerTank, BulletManager, EnemySpawner, PowerUpSystem, GameFlow, TankRenderer, TankAudio, TouchControls, ViewportManager)"
    - "Game is registered in src/data/games.ts under arcade/action category with id 'tank-1990'"
    - "Custom retro papercraft SVG screenshot is registered in src/data/screenshots.ts for 'tank-1990'"
    - "Vite multi-page build configuration in vite.config.ts includes 'tank-1990' entry point"
  artifacts:
    - path: "games/tank-1990/index.html"
      provides: "Standalone game shell with canvas and viewport meta"
      min_lines: 20
    - path: "games/tank-1990/src/main.ts"
      provides: "Complete standalone orchestration loop and playables adapter integration"
      min_lines: 50
    - path: "src/data/games.ts"
      provides: "Game metadata registration with features and theme color"
      contains: "tank-1990"
    - path: "src/data/screenshots.ts"
      provides: "Cardboard tank SVG artwork screenshot"
      contains: "tank-1990"
    - path: "vite.config.ts"
      provides: "Rollup input multi-page entry"
      contains: "tank-1990"
  key_links:
    - from: "games/tank-1990/index.html"
      to: "games/tank-1990/src/main.ts"
      via: "script type=module"
      pattern: 'src="./src/main.ts"'
    - from: "games/tank-1990/src/main.ts"
      to: "games/tank-1990/src/GameFlow.ts"
      via: "Game orchestration"
      pattern: "new GameFlow"
    - from: "vite.config.ts"
      to: "games/tank-1990/index.html"
      via: "rollupOptions input"
      pattern: "'tank-1990': resolve\\(__dirname, 'games/tank-1990/index.html'\\)"
---

<objective>
Package Tank 1990 (Battle City) into standalone game shell (`games/tank-1990/index.html` & `games/tank-1990/src/main.ts`), connect all subsystems into a unified 60FPS game loop with Playables adapter lifecycle hooks, configure Vite multi-page build entry in `vite.config.ts`, register game metadata in `src/data/games.ts`, and render a custom 2D papercraft SVG screenshot in `src/data/screenshots.ts`.

Purpose: Enable standalone playability and discovery from the Arcade Carnival central hub.
Output: Standalone HTML/TS game shell, hub catalog entry, SVG screenshot, Vite build config.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-hub-catalog-registration-test-suite-and-integration/55-CONTEXT.md
@games/tank-1990/src/types.ts
@src/data/games.ts
@src/data/screenshots.ts
@vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble standalone game shell and main loop</name>
  <files>games/tank-1990/index.html, games/tank-1990/src/main.ts</files>
  <action>
1. Create `games/tank-1990/index.html`:
   - Title: "Tank 1990 — Arcade Carnival"
   - Papercraft background styling with radial dots, viewport meta, `Comfortaa` and `Patrick Hand` fonts, `#game` canvas (512x448 logical, styled with aspect-ratio preservation or touch-action none), script entry pointing to `./src/main.ts`.
2. Create `games/tank-1990/src/main.ts`:
   - Initialize `@arcade-carnival/playables-adapter` (`initPlayables`, `onPause`, `onResume`).
   - Instantiate all core subsystems: `GridMap`, `PlayerTank`, `BulletManager`, `EnemySpawner`, `PowerUpSystem`, `ScoreManager`, `GameFlow`, `TankRenderer`, `TankAudio`, `TouchControls`, `ViewportManager`, `ParticleEmitter`.
   - Setup keyboard listeners (WASD / Arrows for 4-way direction, Space / J for firing, Enter for stage start / pause toggle, 1/2 for title options).
   - Setup touch controls via `TouchControls` and canvas pointer events mapped through `ViewportManager.projectTouchToGameCoord`.
   - Setup fixed/variable delta time `requestAnimationFrame` loop updating `GameFlow.update(dt)`, physics, spawning, collisions, audio triggers, particle simulation, and `TankRenderer.renderScene()`.
   - Handle audio context unlock on first user gesture.
   - Handle pause/resume lifecycle hooks.
  </action>
  <verify>
    <automated>npx vitest run games/tank-1990/test/</automated>
  </verify>
  <done>Standalone HTML and main.ts created, all imports compile without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Register Tank 1990 in Vite build, catalog games, and screenshots</name>
  <files>vite.config.ts, src/data/games.ts, src/data/screenshots.ts</files>
  <action>
1. In `vite.config.ts`:
   - Add `'tank-1990': resolve(__dirname, 'games/tank-1990/index.html')` to `build.rollupOptions.input`.
2. In `src/data/games.ts`:
   - Add `tank-1990` game item to `GAMES` list:
     - `id: 'tank-1990'`
     - `title: 'Tank 1990'`
     - `genre: 'Tactical Retro Tank Combat'`
     - `category: 'action'`
     - `description: 'Authentic 2D papercraft Battle City arcade shooter. Defend your cardboard Eagle HQ, blast through destructible brick grids, upgrade your tank across 4 tiers, collect tactical powerups, and battle 20-tank enemy waves across 35 classic stages.'`
     - `badge: 'Retro'`
     - `rating: '5.0 ★'`
     - `plays: '150K plays'`
     - `icon: '🛡️'`
     - `themeColor: '#D97706'`
     - `bannerBg: 'linear-gradient(135deg, #FAF6EE 0%, #D8C3A5 100%)'`
     - `features: ['26×26 Destructible Microgrid', '4-Tier Tank Upgrades & 8 Powerups', 'Procedural 8-Bit Web Audio & Confetti FX']`
3. In `src/data/screenshots.ts`:
   - Add `'tank-1990'` SVG screenshot entry matching 2D papercraft cardboard visual style:
     - Kraft parchment background (`#FAF6EE` / `#F4EAD4`).
     - Sub-tile brick wall clusters (`#E11D48`), steel block (`#94A3B8`), water pond (`#38BDF8`), cardboard grass canopy (`#059669`).
     - Cardboard Eagle Base HQ with perimeter walls.
     - Yellow/Ochre player tank with barrel and cardboard tracks.
     - Enemy tanks (Red Fast Cruiser, White Armor Tank) and bullet projectile trails with muzzle sparks.
     - Top HUD banner / score display.
  </action>
  <verify>
    <automated>npx vitest run test/production/games-catalog.test.ts</automated>
  </verify>
  <done>Game is wired into Vite config, catalog list, and screenshots table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Browser -> LocalStorage | High score and game settings persistence |
| Input Event -> Game Loop | Keyboard and multi-touch pointer events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Tampering | LocalStorage High Score | mitigate | Validate numerical bounds in `ScoreManager` / `getPersonalHighScore` with fallback |
| T-55-02 | Denial of Service | Touch / Pointer Event Flood | mitigate | ViewportManager bounds checking and pointer map pooling to prevent memory leaks |
</threat_model>

<verification>
- Verify `games/tank-1990/index.html` and `games/tank-1990/src/main.ts` exist.
- Verify `vite.config.ts` contains `tank-1990` entry.
- Verify `src/data/games.ts` contains `tank-1990`.
- Verify `src/data/screenshots.ts` contains `tank-1990`.
</verification>

<success_criteria>
Tank 1990 is packaged in standalone shell, wired to build configuration, registered in hub catalog, and visual screenshot is rendered.
</success_criteria>

<output>
After completion, create `.planning/phases/55-hub-catalog-registration-test-suite-and-integration/55-01-SUMMARY.md`
</output>
