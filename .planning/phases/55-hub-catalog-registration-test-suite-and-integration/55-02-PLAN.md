---
phase: 55-hub-catalog-registration-test-suite-and-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 55-01
files_modified:
  - test/production/bundle-audit.test.ts
autonomous: true
requirements:
  - INTEG-04

must_haves:
  truths:
    - "Production bundle audit test verifies dist/games/tank-1990/index.html builds properly"
    - "All 190+ Tank 1990 unit test suites pass with 100% success rate"
    - "Full repository TypeScript compilation passes with zero type errors"
    - "Production Vite build succeeds and generates standalone static assets"
  artifacts:
    - path: "test/production/bundle-audit.test.ts"
      provides: "Updated bundle audit test suite including tank-1990 and calibrated gzipped budget"
      contains: "games/tank-1990/index.html"
  key_links:
    - from: "test/production/bundle-audit.test.ts"
      to: "dist/games/tank-1990/index.html"
      via: "fs.existsSync validation"
      pattern: "games/tank-1990/index.html"
---

<objective>
Execute full platform regression testing, validate 100% pass rate across the comprehensive Tank 1990 Vitest suite (and all existing catalog games), update production bundle audit tests to verify `games/tank-1990/index.html` distribution output with calibrated bundle budget, and run end-to-end Vite production build verification.

Purpose: Guarantee zero-regression stability, build compliance, and milestone v8.0 completion.
Output: Validated Vitest test suite, updated bundle-audit test, clean production build.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-hub-catalog-registration-test-suite-and-integration/55-CONTEXT.md
@test/production/bundle-audit.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update bundle audit test suite for Tank 1990 and calibrate gzip budget</name>
  <files>test/production/bundle-audit.test.ts</files>
  <action>
1. In `test/production/bundle-audit.test.ts`:
   - Add `'games/tank-1990/index.html'` to `requiredEntries` list in `dist directory contains all production entry HTML files`.
   - Update gzipped bundle threshold check if needed (e.g. adjust from 300KB to 350KB to accommodate 43 complete games in total distribution bundle budget).
2. Run build: `npm run build` to generate complete `dist/` directory including `tank-1990`.
3. Run bundle audit test to verify production output.
  </action>
  <verify>
    <automated>npx vitest run test/production/bundle-audit.test.ts</automated>
  </verify>
  <done>Production bundle test includes tank-1990 and passes with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Full platform regression testing and build verification</name>
  <files>test/production/bundle-audit.test.ts</files>
  <action>
1. Run type checking across the entire project: `npx tsc --noEmit`.
2. Run full Vitest suite across all games and engine modules: `npx vitest run`.
3. Confirm all Tank 1990 tests (GridMap, PlayerTank, BulletManager, EnemySpawner, PowerUpSystem, GameFlow, TankRenderer, TankAudio, TouchControls, ViewportManager) pass 100%.
4. Verify production build output: `npm run build`.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Entire test suite (1100+ unit tests) and build pass with 100% success rate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production Build Pipeline | Vite bundling and Rollup output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-03 | Denial of Service | Asset Bloat in Production Dist | mitigate | Enforce individual asset gzip < 50KB and total gzipped dist audit |
</threat_model>

<verification>
- Run `npx tsc --noEmit` and confirm 0 errors.
- Run `npm test` and confirm all tests pass.
- Run `npm run build` and confirm successful build.
</verification>

<success_criteria>
All unit tests, catalog validations, TypeScript compiler checks, and production builds pass cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/55-hub-catalog-registration-test-suite-and-integration/55-02-SUMMARY.md`
</output>
