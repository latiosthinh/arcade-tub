---
phase: 10-hub-views
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/components/FilterChips.ts
  - src/components/GameCard.ts
  - src/components/GameGrid.ts
  - src/views/CatalogView.ts
  - src/styles/components/chips.css
  - src/styles/components/cards.css
  - src/styles/components/catalog.css
  - test/components/chips.test.ts
  - test/components/cards.test.ts
  - test/views/catalog.test.ts
autonomous: true
requirements:
  - COMP-03
  - COMP-04

must_haves:
  truths:
    - "FilterChips renders genre filter options ('all', 'action', 'arcade', 'casual') and updates Store activeFilter"
    - "GameCard renders game icon, title, genre badge, rating, plays count, and persistent personal high score"
    - "GameCard displays play-on-hover neon glow effect and supports keyboard navigation with visible focus rings"
    - "GameGrid updates visible GameCard instances dynamically on filter or search state change without full DOM destruction"
    - "CatalogView orchestrates FilterChips, featured Hero marquee, and GameGrid inside a unified catalog feed"
  artifacts:
    - path: "src/components/FilterChips.ts"
      provides: "FilterChips component extending BaseComponent"
      exports: ["FilterChips"]
    - path: "src/components/GameCard.ts"
      provides: "GameCard visual component with high score and hover glow"
      exports: ["GameCard"]
    - path: "src/components/GameGrid.ts"
      provides: "GameGrid container managing card child instances"
      exports: ["GameGrid"]
    - path: "src/views/CatalogView.ts"
      provides: "CatalogView container feed component"
      exports: ["CatalogView"]
    - path: "src/styles/components/chips.css"
      provides: "Cyber-arcade styles for filter chips bar"
    - path: "src/styles/components/cards.css"
      provides: "Cyber-arcade styles for game cards, badges, and hover glow"
    - path: "src/styles/components/catalog.css"
      provides: "Catalog layout and hero banner styles"
  key_links:
    - from: "src/components/FilterChips.ts"
      to: "src/core/Store.ts"
      via: "store.setState({ activeFilter }) on chip click"
      pattern: "store\\.setState"
    - from: "src/components/GameGrid.ts"
      to: "src/components/GameCard.ts"
      via: "manages child GameCard instances and updates visibility"
      pattern: "new GameCard"
    - from: "src/views/CatalogView.ts"
      to: "src/components/GameGrid.ts"
      via: "mounts GameGrid and FilterChips"
      pattern: "mount"
---

<objective>
Build interactive catalog components (`FilterChips`, `GameCard`, `GameGrid`, and `CatalogView`) extending `BaseComponent` with live filtering, keyboard accessibility, persistent high score displays, and cyber-arcade visual tokens.

Purpose: Deliver responsive live search/filter integration and rich arcade game cards with high score persistence satisfying COMP-03 and COMP-04.
Output: `src/components/FilterChips.ts`, `src/components/GameCard.ts`, `src/components/GameGrid.ts`, `src/views/CatalogView.ts`, component CSS, and Vitest test suites.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@src/core/types.ts
@src/core/Component.ts
@src/core/Store.ts
@src/data/games.ts
@src/styles/tokens.css
@src/styles/theme.css

<interfaces>
<!-- Key types and contracts from Phase 9 and Plan 10-01 -->
From src/data/games.ts:
```typescript
export interface GameItem {
  id: string;
  title: string;
  genre: string;
  description: string;
  badge?: string;
  rating: string;
  plays: string;
  icon: string;
  themeColor: string;
  bannerBg: string;
  features: string[];
}

export const GAMES: GameItem[];
export function getPersonalHighScore(slug: string): number;
```

From src/core/Store.ts:
```typescript
export class Store<T extends object> {
  public getState(): Readonly<T>;
  public setState(patch: Partial<T>): void;
  public subscribe(listener: Listener<T>): () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement FilterChips and GameCard with Tests</name>
  <files>src/components/FilterChips.ts, src/components/GameCard.ts, src/styles/components/chips.css, src/styles/components/cards.css, test/components/chips.test.ts, test/components/cards.test.ts</files>
  <behavior>
    - `FilterChips` renders chip buttons for `all` (All Games), `action` (Action & Breakout), `arcade` (Classic Arcade), and `casual` (Endless Jumpers).
    - Clicking a chip updates `activeFilter` in `Store` and highlights the active chip with neon border / fill.
    - `FilterChips.update(state)` updates `.active` CSS class on chips when store state changes.
    - `GameCard` renders game icon, title, genre badge, rating, plays count, and personal high score formatted with `toLocaleString()`.
    - If personal high score is 0 or unavailable, `GameCard` displays fallback `---`.
    - `GameCard` element has `tabindex="0"`, `role="button"`, and ARIA label for keyboard accessibility.
    - Clicking or pressing `Enter` on `GameCard` navigates to `#/game/:id`.
    - `GameCard` applies hover / focus-visible neon glow using `--glow-cyan` and `--border-accent`.
    - `GameCard.update(props)` updates high score or props dynamically without DOM replacement.
  </behavior>
  <action>
    1. Create `src/styles/components/chips.css`:
       - Scrollable horizontal chips bar with cyber-arcade surface, neon active state, and smooth transitions.
    2. Create `src/styles/components/cards.css`:
       - Interactive game card layout with 16:9 thumbnail preview, badge overlay, title, rating, high score row, hover scale, and keyboard `:focus-visible` neon outline.
    3. Create `src/components/FilterChips.ts` extending `BaseComponent<AppState>`:
       - Accept `Store<AppState>`.
       - Render chips and bind click handlers with `this.addListener`.
       - Update active classes in `update(state)`.
    4. Create `src/components/GameCard.ts` extending `BaseComponent<GameItem, { highScore?: number }>`:
       - Render card with accessible attributes (`tabindex="0"`, `role="button"`, `aria-label`).
       - Bind click and `keydown` (`Enter`/`Space`) to trigger navigation to `#/game/${game.id}`.
       - Implement `update(game, state)` to patch high score text and metadata cleanly.
    5. Write unit tests in `test/components/chips.test.ts` and `test/components/cards.test.ts` verifying rendering, interaction, state dispatch, accessibility, and high score display.
  </action>
  <verify>
    <automated>pnpm test test/components/chips.test.ts test/components/cards.test.ts</automated>
  </verify>
  <done>FilterChips and GameCard components implemented and verified with passing unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GameGrid and CatalogView with Live Filtering and Tests</name>
  <files>src/components/GameGrid.ts, src/views/CatalogView.ts, src/styles/components/catalog.css, test/views/catalog.test.ts</files>
  <behavior>
    - `GameGrid` instantiates and manages `GameCard` instances for all items in `GAMES`.
    - `GameGrid.update(state)` filters visible cards based on `state.searchQuery` and `state.activeFilter` by toggling `display: none` or `.hidden` class on card elements (preserving DOM instances and avoiding input focus drops).
    - If search yields zero results, `GameGrid` renders a cyber-arcade "No games found" empty state message.
    - `CatalogView` renders:
      1. `FilterChips` bar at the top.
      2. Hero banner marquee featuring top game (Sky Hopper) with "Play Instant" button navigating to `#/game/sky-hopper`.
      3. Section title ("Instant Playables") and subtitle.
      4. `GameGrid` container.
    - `CatalogView.update(state)` propagates state updates to child `FilterChips`, Hero high score, and `GameGrid`.
    - `CatalogView.destroy()` calls `destroy()` on all child components and removes event listeners.
  </behavior>
  <action>
    1. Create `src/styles/components/catalog.css`:
       - Responsive grid layout (`repeat(auto-fill, minmax(260px, 1fr))`), featured hero marquee, empty search state styling, and cyber-arcade typography.
    2. Create `src/components/GameGrid.ts` extending `BaseComponent<AppState>`:
       - Instantiate child `GameCard` instances.
       - In `update(state)`, filter `GAMES` list by search query (matching title, genre, description) and active filter (matching category mapping).
       - Toggle `.is-hidden` or card visibility on child cards. Show empty state element when count is 0.
       - Implement `destroy()` to properly call `destroy()` on each child card.
    3. Create `src/views/CatalogView.ts` extending `BaseComponent<AppState>`:
       - Mount child `FilterChips` and `GameGrid`.
       - Render featured Hero banner with dynamic high score and instant launch button.
       - Implement `update(state)` to forward updates to children and hero score.
       - Implement `destroy()` with cascading teardown of children.
    4. Write comprehensive tests in `test/views/catalog.test.ts` verifying live filtering without DOM rebuilding, search matching, empty state handling, hero actions, and cascading destruction.
  </action>
  <verify>
    <automated>pnpm test test/views/catalog.test.ts</automated>
  </verify>
  <done>GameGrid and CatalogView implemented with live non-destructive filtering and passing unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filter / Search Query → Grid Visibility | Filter strings matching against game catalog items |
| Keyboard Input → Route Navigation | User Enter / Space key events triggering route hash updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Denial of Service / DOM Thrashing | `GameGrid.ts` | mitigate | Use CSS `.is-hidden` class toggling on existing card nodes rather than recreating card DOM nodes on every keystroke |
| T-10-04 | Tampering / Malformed Input | `GameGrid.ts` | mitigate | Sanitize and normalize search query with `toLowerCase().trim()` and safe string `.includes()` checks |
</threat_model>

<verification>
```bash
pnpm test test/components/chips.test.ts test/components/cards.test.ts test/views/catalog.test.ts
pnpm test
pnpm typecheck
```
</verification>

<success_criteria>
- `FilterChips` provides reactive genre filtering connected to Store.
- `GameCard` renders rich metadata, personal high scores, hover glow, and keyboard focus.
- `GameGrid` and `CatalogView` deliver smooth live filtering without DOM thrashing or focus loss.
- All unit tests pass with zero errors.
</success_criteria>

<output>
After completion, create `.planning/phases/10-hub-views/10-02-SUMMARY.md`
</output>
