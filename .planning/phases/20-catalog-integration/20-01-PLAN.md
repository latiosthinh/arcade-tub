---
phase: 20-catalog-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/games.ts
  - src/data/screenshots.ts
  - src/components/FilterChips.ts
  - src/components/GameGrid.ts
  - test/components/chips.test.ts
  - test/views/catalog.test.ts
autonomous: true
requirements:
  - HUB-01
must_haves:
  truths:
    - "Hub catalog registers all 12 games with complete metadata, icons, genres, and theme colors"
    - "All 12 games have authentic, high-fidelity SVG screenshots matching in-game canvas visuals"
    - "Filter chips and GameGrid correctly categorize all 12 games into all, action, arcade, puzzle, and casual filters"
  artifacts:
    - path: "src/data/games.ts"
      provides: "GAMES array containing all 12 games"
      contains: "memory-cards"
    - path: "src/data/screenshots.ts"
      provides: "GAME_SCREENSHOTS object with 12 SVG screenshot entries"
      contains: "game-2048"
    - path: "src/components/FilterChips.ts"
      provides: "Arcade genre filter buttons"
      contains: "puzzle"
    - path: "src/components/GameGrid.ts"
      provides: "GameGrid filtering logic for all 12 games"
      contains: "virus-defense"
  key_links:
    - from: "src/components/GameGrid.ts"
      to: "src/data/games.ts"
      via: "GAMES import"
      pattern: "import.*GAMES.*from.*data/games"
    - from: "src/components/GameCard.ts"
      to: "src/data/screenshots.ts"
      via: "GAME_SCREENSHOTS lookup"
      pattern: "GAME_SCREENSHOTS\\[game\\.id\\]"
---

<objective>
Register all 7 new games in `src/data/games.ts` (`GAMES` catalog) with complete metadata, genre definitions, ratings, play counts, features, and custom color themes. Provide high-fidelity, lightweight SVG screenshots in `src/data/screenshots.ts` for the 7 new games. Update `FilterChips.ts` and `GameGrid.ts` to cleanly filter all 12 games across Action, Arcade, Puzzle, and Casual categories, updating related component test suites.

Purpose: Expand the Arcade Carnival hub UI to showcase all 12 games with authentic visual previews and seamless genre navigation.
Output: Updated catalog data, SVG screenshot bank, filter components, and updated unit test suites.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/data/games.ts
@src/data/screenshots.ts
@src/components/GameGrid.ts
@src/components/FilterChips.ts
@test/components/chips.test.ts
@test/views/catalog.test.ts

<interfaces>
From src/data/games.ts:
```typescript
export interface GameItem {
  id: string;
  title: string;
  genre: string;
  description: string;
  badge?: string;
  rating: string;
  plays: string;
  icon: string;
  themeColor: string;
  bannerBg: string;
  features: string[];
}
export const GAMES: GameItem[];
export function getPersonalHighScore(slug: string): number;
```

From src/data/screenshots.ts:
```typescript
export const GAME_SCREENSHOTS: Record<string, string>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register 7 new games in GAMES catalog metadata</name>
  <files>src/data/games.ts</files>
  <behavior>
    - GAMES array contains exactly 12 items
    - Includes memory-cards, memory-boxes, pop-balloon, space-racer, virus-defense, flappy-fish, game-2048
    - Each game entry has non-empty title, genre, description, rating, plays, icon, themeColor, bannerBg, and at least 3 features
  </behavior>
  <action>
    Add the 7 new game definitions to `GAMES` in `src/data/games.ts`:
    1. `memory-cards`: title "Memory Cards", genre "Card Matching", icon "🃏", themeColor "#00f0ff", bannerBg "linear-gradient(135deg, #051923 0%, #003554 100%)", features: ['Cyber Glyph Pairs', 'Streak Multipliers', 'Round Timer & High Score']
    2. `memory-boxes`: title "Memory Boxes", genre "Pattern Memory", icon "🔲", themeColor "#a29bfe", bannerBg "linear-gradient(135deg, #191024 0%, #301934 100%)", features: ['Expanding Sequences', 'Synthesized Audio Tones', '3-Strike Life System']
    3. `pop-balloon`: title "Pop Balloon", genre "Balloon Pop / Reflex", icon "🎈", themeColor "#ff7675", bannerBg "linear-gradient(135deg, #2d0c14 0%, #5c182a 100%)", features: ['Ascending Balloon Chains', 'Color Combo Multipliers', 'Hazard Spike Bombs']
    4. `space-racer`: title "Space Racer", genre "Orbital Racing", icon "🚀", themeColor "#0984e3", bannerBg "linear-gradient(135deg, #050e1e 0%, #0c2461 100%)", features: ['Pseudo-3D Starfield Warp', 'Turbo Boost Gates', 'Dynamic Asteroid Kinematics']
    5. `virus-defense`: title "Virus Defense", genre "Radial Turret Defense", icon "🦠", themeColor "#00cec9", bannerBg "linear-gradient(135deg, #041d1a 0%, #0b3c35 100%)", features: ['360° Laser Turret Aiming', 'Mutating Pathogen Swarms', 'Nucleus Shield Defense']
    6. `flappy-fish`: title "Flappy Fish", genre "Hydrodynamic Flapper", icon "🐠", themeColor "#00b894", bannerBg "linear-gradient(135deg, #031e21 0%, #06444a 100%)", features: ['Underwater Flap Kinematics', 'Glowing Coral Reef Obstacles', 'Pearl Pickups & Medals']
    7. `game-2048`: title "2048 Neon", genre "Sliding Tile Puzzle", icon "🔢", themeColor "#fdcb6e", bannerBg "linear-gradient(135deg, #1f1b0a 0%, #3d3514 100%)", features: ['4x4 Directional Merging', 'Neon Tiered Value Tiles', 'Undo Move & Win State']
  </action>
  <verify>
    <automated>npx vitest run test/components/cards.test.ts</automated>
  </verify>
  <done>GAMES array exports all 12 games with full metadata and passes GameCard unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add authentic SVG screenshots for 7 new games</name>
  <files>src/data/screenshots.ts</files>
  <action>
    Add scalable, lightweight (<3-4KB each), neon-styled SVG gameplay mockups to `GAME_SCREENSHOTS` in `src/data/screenshots.ts` matching actual canvas visuals:
    - `memory-cards`: 4x3 holographic cyber grid with flipped glyph cards, glow outlines, score & timer HUD.
    - `memory-boxes`: 3x3 neon button matrix with active flashing tile, tone wave indicator, round & life icons.
    - `pop-balloon`: Ascending colorful balloons (pink, cyan, yellow), spike bomb hazard, floating +500 score popup, combo meter.
    - `space-racer`: Pseudo-3D starfield perspective grid, player cyber-jet with thruster trail, incoming asteroids, glowing turbo gate.
    - `virus-defense`: Circular bio-arena, central nucleus cell with health ring, rotating 360° laser turret firing beams at pathogen germs.
    - `flappy-fish`: Deep ocean background with caustic light rays, cyber neon fish with bubbles, glowing coral pillars top and bottom.
    - `game-2048`: 4x4 neon tile grid with styled number values (2, 4, 8, 16, 64, 2048), pop glow animations, score HUD.
  </action>
  <verify>
    <automated>npx vitest run test/components/cards.test.ts</automated>
  </verify>
  <done>All 12 games have corresponding SVG strings in GAME_SCREENSHOTS rendered by GameCard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update FilterChips and GameGrid categories for 12 games</name>
  <files>src/components/FilterChips.ts, src/components/GameGrid.ts, test/components/chips.test.ts, test/views/catalog.test.ts</files>
  <behavior>
    - FilterChips provides chips for: 'all' (All Games), 'action' (Action & Defense), 'arcade' (Classic Arcade), 'puzzle' (Puzzle & Memory), 'casual' (Casual & Jumpers)
    - GameGrid maps all 12 games:
      - 'action': brick-blitz, type-strike, virus-defense, pop-balloon
      - 'arcade': safe-cracker, crate-catch, space-racer
      - 'puzzle': memory-cards, memory-boxes, game-2048
      - 'casual': sky-hopper, flappy-fish
    - All tests in chips.test.ts and catalog.test.ts pass
  </behavior>
  <action>
    1. Update `FILTERS` in `src/components/FilterChips.ts` to include:
       - `{ id: 'all', label: 'All Games' }`
       - `{ id: 'action', label: 'Action & Defense' }`
       - `{ id: 'arcade', label: 'Classic Arcade' }`
       - `{ id: 'puzzle', label: 'Puzzle & Memory' }`
       - `{ id: 'casual', label: 'Casual & Jumpers' }`
    2. Update `matchesFilter` logic in `src/components/GameGrid.ts` to map all 12 games to their respective category arrays.
    3. Update `test/components/chips.test.ts` to verify 5 filter buttons (all, action, arcade, puzzle, casual).
    4. Update `test/views/catalog.test.ts` to verify filtering behavior across 12 games.
  </action>
  <verify>
    <automated>npx vitest run test/components/chips.test.ts test/views/catalog.test.ts</automated>
  </verify>
  <done>Filter chips render 5 categories, GameGrid filters all 12 games cleanly, and catalog unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG screenshot rendering | Untrusted HTML/SVG injection into DOM innerHTML |
| LocalStorage High Score | Client storage tampering or invalid JSON/NaN values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | `src/data/games.ts` | mitigate | `getPersonalHighScore` safely validates `parseInt` and ignores NaN/negative values. |
| T-20-02 | Information Disclosure / XSS | `src/data/screenshots.ts` | mitigate | SVGs are hardcoded internal static constants without user input or dynamic script tags. |
</threat_model>

<verification>
Run vitest on catalog and component suites:
`npx vitest run test/components/chips.test.ts test/components/cards.test.ts test/views/catalog.test.ts`
</verification>

<success_criteria>
- 12 games defined in `GAMES` with rich metadata.
- 12 authentic SVG screenshots in `GAME_SCREENSHOTS`.
- Filter chips and GameGrid correctly categorize all 12 games.
- All catalog and filter unit tests pass with 100% success rate.
</success_criteria>

<output>
After completion, create `.planning/phases/20-catalog-integration/20-01-SUMMARY.md`
</output>
