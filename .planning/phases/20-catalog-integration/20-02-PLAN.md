---
phase: 20-catalog-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - test/production/bundle-audit.test.ts
  - scripts/audit-bundle.js
autonomous: true
requirements:
  - HUB-02
must_haves:
  truths:
    - "Multi-page Vite production build compiles all 12 standalone game HTML entries, hub, and embed"
    - "Total gzipped distribution bundle size is strictly under 200KB limit and each asset is under 50KB"
    - "Automated test suite achieves 100% pass rate across all 12 games, shared packages, and hub views"
  artifacts:
    - path: "test/production/bundle-audit.test.ts"
      provides: "Production bundle & entrypoint automated test assertions for all 12 games"
      contains: "games/game-2048/index.html"
    - path: "dist/index.html"
      provides: "Compiled production hub entry"
    - path: "dist/games/memory-cards/index.html"
      provides: "Compiled standalone memory-cards game entry"
  key_links:
    - from: "vite.config.ts"
      to: "dist"
      via: "Rollup multi-page inputs"
      pattern: "rollupOptions.*input"
    - from: "scripts/audit-bundle.js"
      to: "dist"
      via: "Filesystem scan and gzip calculation"
      pattern: "zlib\\.gzipSync"
---

<objective>
Update `test/production/bundle-audit.test.ts` to verify all 12 game entry points in the multi-page Vite production build. Execute Vite build, run bundle audit script to verify < 200KB gzipped budget compliance, and execute the entire workspace unit test suite across all 12 games, hub components, views, and packages to ensure a 100% pass rate.

Purpose: Guarantee release-ready packaging, rock-solid stability, performance budget compliance, and complete standalone multi-entry support.
Output: Verified production distribution bundle, updated bundle audit test, and clean test pass reports.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@vite.config.ts
@scripts/audit-bundle.js
@test/production/bundle-audit.test.ts

<interfaces>
From vite.config.ts:
```typescript
// Rollup input configuration covers hub, embed, and all 12 standalone game directories:
// 'safe-cracker', 'brick-blitz', 'sky-hopper', 'crate-catch', 'type-strike',
// 'memory-cards', 'memory-boxes', 'pop-balloon', 'space-racer',
// 'virus-defense', 'flappy-fish', 'game-2048'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update bundle audit test suite for 12 games</name>
  <files>test/production/bundle-audit.test.ts</files>
  <behavior>
    - test/production/bundle-audit.test.ts asserts presence of all 12 standalone game HTML entries in dist:
      - 'games/safe-cracker/index.html'
      - 'games/brick-blitz/index.html'
      - 'games/sky-hopper/index.html'
      - 'games/crate-catch/index.html'
      - 'games/type-strike/index.html'
      - 'games/memory-cards/index.html'
      - 'games/memory-boxes/index.html'
      - 'games/pop-balloon/index.html'
      - 'games/space-racer/index.html'
      - 'games/virus-defense/index.html'
      - 'games/flappy-fish/index.html'
      - 'games/game-2048/index.html'
    - Asserts total dist bundle is < 200KB gzipped
    - Asserts every asset is < 50KB gzipped
  </behavior>
  <action>
    Update `test/production/bundle-audit.test.ts` `requiredEntries` list to include all 12 games.
  </action>
  <verify>
    <automated>npm run build; if ($?) { npx vitest run test/production/bundle-audit.test.ts }</automated>
  </verify>
  <done>All 12 game entries verified in production dist directory and bundle audit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Execute bundle audit and full workspace test suite</name>
  <files>scripts/audit-bundle.js</files>
  <action>
    1. Run `npm run build` to generate clean production output in `dist/`.
    2. Run `node scripts/audit-bundle.js` and verify total gzipped bundle size is well under 200KB (currently ~103KB) and all individual assets are under 50KB.
    3. Run `npm test` across the entire workspace to ensure 100% pass rate across all 12 games, packages, views, and core components.
  </action>
  <verify>
    <automated>node scripts/audit-bundle.js; if ($?) { npm test }</automated>
  </verify>
  <done>Bundle audit passes under 200KB gzipped limit and all workspace tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Multi-page HTML entrypoints | Standalone game bundles loaded inside iframe or direct browser navigation |
| Static Build Artifacts | Production distribution assets served over CDN / web server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Denial of Service | Build Bundle Distribution | mitigate | Strict 200KB total gzipped & 50KB per-file bundle budget enforced in CI / build test to ensure instant loading. |
| T-20-04 | Elevation of Privilege | Standalone Game iframes | mitigate | All game iframes configured with strict sandbox / allow policy (`autoplay; fullscreen`) without credential access. |
</threat_model>

<verification>
Execute build, bundle audit, and full test suite:
`npm run build; if ($?) { node scripts/audit-bundle.js; if ($?) { npm test } }`
</verification>

<success_criteria>
- Multi-page build produces valid bundles for all 12 games, hub, and embed.
- Bundle audit confirms total gzipped size < 200KB.
- 100% of workspace tests pass without failures.
</success_criteria>

<output>
After completion, create `.planning/phases/20-catalog-integration/20-02-SUMMARY.md`
</output>
