---
phase: 13-memory-cards
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - games/memory-cards/src/MemoryCardsScene.ts
  - games/memory-cards/src/Particles.ts
  - games/memory-cards/src/CardRenderer.ts
  - games/memory-cards/src/main.ts
  - games/memory-cards/index.html
  - vite.config.ts
  - games/memory-cards/test/particles.test.ts
autonomous: true
requirements:
  - MC-02

must_haves:
  truths:
    - "Cards render on canvas with cyber holographic borders and smooth 3D flip perspective effect"
    - "Cyber glyphs render clearly on card face with distinct neon colors and vector geometry"
    - "Particle sparkles explode upon card pair match"
    - "Procedural Web Audio triggers on card flip, match, error, win, and gameover"
    - "Player can interact via mouse click or touch tap to flip cards, restart, and navigate overlays"
    - "Game is integrated with GameLoop, InputManager, and playables-adapter lifecycle"
  artifacts:
    - path: "games/memory-cards/src/MemoryCardsScene.ts"
      provides: "Main GameScene implementation for Memory Cards minigame"
      min_lines: 150
    - path: "games/memory-cards/src/CardRenderer.ts"
      provides: "Holographic card 3D flip rendering, neon glyph vector drawing, and selection highlights"
      min_lines: 100
    - path: "games/memory-cards/src/Particles.ts"
      provides: "Particle sparkle effects on card matching and fanfare"
      min_lines: 50
    - path: "games/memory-cards/src/main.ts"
      provides: "Vite entry point initializing GameLoop, Playables adapter, and Scene"
      min_lines: 20
    - path: "games/memory-cards/index.html"
      provides: "HTML container for Memory Cards minigame"
      contains: "canvas id=\"game\""
  key_links:
    - from: "games/memory-cards/src/MemoryCardsScene.ts"
      to: "@arcade-carnival/game-engine"
      via: "GameScene, InputManager, audio synthesizer"
      pattern: "GameScene|InputManager|audio"
    - from: "games/memory-cards/src/main.ts"
      to: "games/memory-cards/src/MemoryCardsScene.ts"
      via: "Scene instantiation and GameLoop assignment"
      pattern: "MemoryCardsScene"
    - from: "vite.config.ts"
      to: "games/memory-cards/index.html"
      via: "Rollup input configuration"
      pattern: "memory-cards"
---

<objective>
Implement Canvas 2D Presentation and Engine Integration for Memory Cards Minigame:
1. Holographic card flip 3D perspective effect with neon borders and custom vector cyber glyphs.
2. Particle system for match sparkles and win celebrations.
3. Procedural audio triggers (card flip, match fanfare, mismatch buzzer, victory jingle, gameover explosion).
4. MemoryCardsScene wiring with InputManager (click/touch), GameLoop, responsive scaling, pause/resume, and overlay screens (Ready, Paused, GameOver, Victory).
5. Standalone `index.html`, entry `main.ts`, and Vite build rollup input registration.

Purpose: Complete the interactive visual and audio experience for Memory Cards minigame.
Output: `MemoryCardsScene.ts`, `CardRenderer.ts`, `Particles.ts`, `main.ts`, `index.html`, updated `vite.config.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@packages/game-engine/src/index.ts
@packages/game-engine/src/AudioSynthesizer.ts
@games/safe-cracker/src/SafeCrackerScene.ts
@games/safe-cracker/src/Particles.ts
@games/memory-cards/src/CardGrid.ts
@games/memory-cards/src/GameState.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CardRenderer with 3D flip animation, cyber glyphs, and Particle system</name>
  <files>games/memory-cards/src/CardRenderer.ts, games/memory-cards/src/Particles.ts, games/memory-cards/test/particles.test.ts</files>
  <action>
    1. Implement `games/memory-cards/src/Particles.ts` for glowing neon burst particles (sparkles with velocity, life decay, cyan/gold/magenta color palettes) and write `test/particles.test.ts`.
    2. Implement `games/memory-cards/src/CardRenderer.ts`:
       - Card geometry: width ~110px, height ~130px, rounded corners, responsive grid layout centered in 800x600 canvas (4x4 with 20px padding).
       - Card back: Cyber grid pattern, glowing cyan border with corner brackets.
       - Card 3D Flip effect: Calculate horizontal perspective scale `Math.abs(Math.cos(flipProgress * Math.PI))` and draw card face or back depending on half-way point (`flipProgress >= 0.5`).
       - Cyber Glyphs vector rendering for each glyph type:
         * 'CYBER_CHIP': Microchip tracks with glowing central square
         * 'NEON_SKULL': Stylized cyber skull with neon eye sockets
         * 'QUANTUM_NODE': Multi-ring atomic node with orbital electrons
         * 'MATRIX_KEY': Futuristic cryptographic key silhouette
         * 'CIRCUIT_CORE': Radiating hexagonal power core
         * 'DATA_ORB': Layered concentric energetic ring orb
         * 'WARP_GATE': Dual spiral hyper-jump ring portal
         * 'BIO_HAZARD': Cyber bio-synthetic hazard emblem
       - Matched card glow: Pulsing golden/cyan aura.
  </action>
  <verify>
    <automated>pnpm test games/memory-cards/test/particles.test.ts</automated>
  </verify>
  <done>CardRenderer and Particle system created with vector cyber glyphs and passing particle tests.</done>
</task>

<task type="auto">
  <name>Task 2: Build MemoryCardsScene, wire Audio/Input/Playables, create HTML entry & register in Vite</name>
  <files>games/memory-cards/src/MemoryCardsScene.ts, games/memory-cards/src/main.ts, games/memory-cards/index.html, vite.config.ts</files>
  <action>
    1. Implement `games/memory-cards/src/MemoryCardsScene.ts` implementing `GameScene`:
       - Manage `CardGrid`, `GameState`, `CardRenderer`, `ParticleSystem`, and `InputManager`.
       - Handle card click/tap hit detection based on grid bounds.
       - Manage flip animations (transitioning `flipProgress` from 0 to 1 over ~0.25s).
       - Trigger audio via `audio.playClick()` on flip, `audio.playScore()` / `audio.playPowerup()` on match, `audio.playError()` on mismatch, `audio.playVictory()` on full board clear, and `audio.playExplosion()` on timer expiration.
       - Spawn particle sparkles on card center upon match.
       - Manage mismatch timeout delay (~0.6s) before flipping mismatch cards back down.
       - Render cyber matrix background, HUD (Score, High Score, 60s countdown bar, Combo streak multiplier), and modal overlays (Ready, Paused, Game Over, Victory).
       - Support mouse click, touch tap, and keyboard shortcuts (Space to start/restart, Esc to pause).
    2. Create `games/memory-cards/src/main.ts` with `initPlayables()`, `GameLoop`, `InputManager`, `onPause`, `onResume`.
    3. Create `games/memory-cards/index.html` loading `src/main.ts` with CSS tokens and canvas container.
    4. Register `'memory-cards': resolve(__dirname, 'games/memory-cards/index.html')` in `vite.config.ts` build inputs.
  </action>
  <verify>
    <automated>pnpm test && pnpm typecheck</automated>
  </verify>
  <done>MemoryCardsScene fully wired with canvas rendering, audio, input controls, standalone HTML, and clean build/typecheck.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| User Input Coordinates | Pointer/Touch events converted to canvas coordinate space |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-13-03 | Denial of Service | ParticleSystem | mitigate | Cap max active particles at 200 to maintain 60fps |
| T-13-04 | Tampering | Canvas Input Coordinates | mitigate | Scale client coordinates using canvas bounding client rect and scale factors |
</threat_model>

<verification>
Full test suite and type check:
`pnpm test && pnpm typecheck`
</verification>

<success_criteria>
1. Memory Cards launches cleanly in canvas with 60fps cyber aesthetic.
2. Cards flip smoothly with 3D horizontal perspective compression.
3. Matching pairs trigger particle sparkles and audio fanfare.
4. Round ends gracefully on timer expiry or full board clear with score reporting.
5. All workspace tests pass 100% and TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/13-memory-cards/13-02-SUMMARY.md`
</output>
