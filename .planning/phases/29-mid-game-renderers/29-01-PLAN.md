---
phase: 29-mid-game-renderers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - games/brick-blitz/src/BrickBlitzScene.ts
  - games/brick-blitz/src/BrickGrid.ts
  - games/crate-catch/src/CrateCatchScene.ts
  - games/type-strike/src/TypeStrikeScene.ts
autonomous: true
requirements:
  - CRAFT-06
must_haves:
  truths:
    - "Brick Blitz renders with kraft paper background, layered cardboard brick grid with papercut texture, wooden craft paddle with tape bumpers, paper ball, and sticky note HUD"
    - "Crate Catch renders with craft paper factory background, cardboard dual-track cart, construction paper crates with tape crosses, and sticky note status placards"
    - "Type Strike renders with papercut command terminal, papercraft origami enemies, cardboard turret launcher, sticky note challenge banner, and paper particle lasers"
    - "All unit tests for brick-blitz, crate-catch, and type-strike pass without regression"
  artifacts:
    - path: "games/brick-blitz/src/BrickBlitzScene.ts"
      provides: "Kraft background, wooden craft paddle, paper ball, cardboard brick rendering, sticky note HUD and gameover overlays"
      contains: "Patrick Hand"
    - path: "games/crate-catch/src/CrateCatchScene.ts"
      provides: "Craft paper factory background, cardboard cart, papercut crates with tape crosses, taped status placards"
      contains: "Patrick Hand"
    - path: "games/type-strike/src/TypeStrikeScene.ts"
      provides: "Papercut command terminal, origami enemy cards, cardboard turret launcher, sticky note challenge banner"
      contains: "Patrick Hand"
  key_links:
    - from: "games/brick-blitz/src/BrickBlitzScene.ts"
      to: "games/brick-blitz/src/BrickGrid.ts"
      via: "BrickGrid collision and level rendering"
      pattern: "this\\.brickGrid"
    - from: "games/crate-catch/src/CrateCatchScene.ts"
      to: "games/crate-catch/src/Cart.ts"
      via: "Cart position and dual lane coordinates"
      pattern: "this\\.cart"
    - from: "games/type-strike/src/TypeStrikeScene.ts"
      to: "games/type-strike/src/TypingEngine.ts"
      via: "Typing engine active word and streak state"
      pattern: "this\\.typingEngine"
---

<objective>
Convert canvas renderers and scene presentations for 3 mid-complexity games (`brick-blitz`, `crate-catch`, `type-strike`) to the 2D Papercraft visual aesthetic.
1. `brick-blitz`: Replace dark synthwave canvas with kraft paper background (`#F4EAD4`), layered cardboard brick grid (`#C5A880`, `#D8C3A5`, construction primaries `#E11D48`, `#3B82F6`, `#F59E0B`, `#10B981`) with papercut texture and ink borders (`#3E2723`), wooden craft paddle with tape strip bumpers, paper sphere ball, and sticky note HUD/pause/gameover cards.
2. `crate-catch`: Replace dark steampunk industrial look with craft paper factory background (`#F4EAD4` / `#E8DEC8` with stamped gear outlines and paper pipes), cardboard dual-track cart with tape accents, construction paper crates with paper tape crosses and corner staples, paper bomb cutouts, and taped sticky-note HUD and life tags.
3. `type-strike`: Replace neon cyberpunk terminal with papercut command desk (`#F4EAD4` parchment background with stitched radar wireframe and ink guides), papercraft origami enemy placards with inked glyphs, cardboard turret launcher with brass fastener pivot, sticky note challenge banner, and paper laser/confetti effects.

Purpose: Deliver tactile 2D papercraft in-game visuals for the first 3 mid-complexity games in Milestone v5.0.
Output: Updated `games/brick-blitz/src/BrickBlitzScene.ts`, `games/brick-blitz/src/BrickGrid.ts`, `games/crate-catch/src/CrateCatchScene.ts`, `games/type-strike/src/TypeStrikeScene.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@games/brick-blitz/src/BrickBlitzScene.ts
@games/brick-blitz/src/BrickGrid.ts
@games/crate-catch/src/CrateCatchScene.ts
@games/type-strike/src/TypeStrikeScene.ts

<interfaces>
<!-- Papercraft Canvas Theme Tokens -->
const PAPERCRAFT = {
  bgParchment: '#F4EAD4',
  bgCard: '#FFFDF8',
  bgKraft: '#D8C3A5',
  bgCardboard: '#C5A880',
  woodPaddle: '#C85A32',
  woodStrip: '#E8DEC8',
  ink: '#3E2723',
  inkLight: 'rgba(62, 39, 35, 0.4)',
  red: '#E11D48',
  blue: '#3B82F6',
  yellow: '#F59E0B',
  green: '#10B981',
  purple: '#8B5CF6',
  pink: '#EC4899',
  tape: 'rgba(255, 248, 220, 0.85)',
  tapeBorder: 'rgba(62, 39, 35, 0.2)',
  fontDisplay: '"Patrick Hand", cursive, sans-serif',
  fontBody: '"Comfortaa", cursive, sans-serif',
  fontSketch: '"Cabin Sketch", cursive, sans-serif',
};
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brick Blitz Papercraft Visuals</name>
  <files>games/brick-blitz/src/BrickBlitzScene.ts, games/brick-blitz/src/BrickGrid.ts</files>
  <action>
    Transform Brick Blitz from dark synthwave arcade into a handmade cardboard and paper breakout game.
    1. In `games/brick-blitz/src/BrickGrid.ts`:
       - Update brick standard colors to construction paper palette (`#3B82F6` blue, `#E11D48` red, `#F59E0B` yellow, `#10B981` green, `#8B5CF6` durable purple, `#F59E0B` bonus gold, `#E11D48` life red).
    2. In `games/brick-blitz/src/BrickBlitzScene.ts`:
       - In `renderBackground`: Draw warm parchment background (`#F4EAD4`), stitched/dashed ink border guide lines (`rgba(62, 39, 35, 0.15)`), and paper tape corner accents.
       - In `renderBricks`: Render each brick as a layered construction paper cutout with crisp `#3E2723` inked borders, paper drop shadow (`3px 3px 0px rgba(62, 39, 35, 0.2)`), inner paper grain/crease highlights, and cracked paper fold lines when durable bricks take damage.
       - In `renderPaddle`: Render wooden craft stick paddle (`#C85A32` base, `#E8DEC8` center grain strip) with semi-transparent paper tape strips on the left/right bumper edges (`rgba(255, 248, 220, 0.85)` with `#3E2723` dashed edges).
       - In `renderBall`: Render paper ball with subtle concentric paper disc texture, warm `#F59E0B` / `#FFFDF8` tone, and `#3E2723` inked contour.
       - In `renderHUD`: Replace dark HUD header with a taped craft paper placard using `"Patrick Hand"` and `"Comfortaa"` fonts, paper hearts for lives, and ink score tags.
       - In `renderOverlays` (Paused, GameOver, Ready): Render modals as pinned cardboard placards with tape strips, craft typography, and papercut button elements.
  </action>
  <verify>
    <automated>npx vitest run games/brick-blitz</automated>
  </verify>
  <done>Brick Blitz renders with cardboard brick grid, wooden craft paddle with tape bumpers, paper ball, sticky note HUD; all brick-blitz unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Crate Catch Papercraft Visuals</name>
  <files>games/crate-catch/src/CrateCatchScene.ts</files>
  <action>
    Transform Crate Catch from dark steampunk factory into a handmade craft paper and cardboard factory.
    1. In `games/crate-catch/src/CrateCatchScene.ts`:
       - In `renderBackground`: Replace dark gradient with craft paper background (`#F4EAD4`), stamped paper gear silhouettes (`rgba(62, 39, 35, 0.08)`), cardboard conveyor tracks (`#C5A880` with dashed ink rail lines `#3E2723`), and paper pipe accents.
       - In `renderTracks`: Render front and back conveyor lanes as layered corrugated cardboard strips with inked track ties.
       - In `drawFallingItem`:
         - Normal/Golden/Large crates: Render as construction paper boxes (`#F59E0B` front lane amber, `#3B82F6` back lane blue, `#10B981` golden) with paper tape crosses (`rgba(255, 248, 220, 0.9)` with ink borders), cardboard corner braces, and drop shadows (`3px 3px 0px rgba(62, 39, 35, 0.25)`).
         - Bombs: Render as papercut dark brown discs with paper fuse, red warning tape, and ink outline (`#3E2723`).
         - Powerups (Repair toolbox, Magnetic shield): Render as papercut first-aid kit and paper origami shield badge with ink outlines.
       - In `renderCartAndStack`: Render dual-track catcher cart as a corrugated cardboard basket mounted on paper wheel cutouts, with stacked caught crates showing distinct paper cut edges and tilt wobble.
       - In `renderHUD`: Render HP meter on a kraft paper clipboard, score multiplier on paper bookmark tags, and score digits in `"Patrick Hand"`.
       - In `renderOverlays`: Render Ready, Paused, and GameOver screens as taped cardboard cards with craft typography.
  </action>
  <verify>
    <automated>npx vitest run games/crate-catch</automated>
  </verify>
  <done>Crate Catch renders with craft paper factory background, cardboard cart, papercut crates with tape crosses, and sticky note status cards; all crate-catch unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Type Strike Papercraft Visuals</name>
  <files>games/type-strike/src/TypeStrikeScene.ts</files>
  <action>
    Transform Type Strike from dark cyber command terminal into a papercraft typing defense command desk.
    1. In `games/type-strike/src/TypeStrikeScene.ts`:
       - In `render`:
         - Background: Replace dark cyber background with warm parchment desk (`#F4EAD4`), tactical stamped radar rings (`rgba(62, 39, 35, 0.12)`), stitched/dashed ink lane divider guidelines (`rgba(62, 39, 35, 0.2)`), and faint calligraphy/typewriter rain characters (`rgba(62, 39, 35, 0.1)`).
         - Defender zone & Turret: Render left defender barrier as a cardboard fortress strip (`#C5A880`) with paper shield indicator bar, and command turret as a layered cardboard launcher with brass fastener hub (`#F59E0B`) and ink pivot barrel.
         - Enemies: Render approaching word targets as papercraft origami planes/creatures with craft card backing (`#FFFDF8`), ink borders (`#3E2723`), typed letters rendered in `"Patrick Hand"` with matching typed prefix highlighted in bright construction green (`#10B981`) or yellow (`#F59E0B`).
         - HUD & Challenge Banner: Render top stats bar on a taped kraft paper placard with 60-second timer tape bar, multiplier paper badges, and sticky note game start/game over overlays.
  </action>
  <verify>
    <automated>npx vitest run games/type-strike</automated>
  </verify>
  <done>Type Strike renders with papercut command terminal, origami enemy cards, cardboard turret launcher, sticky note challenge banner; all type-strike unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Canvas 2D Transform & Input | User pointer and keyboard events feeding into canvas coordinate transforms |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | Canvas Rendering | mitigate | Balance all `ctx.save()` / `ctx.restore()` calls strictly; bound all path coordinates to viewport bounds |
| T-29-02 | Denial of Service | Particle & Geometry Calculations | mitigate | Keep particle caps clamped and reuse path objects without heap thrashing in render loops |
</threat_model>

<verification>
Automated unit tests for brick-blitz, crate-catch, and type-strike pass:
`npx vitest run games/brick-blitz games/crate-catch games/type-strike`
</verification>

<success_criteria>
Brick Blitz, Crate Catch, and Type Strike fully converted to 2D papercraft visual identity (kraft/cardboard backgrounds, papercut sprites/crates/cards, ink borders, craft typography, and sticky-note overlays).
</success_criteria>

<output>
After completion, create `.planning/phases/29-mid-game-renderers/29-01-SUMMARY.md`
</output>
