---
phase: 29-mid-game-renderers
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - games/flappy-fish/src/FishRenderer.ts
  - games/sky-hopper/src/SkyHopperScene.ts
  - src/data/games.ts
autonomous: true
requirements:
  - CRAFT-06
  - CRAFT-08
must_haves:
  truths:
    - "Flappy Fish renders with kraft/parchment water background, paper cutout fish with ink outlines, cardboard coral pillars with paper tape accents, glowing paper pearls, and sticky note HUD"
    - "Sky Hopper renders with layered parchment altitude gradient, papercut star stickers, cardboard platforms with tape/spring craft props, papercut hopper character, origami drone obstacles, and pinned note overlays"
    - "src/data/games.ts catalog metadata (titles, descriptions, theme colors, banner gradients) updated for all 5 mid-complexity games to match 2D papercraft aesthetic"
    - "Full project unit test suite and production build pass cleanly"
  artifacts:
    - path: "games/flappy-fish/src/FishRenderer.ts"
      provides: "Parchment underwater background, cardboard coral pillars, papercut fish with inked contour, sticky note HUD and gameover placards"
      contains: "Patrick Hand"
    - path: "games/sky-hopper/src/SkyHopperScene.ts"
      provides: "Parchment altitude gradient, papercut platforms, origami drone and spike obstacles, cardboard mothership, sticky note HUD"
      contains: "Patrick Hand"
    - path: "src/data/games.ts"
      provides: "Updated catalog entries for brick-blitz, crate-catch, type-strike, flappy-fish, and sky-hopper"
      contains: "linear-gradient(135deg, #F4EAD4"
  key_links:
    - from: "games/flappy-fish/src/FlappyFishScene.ts"
      to: "games/flappy-fish/src/FishRenderer.ts"
      via: "FishRenderer render pipeline"
      pattern: "this\\.renderer\\.render"
    - from: "games/sky-hopper/src/SkyHopperScene.ts"
      to: "games/sky-hopper/src/PlatformManager.ts"
      via: "PlatformManager platform instances"
      pattern: "this\\.platformManager\\.platforms"
    - from: "src/data/games.ts"
      to: "index.html"
      via: "Hub game catalog cards"
      pattern: "GAMES"
---

<objective>
Convert canvas renderers for `flappy-fish` and `sky-hopper` to the 2D Papercraft visual aesthetic, update catalog metadata in `src/data/games.ts` for all 5 mid-complexity games, and verify tests and build across the project.
1. `flappy-fish`: Replace legacy underwater/neon styling with warm kraft/parchment water background (`#F4EAD4`), cardboard coral pillars (`#C5A880`, `#4A6D56`, `#C85A32`) with paper tape caps and inked edges (`#3E2723`), layered papercut fish with hand-drawn eye and fin wobble, glowing paper pearl cutouts, and taped sticky-note HUD/modals with `"Patrick Hand"` and `"Comfortaa"` typography.
2. `sky-hopper`: Replace dark synthwave sky gradient with tactile altitude parchment tones (craft cream to warm sunset cardboard to deep ink wash), papercut star stickers, cardboard/construction paper platforms (`#10B981` standard, `#E8DEC8` fragile dashed, `#3B82F6` moving with ink arrows, `#8B5CF6` spring with paper coil), papercraft origami drone/spire/balloon obstacles, papercut hopper hero with cardboard visor and rocket booster sparks, and taped note HUD/modals.
3. `src/data/games.ts`: Update titles, descriptions, theme colors, and banner gradients for `brick-blitz`, `crate-catch`, `type-strike`, `flappy-fish`, and `sky-hopper` to reflect the 2D papercraft theme.
4. Verify all tests and full production build pass.

Purpose: Complete the papercraft visual transition for all 5 mid-complexity games in Milestone v5.0 and ensure catalog consistency.
Output: Updated `games/flappy-fish/src/FishRenderer.ts`, `games/sky-hopper/src/SkyHopperScene.ts`, and `src/data/games.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@games/flappy-fish/src/FishRenderer.ts
@games/flappy-fish/src/FlappyFishScene.ts
@games/sky-hopper/src/SkyHopperScene.ts
@src/data/games.ts

<interfaces>
<!-- Papercraft Canvas Theme Tokens -->
const PAPERCRAFT = {
  bgParchment: '#F4EAD4',
  bgCard: '#FFFDF8',
  bgKraft: '#D8C3A5',
  bgCardboard: '#C5A880',
  ink: '#3E2723',
  inkLight: 'rgba(62, 39, 35, 0.4)',
  red: '#E11D48',
  blue: '#3B82F6',
  yellow: '#F59E0B',
  green: '#10B981',
  purple: '#8B5CF6',
  pink: '#EC4899',
  tape: 'rgba(255, 248, 220, 0.85)',
  tapeBorder: 'rgba(62, 39, 35, 0.2)',
  fontDisplay: '"Patrick Hand", cursive, sans-serif',
  fontBody: '"Comfortaa", cursive, sans-serif',
  fontSketch: '"Cabin Sketch", cursive, sans-serif',
};
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flappy Fish Papercraft Visuals</name>
  <files>games/flappy-fish/src/FishRenderer.ts</files>
  <action>
    Transform Flappy Fish from cyber-ocean into a handmade papercraft aquarium storybook flapper.
    1. In `games/flappy-fish/src/FishRenderer.ts`:
       - Background & Caustics: Render warm kraft/parchment background (`#F4EAD4`) with soft water ripples / sketched caustics (`rgba(59, 130, 246, 0.08)` or soft green paper tints) and paper cutout bubble rings (`#FFFDF8` with `#3E2723` fine ink outlines).
       - Coral Pillars: Render top and bottom obstacles as corrugated cardboard pillars (`#C5A880`, `#C85A32`, `#4A6D56`) with paper drop shadows (`3px 3px 0px rgba(62, 39, 35, 0.25)`), stitched/inked outlines (`#3E2723`), decorative paper tape rim caps (`#FAF6EE` / `#E8DEC8`), and paper dot/crease accents.
       - Pearls: Render bonus pearls as glowing construction yellow paper discs (`#F59E0B` / `#FFFDF8`) with paper sparkle aura and ink outlines.
       - Fish Entity: Render fish body as vibrant construction paper cutout (`#C85A32` or `#F59E0B`), paper fins with ink border ridges, hand-drawn papercraft eye (`#FFFDF8` white disc, `#3E2723` pupil), and wobble fin oscillation.
       - HUD & Overlays: Replace monospace text with craft fonts (`"Patrick Hand"`, `"Comfortaa"`), sticky-note score tags, cardboard medal badges, and taped paper start/pause/gameover modal placards.
  </action>
  <verify>
    <automated>npx vitest run games/flappy-fish</automated>
  </verify>
  <done>Flappy Fish renders with papercraft ocean theme, cardboard coral pillars, papercut fish, sticky note HUD and gameover overlays; all flappy-fish unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Sky Hopper Papercraft Visuals</name>
  <files>games/sky-hopper/src/SkyHopperScene.ts</files>
  <action>
    Transform Sky Hopper from neon void climber into a handmade vertical papercraft ascent.
    1. In `games/sky-hopper/src/SkyHopperScene.ts`:
       - Altitude Gradient & Stars: Render altitude-based parchment gradient (0-1000m: warm craft sunrise `#F4EAD4` to `#FDE68A`, 1000-3000m: twilight cardboard `#D8C3A5` to `#C5A880`, 3000m+: deep inked craft wash `#473C35` to `#2B2118`), papercut star stickers and stamped dot constellations.
       - Platforms: Render standard platforms as construction green paper strips (`#10B981`) with cardboard backing (`#C5A880`) and inked borders (`#3E2723`); fragile platforms as craft paper with dashed perforated tear lines; moving platforms with paper tape chevrons (`#3B82F6`); spring platforms with paper coil cutouts (`#8B5CF6`, `#F59E0B`); rocket powerups as papercut rocket badges.
       - Obstacles: Render drones as origami paper flyer cards with ink propellers, spires as cardboard spiked gears, balloons as construction paper balloons with crinkled string ribbons.
       - Player: Render hopper hero as a rounded papercut character (`#10B981` / `#3B82F6`) with ink outlines, cardboard antenna, and paper particle dust/sparks on jump and rocket boost.
       - Story Mothership: Render mothership docking bay at 5000m as a giant corrugated cardboard airship with paper landing lights and taped banner.
       - HUD & Overlays: Render altitude meter, score, mode badges, and start/pause/gameover/victory modals on pinned and taped craft placards using `"Patrick Hand"` and `"Comfortaa"`.
  </action>
  <verify>
    <automated>npx vitest run games/sky-hopper</automated>
  </verify>
  <done>Sky Hopper renders with altitude parchment gradient, cardboard and construction paper platforms, papercut hopper hero, origami obstacles, and sticky note HUD; all sky-hopper unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Mid-Game Catalog Metadata Update & Full Project Verification</name>
  <files>src/data/games.ts</files>
  <action>
    Update metadata for all 5 mid-complexity games in `src/data/games.ts` to align with the papercraft theme:
    1. `brick-blitz`: Title "Brick Blitz", update description to mention wooden craft stick paddle, paper balls, and layered cardboard brick grids, update themeColor to `#F59E0B` or `#E11D48`, and bannerBg to warm papercraft gradient (`linear-gradient(135deg, #F4EAD4 0%, #D8C3A5 100%)`).
    2. `crate-catch`: Title "Crate Catch", update description to mention cardboard catcher cart, conveyor tracks, and construction paper crates with tape crosses, update themeColor to `#C85A32`, and bannerBg to warm kraft gradient (`linear-gradient(135deg, #F4EAD4 0%, #E8DEC8 100%)`).
    3. `type-strike`: Title "Type Strike", update description to mention papercut defense command terminal, origami enemy cards, and cardboard turret launcher, update themeColor to `#E11D48`, and bannerBg to craft paper gradient.
    4. `flappy-fish`: Title "Flappy Fish", update description to mention papercut fish swimming through cardboard coral pillars and collecting glowing paper pearls, update themeColor to `#3B82F6`, and bannerBg to parchment water gradient (`linear-gradient(135deg, #F4EAD4 0%, #D8C3A5 100%)`).
    5. `sky-hopper`: Title "Sky Hopper", update description to mention papercut altitude hopper climbing cardboard platforms to reach the 5,000m cardboard mothership, update themeColor to `#10B981`, and bannerBg to craft altitude gradient (`linear-gradient(135deg, #F4EAD4 0%, #E8DEC8 100%)`).
    6. Execute full Vitest test suite and Vite build to confirm 100% clean passes without regressions.
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <done>src/data/games.ts updated with papercraft metadata for all 5 mid-complexity games; full test suite passes and production build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Canvas Coordinate Transforms | Dynamic camera projection and canvas rendering loops processing altitude and obstacle arrays |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03 | Tampering | Camera & Coordinate Scaling | mitigate | Keep viewport transforms bounded within canvas boundaries; sanitize camera offsets against NaN and Infinity |
| T-29-04 | Information Disclosure | Metadata Schema | accept | Public static game catalog data only |
</threat_model>

<verification>
Automated unit tests for flappy-fish, sky-hopper, and full suite pass:
`npx vitest run games/flappy-fish games/sky-hopper && npm test && npm run build`
</verification>

<success_criteria>
All 5 mid-complexity games (brick-blitz, crate-catch, type-strike, flappy-fish, sky-hopper) fully converted to 2D papercraft visual identity with updated catalog metadata in src/data/games.ts, 100% passing test suite, and successful production build.
</success_criteria>

<output>
After completion, create `.planning/phases/29-mid-game-renderers/29-02-SUMMARY.md`
</output>
