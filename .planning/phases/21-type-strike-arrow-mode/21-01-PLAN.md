---
phase: 21-type-strike-arrow-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - games/type-strike/src/Dictionary.ts
  - games/type-strike/src/TypingEngine.ts
  - games/type-strike/src/Enemy.ts
  - games/type-strike/test/dictionary.test.ts
  - games/type-strike/test/typing.test.ts
  - games/type-strike/test/enemy.test.ts
autonomous: true
requirements:
  - TS-ARR-01
must_haves:
  truths:
    - "Dictionary generates procedural arrow direction sequences ('U', 'D', 'L', 'R') categorized into short (3-4), medium (5-7), and long (8-10) tiers."
    - "Arrow symbol converter accurately maps direction characters 'U'->'↑', 'D'->'↓', 'L'->'←', 'R'->'→'."
    - "TypingEngine handles directional inputs (ArrowUp/w/W -> U, ArrowDown/s/S -> D, ArrowLeft/a/A -> L, ArrowRight/d/D -> R) in 'arrows' mode."
    - "TypingEngine preserves streak, multiplier scaling, typo penalties, and target locking consistently across both 'words' and 'arrows' mode."
    - "All unit tests pass with 100% coverage for Dictionary, Enemy, and TypingEngine in both modes."
  artifacts:
    - path: games/type-strike/src/Dictionary.ts
      provides: "GameMode type, arrow sequence generator, and symbol mapping utilities"
      contains: "export type GameMode"
    - path: games/type-strike/src/TypingEngine.ts
      provides: "Directional key mapping and target matching in arrow mode"
      contains: "normalizeKey"
    - path: games/type-strike/src/Enemy.ts
      provides: "Enemy support for word and arrow sequence display formatting"
  key_links:
    - from: games/type-strike/src/TypingEngine.ts
      to: games/type-strike/src/Enemy.ts
      via: "target matching and letter advancement"
      pattern: "activeTarget\\.advanceLetter"
---

<objective>
Implement Arrow Mode domain logic in Dictionary, TypingEngine, and Enemy. Support 'words' and 'arrows' modes with arrow sequence generation ('U', 'D', 'L', 'R'), arrow symbol mapping ('↑', '↓', '←', '→'), and directional input handling (Arrow keys and WASD).

Purpose: Enable arrow sequence typing mechanics while maintaining complete backwards compatibility with word typing and 100% test coverage.
Output: Updated Dictionary.ts, TypingEngine.ts, Enemy.ts, and comprehensive unit tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@games/type-strike/src/Dictionary.ts
@games/type-strike/src/TypingEngine.ts
@games/type-strike/src/Enemy.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Dictionary and Enemy with Arrow Mode sequence generation and symbol utilities</name>
  <files>games/type-strike/src/Dictionary.ts, games/type-strike/src/Enemy.ts, games/type-strike/test/dictionary.test.ts, games/type-strike/test/enemy.test.ts</files>
  <behavior>
    - Test 1: Dictionary generates arrow sequences of length 3-4 (short), 5-7 (medium), 8-10 (long) when mode is 'arrows'
    - Test 2: Arrow utility correctly converts 'U'->'↑', 'D'->'↓', 'L'->'←', 'R'->'→' and formats arrow sequences
    - Test 3: Enemy formats display label for both words and arrow sequences
  </behavior>
  <action>
    1. In `games/type-strike/src/Dictionary.ts`:
       - Export `export type GameMode = 'words' | 'arrows';`
       - Export `export type ArrowDir = 'U' | 'D' | 'L' | 'R';`
       - Export helper function `export function arrowCharToSymbol(char: string): string`: 'U'->'↑', 'D'->'↓', 'L'->'←', 'R'->'→', default returning the character.
       - Export helper function `export function formatArrowSequence(seq: string): string`: joins symbols with spaces (e.g. "↑ ↓ ← →").
       - Add arrow sequence generator `getRandomArrowSequence(activeSequences: Set<string> | string[], tier: WordTier): WordEntry` or update `getRandomWord(activeWords, tier, mode: GameMode = 'words')` to generate random arrow sequences when mode is 'arrows' (short: 3-4 dirs, medium: 5-7 dirs, long: 8-10 dirs).
    2. In `games/type-strike/src/Enemy.ts`:
       - Add `mode?: GameMode` to `EnemyConfig` and `Enemy` class (default `'words'`).
       - Add helper `getFormattedWord(): string` and `getFormattedNextChar(): string` using `arrowCharToSymbol`.
    3. Update `games/type-strike/test/dictionary.test.ts` and `games/type-strike/test/enemy.test.ts` with full unit test coverage.
  </action>
  <verify>
    <automated>npx vitest run games/type-strike/test/dictionary.test.ts games/type-strike/test/enemy.test.ts</automated>
  </verify>
  <done>Dictionary and Enemy support arrow mode generation and formatting with all tests passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update TypingEngine with Directional Input Mapping and Arrow Mode Target Matching</name>
  <files>games/type-strike/src/TypingEngine.ts, games/type-strike/test/typing.test.ts</files>
  <behavior>
    - Test 1: In 'words' mode, normalizes 'a'-'z' to uppercase 'A'-'Z' and ignores arrows
    - Test 2: In 'arrows' mode, maps 'ArrowUp'/'w'/'W'->'U', 'ArrowDown'/'s'/'S'->'D', 'ArrowLeft'/'a'/'A'->'L', 'ArrowRight'/'d'/'D'->'R'
    - Test 3: Matches and locks onto closest enemy with matching arrow sequence start
    - Test 4: Advances arrow progress and triggers completion on final arrow step
    - Test 5: Typo penalty resets active arrow target and drops streak/multiplier to 1x
    - Test 6: Switching mode resets engine state cleanly
  </behavior>
  <action>
    1. In `games/type-strike/src/TypingEngine.ts`:
       - Add `mode: GameMode = 'words'` property and `setMode(mode: GameMode): void` method that calls `this.reset()`.
       - Update `handleKey(key: string, enemies: Enemy[], mode?: GameMode)`:
         - Support mode passed in parameter or via `this.mode`.
         - Helper method `normalizeKey(key: string, mode: GameMode): string | null`:
           - If `mode === 'words'`: if key is single alpha char 'a'-'z' or 'A'-'Z', return `key.toUpperCase()`; else `null`.
           - If `mode === 'arrows'`:
             - `'ArrowUp'`, `'w'`, `'W'`, `'KeyW'` -> `'U'`
             - `'ArrowDown'`, `'s'`, `'S'`, `'KeyS'` -> `'D'`
             - `'ArrowLeft'`, `'a'`, `'A'`, `'KeyA'` -> `'L'`
             - `'ArrowRight'`, `'d'`, `'D'`, `'KeyD'` -> `'R'`
             - Else -> `null`.
         - If `normalizeKey` returns `null`, return `{ status: 'ignored', targetId: null, multiplier: this.multiplier, streak: this.streak }`.
         - Run target lock / matching / progress / completion / typo logic using the normalized token.
    2. In `games/type-strike/test/typing.test.ts`:
       - Add comprehensive test cases for arrow input mappings (Arrow keys, WASD, lowercase and uppercase).
       - Add test cases for arrow sequence completion, multipliers, typos, target loss, and mode switching.
  </action>
  <verify>
    <automated>npx vitest run games/type-strike/test/typing.test.ts</automated>
  </verify>
  <done>TypingEngine handles both words and arrows mode with 100% unit test coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard/Event Input -> TypingEngine | Untrusted raw string input from DOM keyboard events crosses into game logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering / Denial | TypingEngine.handleKey | mitigate | Strict whitelist normalization in normalizeKey() discarding invalid strings and long non-printable keys |
| T-21-02 | Information Disclosure | Dictionary.getRandomWord | mitigate | Ensure procedural arrow generation generates valid deterministic direction subsets without crashing or infinite loops |
</threat_model>

<verification>
`npx vitest run games/type-strike/test/` passes all tests cleanly.
</verification>

<success_criteria>
Dictionary, Enemy, and TypingEngine support arrow mode with complete directional input normalization and 100% unit test pass rate.
</success_criteria>

<output>
After completion, create `.planning/phases/21-type-strike-arrow-mode/21-01-SUMMARY.md`
</output>
