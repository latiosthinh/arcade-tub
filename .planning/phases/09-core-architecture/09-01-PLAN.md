---
phase: 09-core-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/types.ts
  - src/core/Component.ts
  - src/core/Store.ts
  - test/core/component.test.ts
  - test/core/store.test.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02

must_haves:
  truths:
    - "BaseComponent lifecycle methods (mount, update, destroy) attach DOM nodes and cleanly unbind event listeners on destroy"
    - "Store initializes with immutable state snapshot and broadcasts partial updates to registered subscribers"
    - "Unsubscribing from Store stops subsequent notification callbacks without affecting other listeners"
    - "Unit test suites for Component and Store pass with 100% test coverage"
  artifacts:
    - path: "src/core/types.ts"
      provides: "Global AppState, RouteInfo, and component type definitions"
      min_lines: 20
    - path: "src/core/Component.ts"
      provides: "BaseComponent abstract class with lifecycle and auto-unbind listeners"
      min_lines: 30
    - path: "src/core/Store.ts"
      provides: "Typed pub/sub reactive Store class"
      min_lines: 25
    - path: "test/core/component.test.ts"
      provides: "Unit tests for BaseComponent mount, update, destroy, and unbinds"
      min_lines: 40
    - path: "test/core/store.test.ts"
      provides: "Unit tests for Store state retrieval, partial updates, and subscriptions"
      min_lines: 40
  key_links:
    - from: "src/core/Component.ts"
      to: "src/core/types.ts"
      via: "imports Component interface contract"
      pattern: "import.*Component.*from.*types"
    - from: "src/core/Store.ts"
      to: "src/core/types.ts"
      via: "imports AppState or generic type constraint"
      pattern: "import.*AppState.*from.*types"
---

<objective>
Implement zero-dependency `BaseComponent` lifecycle abstraction (`mount`, `update`, `destroy`, `addListener`, `element`) and typed reactive pub/sub `Store` managing app state (`route`, `searchQuery`, `activeFilter`, `isMuted`, `isTheaterMode`, `highScores`), with automated unit test suites.

Purpose: Eliminates full-page innerHTML rebuilds, avoids lost input focus or zombie iframe leaks, and centralizes cross-component state synchronization in vanilla TypeScript.
Output: `src/core/types.ts`, `src/core/Component.ts`, `src/core/Store.ts`, `test/core/component.test.ts`, and `test/core/store.test.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Core Types and BaseComponent Lifecycle with Unit Tests</name>
  <files>src/core/types.ts, src/core/Component.ts, test/core/component.test.ts</files>
  <behavior>
    - Test 1: BaseComponent creates element with specified tag and class name on instantiation
    - Test 2: mount(parent) appends component.element to target container and returns element
    - Test 3: update(props) triggers component-specific DOM updates without replacing parent container
    - Test 4: addListener attaches event listener to target and unbinds cleanly when destroy() is called
    - Test 5: destroy() removes element from DOM and clears all active listener bindings
  </behavior>
  <action>
    1. Create `src/core/types.ts` defining:
       - `RouteInfo`: `{ path: string; params: Record<string, string> }`
       - `AppState`: `{ route: RouteInfo; activeFilter: string; searchQuery: string; isMuted: boolean; isTheaterMode: boolean; highScores: Record<string, number> }`
       - `Component<P, S>` interface with `element: HTMLElement`, `mount(parent: HTMLElement): HTMLElement`, `update(props: P, state?: S): void`, and `destroy(): void`.
    2. Create `src/core/Component.ts` implementing `abstract class BaseComponent<P = void, S = void> implements Component<P, S>`:
       - `public element: HTMLElement`
       - `protected unbinds: Array<() => void> = []`
       - `constructor(tagName: keyof HTMLElementTagNameMap = 'div', className = '')` initializing element.
       - `mount(parent: HTMLElement): HTMLElement` appending `this.element` to parent.
       - `abstract update(props: P, state?: S): void`
       - `destroy(): void` iterating `this.unbinds`, resetting `this.unbinds = []`, and calling `this.element.remove()`.
       - `protected addListener<K extends keyof HTMLElementEventMap>(target: EventTarget, type: K, listener: (ev: HTMLElementEventMap[K]) => void): void` registering handler and recording cleanup callback.
    3. Create `test/core/component.test.ts` covering lifecycle methods and listener cleanup verification.
  </action>
  <verify>
    <automated>pnpm test test/core/component.test.ts</automated>
  </verify>
  <done>BaseComponent lifecycle and listener cleanup implemented and verified with 100% passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Typed Pub/Sub Store with Unit Tests</name>
  <files>src/core/Store.ts, test/core/store.test.ts</files>
  <behavior>
    - Test 1: Store initializes with immutable state snapshot via Object.freeze
    - Test 2: getState() returns current frozen state object
    - Test 3: setState(patch) merges partial state immutably and notifies all subscribers with (nextState, prevState)
    - Test 4: subscribe(listener) registers callback and returns unbind function that cleanly removes listener
    - Test 5: Multiple subscribers receive notifications in order without race conditions or memory leaks
  </behavior>
  <action>
    1. Create `src/core/Store.ts`:
       - Define `type Listener<T> = (state: T, prevState: T) => void`
       - Implement generic `class Store<T extends object>`:
         - `private state: T`
         - `private listeners: Set<Listener<T>> = new Set()`
         - `constructor(initialState: T)` freezing initial state with `Object.freeze({ ...initialState })`.
         - `getState(): Readonly<T>` returning frozen state.
         - `setState(patch: Partial<T>): void` freezing updated state `{ ...this.state, ...patch }` and broadcasting to listeners.
         - `subscribe(listener: Listener<T>): () => void` returning unbind function deleting listener from set.
    2. Create `test/core/store.test.ts` asserting initialization, getState immutability, setState notification flow, and subscribe/unsubscribe lifecycle.
  </action>
  <verify>
    <automated>pnpm test test/core/store.test.ts</automated>
  </verify>
  <done>Store pub/sub state manager implemented and verified with 100% passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component DOM Event Binding | Untrusted / repeated event listener registration could cause memory leaks or zombie callbacks |
| Store State Mutation | Unauthorized direct property mutation could cause state drift and desynchronized UI components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Denial of Service | `src/core/Component.ts` | mitigate | Explicit unbinds tracking array ensures all event listeners are removed upon `destroy()`. |
| T-09-02 | Tampering | `src/core/Store.ts` | mitigate | Enforce immutable state snapshots via `Object.freeze()` on both constructor and `setState()`. |
</threat_model>

<verification>
Run test suite to verify component lifecycle and store behavior:
```bash
pnpm test test/core/component.test.ts test/core/store.test.ts
pnpm typecheck
```
</verification>

<success_criteria>
- `src/core/types.ts`, `src/core/Component.ts`, and `src/core/Store.ts` exist with zero external runtime dependencies.
- `BaseComponent` provides clean DOM attachment and listener cleanup.
- `Store` provides immutable pub/sub state distribution.
- All unit tests pass with zero errors.
</success_criteria>

<output>
After completion, create `.planning/phases/09-core-architecture/09-01-SUMMARY.md`
</output>
