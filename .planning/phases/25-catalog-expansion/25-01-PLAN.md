---
phase: 25-catalog-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/games.ts
  - src/data/screenshots.ts
  - src/components/FilterChips.ts
  - src/components/GameGrid.ts
  - test/components/header.test.ts
  - test/views/catalog.test.ts
autonomous: true
requirements:
  - HUB-CAT-01
must_haves:
  truths:
    - "Hub catalog registers all 15 games with complete metadata, icons, genres, and theme colors"
    - "All 15 games have authentic, high-fidelity SVG screenshots matching in-game canvas visuals"
    - "Filter chips and GameGrid correctly categorize all 15 games across all, action, arcade, puzzle, and casual filters"
  artifacts:
    - path: "src/data/games.ts"
      provides: "GAMES array containing all 15 games"
      contains: "snake-eat"
    - path: "src/data/screenshots.ts"
      provides: "GAME_SCREENSHOTS object with 15 SVG screenshot entries"
      contains: "car-race"
    - path: "src/components/GameGrid.ts"
      provides: "GameGrid filtering logic for all 15 games"
      contains: "bug-climb"
  key_links:
    - from: "src/components/GameGrid.ts"
      to: "src/data/games.ts"
      via: "GAMES import"
      pattern: "import.*GAMES.*from.*data/games"
    - from: "src/components/GameCard.ts"
      to: "src/data/screenshots.ts"
      via: "GAME_SCREENSHOTS lookup"
      pattern: "GAME_SCREENSHOTS\\[game\\.id\\]"
---

<objective>
Register 3 new games (`snake-eat`, `bug-climb`, `car-race`) in `src/data/games.ts` (`GAMES` catalog) with complete metadata, genre definitions, ratings, play counts, features, and custom color themes (total: 15 games). Provide high-fidelity, lightweight SVG screenshots in `src/data/screenshots.ts` for the 3 new games. Update `GameGrid.ts` and `FilterChips.ts` to cleanly filter all 15 games across Action, Arcade, Puzzle, and Casual categories, updating header and catalog test suites.

Purpose: Expand the Arcade Carnival hub UI to showcase all 15 games with authentic visual previews and seamless genre navigation.
Output: Updated catalog data, SVG screenshot bank, filter components, and updated unit test suites.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/data/games.ts
@src/data/screenshots.ts
@src/components/GameGrid.ts
@src/components/FilterChips.ts
@test/components/header.test.ts
@test/views/catalog.test.ts

<interfaces>
From src/data/games.ts:
```typescript
export interface GameItem {
  id: string;
  title: string;
  genre: string;
  description: string;
  badge?: string;
  rating: string;
  plays: string;
  icon: string;
  themeColor: string;
  bannerBg: string;
  features: string[];
}
export const GAMES: GameItem[];
export function getPersonalHighScore(slug: string): number;
```

From src/data/screenshots.ts:
```typescript
export const GAME_SCREENSHOTS: Record<string, string>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register 3 new games in GAMES catalog metadata and update header tests</name>
  <files>src/data/games.ts, test/components/header.test.ts</files>
  <behavior>
    - GAMES array contains exactly 15 items
    - Includes snake-eat, bug-climb, car-race
    - Each game entry has non-empty title, genre, description, rating, plays, icon, themeColor, bannerBg, and at least 3 features
    - test/components/header.test.ts passes asserting 15 games
  </behavior>
  <action>
    1. Add the 3 new game definitions to `GAMES` in `src/data/games.ts`:
       - `snake-eat`: title "Cyber Snake", genre "Grid Matrix / Snake", description "Cyber neon snake grid challenge. Devour energy food pellets, extend glowing tail, and avoid boundary walls or self collision.", badge "New", rating "4.9 ★", plays "180K plays", icon "🐍", themeColor "#00ff88", bannerBg "linear-gradient(135deg, #021a0e 0%, #063d20 100%)", features: ['Matrix Grid Kinematics', 'Speed Scaling & Growth', 'Neon Eating Particle Bursts']
       - `bug-climb`: title "Bug Climb Tree", genre "Reflex Climber", description "Fast-paced tree trunk climb. Tap left/right to dodge falling branch obstacles before the urgent countdown timer runs out.", badge "New", rating "4.8 ★", plays "140K plays", icon "🪲", themeColor "#e17055", bannerBg "linear-gradient(135deg, #2d1309 0%, #4a210d 100%)", features: ['Two-Sided Trunk Switching', 'Branch Hazard Collision', 'Urgent Rush Timer & Particles']
       - `car-race`: title "Neon Highway", genre "Highway Racing", description "High-speed synthwave traffic dodger. Navigate multi-lane highway traffic, draft close behind cars for speed boosts, and survive the rush.", badge "New", rating "4.9 ★", plays "260K plays", icon "🏎️", themeColor "#e056fd", bannerBg "linear-gradient(135deg, #180524 0%, #3b0d59 100%)", features: ['Multi-Lane Traffic Navigation', 'Slipstream Draft Bonus', 'Dynamic Speed Control']
    2. Update `test/components/header.test.ts` to expect 15 games (`expect(GAMES).toHaveLength(15)`).
  </action>
  <verify>
    <automated>npx vitest run test/components/header.test.ts test/components/cards.test.ts</automated>
  </verify>
  <done>GAMES array exports all 15 games with full metadata and passes header/cards unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add authentic SVG screenshots for 3 new games</name>
  <files>src/data/screenshots.ts</files>
  <action>
    Add scalable, lightweight (<3-4KB each), neon-styled SVG gameplay mockups to `GAME_SCREENSHOTS` in `src/data/screenshots.ts` matching actual canvas visuals:
    - `snake-eat`: Cyber neon green segmented snake body on dark grid, glowing yellow/orange energy food pellet, score and speed multiplier HUD, particle sparkles.
    - `bug-climb`: Vertical tree trunk with procedural branch hazards on left/right sides, animated glowing climber bug, top urgent timer bar, height score HUD.
    - `car-race`: Multi-lane synthwave vertical highway, player sports car with headlights and draft aura, oncoming colored traffic vehicles, speedometer and distance HUD.
  </action>
  <verify>
    <automated>npx vitest run test/components/cards.test.ts</automated>
  </verify>
  <done>All 15 games have corresponding SVG strings in GAME_SCREENSHOTS rendered by GameCard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update GameGrid and FilterChips category mappings for 15 games</name>
  <files>src/components/FilterChips.ts, src/components/GameGrid.ts, test/views/catalog.test.ts</files>
  <behavior>
    - GameGrid maps all 15 games across categories:
      - 'action' (5 games): brick-blitz, type-strike, virus-defense, pop-balloon, snake-eat
      - 'arcade' (4 games): safe-cracker, crate-catch, space-racer, car-race
      - 'puzzle' (3 games): memory-cards, memory-boxes, game-2048
      - 'casual' (3 games): sky-hopper, flappy-fish, bug-climb
    - test/views/catalog.test.ts passes with updated assertions for 15 games
  </behavior>
  <action>
    1. Update `matchesFilter` logic in `src/components/GameGrid.ts` to include the 3 new games:
       - 'action': add `'snake-eat'`
       - 'arcade': add `'car-race'`
       - 'casual': add `'bug-climb'`
    2. Update `test/views/catalog.test.ts` to verify filtering behavior across all 15 games:
       - Single match search hides 14 cards
       - Action filter shows 5 cards: `brick-blitz`, `type-strike`, `pop-balloon`, `virus-defense`, `snake-eat`
       - Puzzle filter shows 3 cards: `memory-cards`, `memory-boxes`, `game-2048`
  </action>
  <verify>
    <automated>npx vitest run test/views/catalog.test.ts test/components/chips.test.ts</automated>
  </verify>
  <done>GameGrid filters all 15 games cleanly and catalog unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG screenshot rendering | Untrusted HTML/SVG injection into DOM innerHTML |
| LocalStorage High Score | Client storage tampering or invalid JSON/NaN values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | `src/data/games.ts` | mitigate | `getPersonalHighScore` safely validates `parseInt` and ignores NaN/negative values. |
| T-25-02 | Information Disclosure / XSS | `src/data/screenshots.ts` | mitigate | SVGs are hardcoded internal static constants without user input or dynamic script tags. |
</threat_model>

<verification>
Run vitest on catalog, header, and component suites:
`npx vitest run test/components/header.test.ts test/components/chips.test.ts test/components/cards.test.ts test/views/catalog.test.ts`
</verification>

<success_criteria>
- 15 games defined in `GAMES` with rich metadata.
- 15 authentic SVG screenshots in `GAME_SCREENSHOTS`.
- Filter chips and GameGrid correctly categorize all 15 games.
- All catalog and filter unit tests pass with 100% success rate.
</success_criteria>

<output>
After completion, create `.planning/phases/25-catalog-expansion/25-01-SUMMARY.md`
</output>
