---
phase: 52-game-flow-state-machine-and-tally-hud
plan: 02
type: execute
wave: 2
depends_on:
  - 52-01
files_modified:
  - games/tank-1990/test/ScoreManager.test.ts
  - games/tank-1990/test/GameFlow.test.ts
autonomous: true
requirements:
  - LOOP-01
  - LOOP-02
  - LOOP-03
  - LOOP-04
  - LOOP-05
  - LOOP-06
must_haves:
  truths:
    - "ScoreManager unit tests verify score tracking, per-enemy kill logging, stage tally breakdowns, and safe localStorage high score persistence."
    - "GameFlow unit tests verify all state machine transitions (TITLE, STAGE_INTRO, PLAYING, PAUSED, STAGE_TALLY, GAME_OVER, VICTORY) and transition guards."
    - "Stage intro curtain timer decrements accurately and auto-transitions to PLAYING state upon timeout."
    - "Stage progression advances from 1 through 35 and triggers VICTORY when clearing stage 35."
    - "HUD state snapshots provide accurate live data (lives, score, high score, reserve tanks, stage number) matching current game status."
    - "100% of Vitest unit tests pass across all Tank 1990 test suites."
  artifacts:
    - path: "games/tank-1990/test/ScoreManager.test.ts"
      provides: "Unit tests covering score accumulation, kill tally counting, stage roll-up breakdown, and localStorage persistence"
      min_lines: 90
    - path: "games/tank-1990/test/GameFlow.test.ts"
      provides: "Unit tests covering GameState transitions, curtain timer, pause/resume, stage progression (1-35), game over, victory, and HUDState snapshots"
      min_lines: 120
  key_links:
    - from: "games/tank-1990/test/ScoreManager.test.ts"
      to: "games/tank-1990/src/ScoreManager.ts"
      via: "Unit testing of score accumulation and high score persistence"
      pattern: "scoreManager\\.recordKill|scoreManager\\.calculateStageTally"
    - from: "games/tank-1990/test/GameFlow.test.ts"
      to: "games/tank-1990/src/GameFlow.ts"
      via: "Unit testing of state machine transitions and curtain timers"
      pattern: "gameFlow\\.startStage|gameFlow\\.getHUDState"
---

<objective>
Construct comprehensive Vitest unit test suites for ScoreManager and GameFlow to verify state transitions, stage intro curtain timer, score accumulation, kill tally calculations, high score persistence, and HUD state generation.

Purpose: Guarantee 100% test coverage and prevent regressions in game flow, state machine, and scoring logic.
Output: `games/tank-1990/test/ScoreManager.test.ts`, `games/tank-1990/test/GameFlow.test.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-game-flow-state-machine-and-tally-hud/52-CONTEXT.md
@.planning/phases/52-game-flow-state-machine-and-tally-hud/52-01-PLAN.md
@games/tank-1990/src/types.ts
@games/tank-1990/src/ScoreManager.ts
@games/tank-1990/src/GameFlow.ts

<interfaces>
From games/tank-1990/src/types.ts:
```typescript
export enum GameState {
  TITLE = 'TITLE',
  STAGE_INTRO = 'STAGE_INTRO',
  PLAYING = 'PLAYING',
  PAUSED = 'PAUSED',
  STAGE_TALLY = 'STAGE_TALLY',
  GAME_OVER = 'GAME_OVER',
  VICTORY = 'VICTORY',
}
export interface StageTallyResult {
  stage: number;
  rows: EnemyTallyRow[];
  totalKills: number;
  totalStagePoints: number;
  cumulativeScore: number;
  isNewHighScore: boolean;
}
export interface HUDState {
  stage: number;
  lives: number;
  score: number;
  highScore: number;
  enemyReserveCount: number;
  playerTier: TankTier;
  isGameOver: boolean;
  isPaused: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Comprehensive Vitest Unit Tests for ScoreManager</name>
  <files>games/tank-1990/test/ScoreManager.test.ts</files>
  <behavior>
    - Score tracking tests:
      - recordKill(BASIC) awards 100 points and increments basic kill count.
      - recordKill(FAST) awards 200 points.
      - recordKill(POWER) awards 300 points.
      - recordKill(ARMOR) awards 400 points.
      - addScore(500) adds 500 bonus points.
      - calculateStageTally() accurately calculates row sums, total kills, and total points.
    - LocalStorage persistence tests:
      - Reads existing high score on initialization.
      - Updates high score when current score exceeds it.
      - Saves new high score to localStorage.
      - Gracefully handles localStorage errors / missing browser environment without crashing.
  </behavior>
  <action>
    Create `games/tank-1990/test/ScoreManager.test.ts` testing:
    1. Score accumulation and per-enemy kill logging for all 4 archetypes.
    2. Stage tally calculation and structured breakdown generation.
    3. Stage score reset vs full game score reset.
    4. LocalStorage high score loading, updating, persistence, and safe error fallbacks (with mocked localStorage).
  </action>
  <verify>
    <automated>npm test -- games/tank-1990/test/ScoreManager.test.ts</automated>
  </verify>
  <done>All ScoreManager unit tests pass with 100% success rate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Comprehensive Vitest Unit Tests for GameFlow</name>
  <files>games/tank-1990/test/GameFlow.test.ts</files>
  <behavior>
    - State machine tests:
      - Initial state is TITLE.
      - startStage(1) transitions to STAGE_INTRO, sets curtainTimer to 2.0s.
      - Updating 2.0s in STAGE_INTRO automatically transitions state to PLAYING.
      - pause() and resume() toggle between PLAYING and PAUSED; invalid states reject pause.
      - triggerGameOver() transitions to GAME_OVER.
      - onStageCleared() transitions to STAGE_TALLY.
      - nextStage() advances stage count (1 to 2, 2 to 3...) and starts stage.
      - Completing stage 35 triggers VICTORY state.
      - restart() resets stage to 1 and restarts game.
    - Stage select and clamping tests:
      - selectStage clamps to valid range [1, 35].
    - HUD snapshot tests:
      - getHUDState returns matching snapshot of lives, reserve count, current score, high score, and flags.
  </behavior>
  <action>
    Create `games/tank-1990/test/GameFlow.test.ts` testing:
    1. Full state machine lifecycle: TITLE -> STAGE_INTRO -> PLAYING -> PAUSED -> STAGE_TALLY -> STAGE_INTRO -> VICTORY.
    2. Stage intro curtain timer countdown and auto-transition to PLAYING.
    3. Stage advancement from 1 through 35, and final VICTORY transition on stage 35 completion.
    4. Defeat / game over transitions.
    5. HUDState snapshot generation and data integrity.
  </action>
  <verify>
    <automated>npm test -- games/tank-1990</automated>
  </verify>
  <done>All GameFlow and Tank 1990 unit tests pass with 100% success rate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Environment -> Browser Storage Mock | Storage mock behavior in test environment must accurately simulate real browser window.localStorage and storage exceptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04 | Tampering / Mock Isolation | ScoreManager Vitest Suite | mitigate | Use `beforeEach` / `afterEach` hooks to clear and reset mocked localStorage state between tests |
</threat_model>

<verification>
Execute full Vitest test suite across all Tank 1990 test files:
`npm test -- games/tank-1990`
</verification>

<success_criteria>
All unit tests in `ScoreManager.test.ts` and `GameFlow.test.ts` pass cleanly, achieving 100% test pass rate across the entire `games/tank-1990` package.
</success_criteria>

<output>
After completion, create `.planning/phases/52-game-flow-state-machine-and-tally-hud/52-02-SUMMARY.md`
</output>
