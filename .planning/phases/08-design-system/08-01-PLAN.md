---
phase: 08-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/tokens.css
  - src/styles/theme.css
  - test/tokens.test.ts
autonomous: true
requirements:
  - DS-01
  - DS-02

must_haves:
  truths:
    - "Design tokens define retro-modern cyber-arcade palette, typography, surfaces, borders, and glow filters"
    - "Base theme stylesheet maps tokens to core HTML elements and reusable arcade UI classes"
    - "Tokens validate cleanly with automated tests ensuring all semantic CSS variables exist"
  artifacts:
    - path: "src/styles/tokens.css"
      provides: "CSS custom properties for colors, typography, glowing shadows, radii, and surfaces"
      min_lines: 40
    - path: "src/styles/theme.css"
      provides: "Global arcade resets, typography styles, utility classes, and component primitives"
      min_lines: 50
    - path: "test/tokens.test.ts"
      provides: "Unit test validating token definitions and CSS syntax validity"
  key_links:
    - from: "src/styles/theme.css"
      to: "src/styles/tokens.css"
      via: "CSS @import './tokens.css' or root consumption"
      pattern: "var\\(--bg-primary|var\\(--neon-cyan"
---

<objective>
Create retro-modern cyber-arcade CSS design tokens (`src/styles/tokens.css`) and shared base arcade theme (`src/styles/theme.css`) establishing the unified visual foundation for Arcade Carnival v2.0.

Purpose: Replace legacy YouTube clone styles with distinct cyber-arcade visual tokens (neon cyan/pink/yellow, deep indigo/cyber surfaces, glowing borders, retro-futuristic typography).
Output: `src/styles/tokens.css`, `src/styles/theme.css`, `test/tokens.test.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-design-system/08-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create cyber-arcade CSS design tokens and theme test</name>
  <files>src/styles/tokens.css, test/tokens.test.ts</files>
  <behavior>
    - Test 1: tokens.css file exists and defines required semantic tokens (--bg-primary, --bg-surface, --bg-card, --neon-cyan, --neon-pink, --neon-yellow, --neon-green, --neon-purple, --glow-cyan, --glow-pink, --font-display, --font-mono, --font-sans, --radius-sm, --radius-md, --radius-lg, --border-subtle, --border-accent).
    - Test 2: tokens parse without syntax errors and contain valid hex/rgba/css value structures.
  </behavior>
  <action>
    1. Write `test/tokens.test.ts` to assert that `src/styles/tokens.css` contains all required design tokens specified in D-01 and DS-01.
    2. Implement `src/styles/tokens.css` per D-01:
       - Deep cyber darks: `--bg-primary: #0a0a12`, `--bg-surface: #121324`, `--bg-card: #181a30`, `--bg-card-hover: #222544`, `--bg-overlay: rgba(10, 10, 18, 0.85)`
       - Neon accents: `--neon-cyan: #00f0ff`, `--neon-pink: #ff007f`, `--neon-yellow: #ffe600`, `--neon-green: #00ff88`, `--neon-purple: #9d00ff`
       - Glowing shadows: `--glow-cyan: 0 0 12px rgba(0, 240, 255, 0.45)`, `--glow-pink: 0 0 12px rgba(255, 0, 127, 0.45)`, `--glow-yellow: 0 0 12px rgba(255, 230, 0, 0.45)`
       - Typography: `--font-display: 'Impact', 'Segoe UI Black', -apple-system, sans-serif`, `--font-mono: 'Courier New', Courier, monospace`, `--font-sans: system-ui, -apple-system, sans-serif`
       - Font sizes: `--text-xs: 0.75rem`, `--text-sm: 0.875rem`, `--text-base: 1rem`, `--text-lg: 1.125rem`, `--text-xl: 1.25rem`, `--text-2xl: 1.5rem`, `--text-3xl: 2rem`
       - Borders & radii: `--border-subtle: 1px solid rgba(255, 255, 255, 0.08)`, `--border-accent: 1px solid rgba(0, 240, 255, 0.3)`, `--radius-sm: 4px`, `--radius-md: 8px`, `--radius-lg: 16px`, `--radius-full: 9999px`.
  </action>
  <verify>
    <automated>pnpm test test/tokens.test.ts</automated>
  </verify>
  <done>tokens.css provides complete token system with passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create base arcade theme stylesheet</name>
  <files>src/styles/theme.css</files>
  <action>
    1. Create `src/styles/theme.css` importing `./tokens.css` per D-01 and D-02.
    2. Establish base global styles:
       - Reset body/html background to `var(--bg-primary)` and text to `#f8fafc`.
       - Base font stack using `var(--font-sans)` with crisp subpixel antialiasing.
       - Reusable arcade utility classes: `.neon-text-cyan`, `.neon-text-pink`, `.neon-border-cyan`, `.neon-border-pink`, `.arcade-card`, `.arcade-btn`, `.badge-neon`.
       - Focus-visible styles with high-contrast keyboard navigation rings (`outline: 2px solid var(--neon-cyan); outline-offset: 2px;`).
  </action>
  <verify>
    <automated>pnpm test test/tokens.test.ts</automated>
  </verify>
  <done>theme.css delivers unified arcade base styles and utility classes using token variables.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CSS Variables -> DOM styling | CSS custom properties applied globally across document |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `tokens.css` | mitigate | Use strictly validated CSS variable names and static tokens without dynamic user-injected styling |
</threat_model>

<verification>
Automated test suite validates `tokens.css` token definitions and CSS structure.
</verification>

<success_criteria>
`src/styles/tokens.css` and `src/styles/theme.css` created with 100% test pass rate.
</success_criteria>

<output>
After completion, create `.planning/phases/08-design-system/08-01-SUMMARY.md`
</output>
