---
phase: 08-design-system
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/styles/crt.css
  - src/crt.ts
  - src/hub.css
  - embed.html
  - index.html
  - games/safe-cracker/index.html
  - games/brick-blitz/index.html
  - games/sky-hopper/index.html
  - games/crate-catch/index.html
  - games/type-strike/index.html
  - test/crt.test.ts
autonomous: true
requirements:
  - DS-02
  - DS-03

must_haves:
  truths:
    - "Shared design tokens and theme are imported into embed.html, hub.css, and all 5 game index.html templates"
    - "CRT scanline and bloom visual overlay renders across the viewport with pointer-events disabled"
    - "CRT overlay toggle persists on/off state in localStorage and updates the DOM class accordingly"
    - "Automated unit tests verify CRT toggle helper methods and storage persistence"
  artifacts:
    - path: "src/styles/crt.css"
      provides: "CSS scanline pattern, vignette, and phosphor bloom overlay styles"
      min_lines: 30
    - path: "src/crt.ts"
      provides: "CRT overlay controller, initialization, and localStorage toggle persistence"
      exports: ["initCrtOverlay", "toggleCrt", "isCrtEnabled"]
    - path: "test/crt.test.ts"
      provides: "Unit tests for CRT controller and persistence"
  key_links:
    - from: "src/crt.ts"
      to: "localStorage"
      via: "arcade_crt_mode key read/write"
      pattern: "localStorage\\.(getItem|setItem)\\(['\"]arcade_crt_mode['\"]"
    - from: "embed.html"
      to: "src/styles/tokens.css"
      via: "stylesheet link"
      pattern: "tokens\\.css"
    - from: "src/hub.css"
      to: "src/styles/tokens.css"
      via: "token variable consumption"
      pattern: "var\\(--bg-primary|var\\(--bg-surface|var\\(--neon-"
---

<objective>
Implement CRT scanline/bloom visual overlay with persistent toggle and integrate shared design tokens into `hub.css`, `embed.html`, and all 5 standalone game templates (`games/*/index.html`).

Purpose: Complete requirement DS-02 and DS-03 by connecting the design system to real pages and providing an authentic retro-arcade CRT overlay.
Output: `src/styles/crt.css`, `src/crt.ts`, `test/crt.test.ts`, updated `src/hub.css`, `embed.html`, `index.html`, and 5 game HTML files.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-design-system/08-CONTEXT.md
@.planning/phases/08-design-system/08-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CRT visual overlay stylesheet and TypeScript controller</name>
  <files>src/styles/crt.css, src/crt.ts, test/crt.test.ts</files>
  <behavior>
    - Test 1: initCrtOverlay injects overlay element or applies CRT class to body/app.
    - Test 2: toggleCrt toggles state, updates localStorage('arcade_crt_mode'), and updates DOM class.
    - Test 3: isCrtEnabled returns current boolean state based on localStorage default.
  </behavior>
  <action>
    1. Write `test/crt.test.ts` testing `initCrtOverlay`, `toggleCrt`, `isCrtEnabled`, and `localStorage` integration under `arcade_crt_mode` key per D-03 and DS-03.
    2. Create `src/styles/crt.css`:
       - Fixed fullscreen overlay container with `pointer-events: none`, `z-index: 9999`.
       - Scanline layer: subtle repeating linear-gradient scanlines (e.g. `rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.25) 50%`, background-size: 100% 4px).
       - Vignette layer: radial gradient darkening corners (`radial-gradient(circle at center, transparent 60%, rgba(0, 0, 0, 0.6) 100%)`).
       - `.crt-active` class toggling visibility on/off.
       - Include `prefers-reduced-motion` media query to disable any flicker animations.
    3. Create `src/crt.ts`:
       - Export `isCrtEnabled(): boolean` (reads `localStorage.getItem('arcade_crt_mode') !== 'off'`).
       - Export `toggleCrt(force?: boolean): boolean` (updates state, writes `localStorage`, adds/removes `.crt-active` class on root/body).
       - Export `initCrtOverlay(): void` (attaches overlay element if not present, reads stored preference, applies initial class).
  </action>
  <verify>
    <automated>pnpm test test/crt.test.ts</automated>
  </verify>
  <done>CRT overlay styles and TypeScript controller implemented with passing unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate tokens and CRT overlay into hub.css, embed.html, and standalone templates</name>
  <files>src/hub.css, embed.html, index.html, games/safe-cracker/index.html, games/brick-blitz/index.html, games/sky-hopper/index.html, games/crate-catch/index.html, games/type-strike/index.html</files>
  <action>
    1. Update `src/hub.css` to consume tokens from `src/styles/tokens.css` (replace legacy `--yt-*` variables with `--bg-primary`, `--bg-surface`, `--bg-card`, `--neon-cyan`, `--neon-pink`, `--font-sans`, etc.) per D-02.
    2. Update `index.html` to link `src/styles/tokens.css`, `src/styles/theme.css`, and `src/styles/crt.css`.
    3. Update `embed.html` to link `src/styles/tokens.css` and `src/styles/theme.css`, replacing hardcoded inline color variables with design tokens per DS-02.
    4. Update all 5 game standalone HTML files (`games/safe-cracker/index.html`, `games/brick-blitz/index.html`, `games/sky-hopper/index.html`, `games/crate-catch/index.html`, `games/type-strike/index.html`):
       - Link `../../src/styles/tokens.css` and use token variables for background (`var(--bg-primary)`), typography (`var(--font-mono)`), button styles, and glowing borders.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>Tokens and theme unified across hub, embed kit, and standalone game shells without broken styles or iframe conflicts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| LocalStorage -> CRT preference | Client-side storage read/write for user visual preferences |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Tampering | `src/crt.ts` | mitigate | Strictly sanitize and validate localStorage value against allowed enum ('on' / 'off') before evaluating |
</threat_model>

<verification>
Run complete test suite (`pnpm test`) to ensure CRT controller and tokens pass without regressions.
</verification>

<success_criteria>
`08-02-PLAN.md` covers full integration of DS-02 across all templates and DS-03 CRT overlay toggle with persistent localStorage.
</success_criteria>

<output>
After completion, create `.planning/phases/08-design-system/08-02-SUMMARY.md`
</output>
