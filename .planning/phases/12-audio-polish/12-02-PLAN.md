---
phase: 12-audio-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - scripts/audit-bundle.js
  - package.json
  - test/production/bundle-audit.test.ts
autonomous: true
requirements:
  - POL-02

must_haves:
  truths:
    - "Production build artifacts in dist/ remain strictly under the 200KB gzipped total and per-game budget."
    - "scripts/audit-bundle.js checks both per-file limits and total distribution bundle budget with clear pass/fail status."
    - "100% of all unit and integration tests across monorepo packages, games, core architecture, and UI components pass with zero regressions."
  artifacts:
    - path: "scripts/audit-bundle.js"
      provides: "Automated bundle audit script asserting gzip and raw size thresholds"
      min_lines: 60
    - path: "test/production/bundle-audit.test.ts"
      provides: "Automated test verifying dist bundle budget compliance and file integrity"
      min_lines: 40
  key_links:
    - from: "package.json"
      to: "scripts/audit-bundle.js"
      via: "pnpm run audit:bundle"
      pattern: "audit-bundle"
---

<objective>
Execute production build verification, bundle size audit (< 200KB gzipped limit), and total test suite regression verification (POL-02).
Ensure complete test coverage across entire monorepo with 100% pass rate.

Purpose: Guarantee high-performance lightweight static distribution ready for YouTube Playables without performance or size regressions.
Output: Validated `dist/` bundle sizes, enhanced `scripts/audit-bundle.js`, and passing test suite.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@scripts/audit-bundle.js
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance audit-bundle script and add automated bundle verification test</name>
  <files>scripts/audit-bundle.js, test/production/bundle-audit.test.ts</files>
  <action>
    1. Enhance `scripts/audit-bundle.js`:
       - Verify total distribution gzipped size < 200KB.
       - Verify individual HTML/JS/CSS assets < 50KB gzipped each.
       - Provide clear summary output with exit code 0 on success and exit code 1 on violation.
    2. Create `test/production/bundle-audit.test.ts`:
       - Programmatically verify built `dist/` directory files exist (index.html, embed.html, games/*/index.html).
       - Verify gzip compressed sizes of all generated assets satisfy budget constraints.
  </action>
  <verify>
    <automated>pnpm build && pnpm run audit:bundle && pnpm vitest run test/production/bundle-audit.test.ts</automated>
  </verify>
  <done>Bundle audit script and test verify distribution size < 200KB gzipped.</done>
</task>

<task type="auto">
  <name>Task 2: Monorepo end-to-end test suite and typecheck verification</name>
  <files>package.json</files>
  <action>
    Run complete monorepo verification:
    1. `pnpm typecheck` (TypeScript project references check).
    2. `pnpm test` (Full Vitest suite across all packages, views, components, audio, and games).
    3. `pnpm build` (Production static build).
    4. `pnpm run audit:bundle` (Size budget audit).
    Ensure zero failures, zero warnings, and 100% test pass rate.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test && pnpm build && pnpm run audit:bundle</automated>
  </verify>
  <done>All monorepo tests, typechecks, and bundle audits pass with 100% success rate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build Pipeline -> Dist Output | Static build output must not include source maps or debug secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Information Disclosure | `dist/` build artifacts | mitigate | Bundle audit verifies only production minified assets exist in dist/ |
</threat_model>

<verification>
`pnpm typecheck` returns 0 errors.
`pnpm test` passes 100% of all tests (>270 tests).
`pnpm build` builds dist/.
`pnpm run audit:bundle` reports PASSED.
</verification>

<success_criteria>
1. Total distribution bundle is under 200KB gzipped.
2. 100% of tests pass in Vitest.
3. Zero TypeScript typecheck errors.
4. Production audit script exits with status 0.
</success_criteria>

<output>
After completion, create `.planning/phases/12-audio-polish/12-02-SUMMARY.md`
</output>
