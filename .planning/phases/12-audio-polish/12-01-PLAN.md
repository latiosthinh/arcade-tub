---
phase: 12-audio-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/audio/ui-audio.ts
  - src/components/AppHeader.ts
  - src/components/FilterChips.ts
  - src/components/GameCard.ts
  - src/views/GameView.ts
  - src/main.ts
  - test/audio/ui-audio.test.ts
autonomous: true
requirements:
  - POL-01

must_haves:
  truths:
    - "UI audio synthesizer plays synthesized procedural sound effects for button clicks, card hover blips, game launch swooshes, view transitions, and CRT toggles."
    - "UI audio respects mute state and synchronizes with AppState isMuted stored in reactive Store and localStorage."
    - "AppHeader, FilterChips, GameCard, GameView, and Router trigger corresponding UI audio effects on user interaction."
    - "Unit tests verify all procedural audio triggers, oscillator/gain parameters, mute toggling, and fallback behavior."
  artifacts:
    - path: "src/audio/ui-audio.ts"
      provides: "Procedural Web Audio synthesizer for UI sound effects (click, hover, launch, transition, crtToggle, victory, error)"
      min_lines: 80
    - path: "test/audio/ui-audio.test.ts"
      provides: "Unit tests for UIAudioSynthesizer and UI component sound integrations"
      min_lines: 60
  key_links:
    - from: "src/components/AppHeader.ts"
      to: "src/audio/ui-audio.ts"
      via: "uiAudio.playClick() / uiAudio.playCrtToggle()"
      pattern: "uiAudio\\.play"
    - from: "src/components/GameCard.ts"
      to: "src/audio/ui-audio.ts"
      via: "uiAudio.playHover() / uiAudio.playLaunch()"
      pattern: "uiAudio\\.play"
    - from: "src/views/GameView.ts"
      to: "src/audio/ui-audio.ts"
      via: "uiAudio.playLaunch()"
      pattern: "uiAudio\\.play"
    - from: "src/main.ts"
      to: "src/audio/ui-audio.ts"
      via: "uiAudio.playTransition() / uiAudio.setMuted()"
      pattern: "uiAudio\\."
---

<objective>
Implement procedural UI sound effects module (`src/audio/ui-audio.ts`) utilizing Web Audio API / AudioSynthesizer patterns (POL-01).
Wire synthesized audio triggers across UI components (`AppHeader`, `FilterChips`, `GameCard`, `GameView`, `main.ts` router transitions).
Ensure mute state is strictly respected across all triggers and synchronized with Store `isMuted`.

Purpose: Provide responsive tactile audio feedback for arcade hub interactions without external sound asset files.
Output: `src/audio/ui-audio.ts`, wired component event listeners, and comprehensive unit tests in `test/audio/ui-audio.test.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@packages/game-engine/src/AudioSynthesizer.ts
@src/components/AppHeader.ts
@src/components/FilterChips.ts
@src/components/GameCard.ts
@src/views/GameView.ts
@src/main.ts

<interfaces>
From packages/game-engine/src/AudioSynthesizer.ts:
```typescript
export class AudioSynthesizer {
  public isMuted(): boolean;
  public setMuted(muted: boolean): void;
  public toggleMute(): boolean;
  public playClick(): void;
  public playScore(): void;
  public playBounce(): void;
  public playExplosion(): void;
  public playPowerup(): void;
  public playError(): void;
  public playVictory(): void;
}
```

From src/core/types.ts:
```typescript
export interface AppState {
  route: RouteInfo;
  activeFilter: string;
  searchQuery: string;
  isMuted: boolean;
  isTheaterMode: boolean;
  highScores: Record<string, number>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create UIAudioSynthesizer module with procedural audio presets and unit tests</name>
  <files>src/audio/ui-audio.ts, test/audio/ui-audio.test.ts</files>
  <behavior>
    - Test 1: `uiAudio.isMuted()` initializes from localStorage or defaults to false
    - Test 2: `uiAudio.setMuted(true)` prevents Web Audio oscillator creation and sets gain to 0
    - Test 3: `uiAudio.playClick()` triggers short snappy sine click (800Hz -> 400Hz)
    - Test 4: `uiAudio.playHover()` triggers gentle high-pitch blip (1200Hz -> 1800Hz, 30ms)
    - Test 5: `uiAudio.playLaunch()` triggers ascending frequency sweep swoosh (220Hz -> 880Hz, 200ms)
    - Test 6: `uiAudio.playTransition()` triggers smooth chord/filter swell (440Hz -> 660Hz)
    - Test 7: `uiAudio.playCrtToggle()` triggers cathode tube degauss / pop blip (150Hz -> 50Hz with noise burst)
    - Test 8: Safe execution in non-browser / headless environments (SSR/test without AudioContext)
  </behavior>
  <action>
    Create `src/audio/ui-audio.ts` exporting `UIAudioSynthesizer` class and `uiAudio` singleton instance.
    Implement methods:
    - `isMuted(): boolean`
    - `setMuted(muted: boolean): void`
    - `toggleMute(): boolean`
    - `playClick(): void`
    - `playHover(): void`
    - `playLaunch(): void`
    - `playTransition(): void`
    - `playCrtToggle(): void`
    - `playError(): void`
    - `playSuccess(): void`

    Use standard Web Audio API (`AudioContext`, `OscillatorNode`, `GainNode`) with gain envelopes (`exponentialRampToValueAtTime`) and automatic node disconnection on `ended`. Keep zero external dependencies. Handle audio context auto-resume on first user gesture.
  </action>
  <verify>
    <automated>pnpm vitest run test/audio/ui-audio.test.ts</automated>
  </verify>
  <done>UIAudioSynthesizer created, exported, and 100% tested with mocked Web Audio nodes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire UI audio triggers across components and router</name>
  <files>src/components/AppHeader.ts, src/components/FilterChips.ts, src/components/GameCard.ts, src/views/GameView.ts, src/main.ts, test/components/header.test.ts, test/components/chips.test.ts, test/components/cards.test.ts, test/views/player.test.ts</files>
  <action>
    Integrate `uiAudio` calls into user interaction handlers:
    1. `src/components/AppHeader.ts`:
       - Audio toggle button click -> `uiAudio.setMuted(nextMuted)` + `uiAudio.playClick()`
       - CRT toggle button click -> `uiAudio.playCrtToggle()`
    2. `src/components/FilterChips.ts`:
       - Filter chip click -> `uiAudio.playClick()`
    3. `src/components/GameCard.ts`:
       - `mouseenter` / `focus` -> `uiAudio.playHover()`
       - Card click / Enter / Space press (game selection) -> `uiAudio.playLaunch()`
    4. `src/views/GameView.ts`:
       - Theater toggle button click / `T` key -> `uiAudio.playClick()`
       - Back to games button click / `Escape` key -> `uiAudio.playTransition()`
    5. `src/main.ts`:
       - Synchronize `store.subscribe` with `uiAudio.setMuted(state.isMuted)`
       - Router `mountView` -> `uiAudio.playTransition()` on route changes
    6. Update existing tests in `test/components/` and `test/views/` to assert UI audio triggers via spies or ensure safe execution with mocks.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>All UI interactions trigger tactile procedural audio feedback; all tests pass cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Gesture -> Web Audio Context | Browser autoplay policy blocks unprompted audio |
| Iframe postMessage -> UI Audio | Only trusted internal origins can trigger audio feedback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Denial of Service (Audio thrashing) | `src/audio/ui-audio.ts` | mitigate | Disconnect oscillator/gain nodes onended and guard with try/catch |
| T-12-02 | Information Disclosure | `localStorage` | mitigate | Only store boolean mute flag, no sensitive telemetry |
</threat_model>

<verification>
`pnpm test` passes 100% across all unit tests including new audio tests.
`pnpm typecheck` passes with zero errors.
</verification>

<success_criteria>
1. `src/audio/ui-audio.ts` is implemented and exports `uiAudio` singleton.
2. Button clicks, chip filters, card hovers, game launches, view changes, and CRT toggles produce procedural audio.
3. Audio respects `isMuted` and persists mute preference across sessions.
4. All unit tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/12-audio-polish/12-01-SUMMARY.md`
</output>
