---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/hub.ts
  - src/hub.css
  - index.html
autonomous: false
requirements:
  - REQ-01
  - REQ-10

must_haves:
  truths:
    - "Hub page displays 5 game cards in a CSS grid"
    - "Each card shows game name and a placeholder thumbnail"
    - "Clicking a card navigates to /games/{name}/index.html"
    - "Hub has dark arcade theme with neon accent borders"
    - "pnpm dev serves hub and all game cards are clickable"
  artifacts:
    - path: "src/hub.ts"
      provides: "Hub menu logic — renders 5 game cards"
      contains: "safe-cracker"
    - path: "src/hub.css"
      provides: "Dark arcade theme CSS"
      contains: "grid"
    - path: "index.html"
      provides: "Hub HTML shell linking hub.ts and hub.css"
      contains: "Arcade Carnival"
  key_links:
    - from: "src/hub.ts"
      to: "games/*/index.html"
      via: "anchor href navigation"
      pattern: "games/.*index.html"
---

<objective>
Build the hub menu page — dark arcade-themed CSS grid of 5 game cards that navigate to each game's placeholder page.

Purpose: Provide the main entry point users see when launching Arcade Carnival. Satisfies REQ-01 (hub listing all games).
Output: Working hub page at root with clickable game cards.
</objective>

<execution_context>
@.planning/phases/01-foundation/01-03-PLAN.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement hub menu with arcade-themed game cards</name>
  <files>src/hub.ts, src/hub.css, index.html</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md
    - index.html
    - src/hub.ts
  </read_first>
  <action>
**Update `index.html`** to link the CSS:
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Arcade Carnival</title>
  <link rel="stylesheet" href="/src/hub.css" />
</head>
<body>
  <div id="app"></div>
  <script type="module" src="/src/hub.ts"></script>
</body>
</html>
```

**Create `src/hub.css`** — dark arcade theme:
- `body`: background `#0a0a1a`, font-family `'Segoe UI', system-ui, sans-serif`, color `#e0e0e0`, margin 0, min-height 100vh
- `.hub-title`: text-align center, font-size 3rem, color `#fff`, text-shadow `0 0 20px #ff00ff, 0 0 40px #ff00ff`, padding 2rem 0, letter-spacing 0.15em, text-transform uppercase
- `.hub-grid`: display grid, `grid-template-columns: repeat(auto-fit, minmax(280px, 1fr))`, gap 1.5rem, max-width 1200px, margin 0 auto, padding 0 2rem 2rem
- `.game-card`: background `#1a1a2e`, border-radius 12px, border `2px solid #333`, overflow hidden, cursor pointer, transition `border-color 0.3s, box-shadow 0.3s, transform 0.2s`, text-decoration none, color inherit, display block
- `.game-card:hover`: border-color with neon glow matching card color, box-shadow `0 0 15px {color}40`, transform `translateY(-4px)`
- `.card-thumbnail`: width 100%, aspect-ratio 16/9, display flex, align-items center, justify-content center, font-size 3rem (emoji or icon placeholder)
- `.card-info`: padding 1rem
- `.card-title`: font-size 1.25rem, font-weight bold, margin-bottom 0.25rem
- `.card-genre`: font-size 0.85rem, color `#888`
- `.card-play`: display inline-block, margin-top 0.75rem, padding `0.4rem 1.2rem`, background `#ff00ff`, color `#fff`, border-radius 6px, font-weight bold, text-transform uppercase, font-size 0.8rem, letter-spacing 0.05em

Each card gets a unique neon accent color for hover glow:
- safe-cracker: `#ffcc00` (gold)
- brick-blitz: `#0984e3` (blue)
- sky-hopper: `#6c5ce7` (purple)
- crate-catch: `#00b894` (green)
- type-strike: `#d63031` (red)

**Create `src/hub.ts`**:
```typescript
interface GameCard {
  slug: string;
  name: string;
  genre: string;
  color: string;
  icon: string;
}

const GAMES: GameCard[] = [
  { slug: 'safe-cracker', name: 'Safe Cracker', genre: 'Clicker / Timing', color: '#ffcc00', icon: '🔐' },
  { slug: 'brick-blitz', name: 'Brick Blitz', genre: 'Breakout', color: '#0984e3', icon: '🧱' },
  { slug: 'sky-hopper', name: 'Sky Hopper', genre: 'Vertical Platformer', color: '#6c5ce7', icon: '🚀' },
  { slug: 'crate-catch', name: 'Crate Catch', genre: 'Catcher / Stacker', color: '#00b894', icon: '📦' },
  { slug: 'type-strike', name: 'Type Strike', genre: 'Typing Defense', color: '#d63031', icon: '⌨️' },
];

document.addEventListener('DOMContentLoaded', () => {
  const app = document.getElementById('app');
  if (!app) return;

  const title = document.createElement('h1');
  title.className = 'hub-title';
  title.textContent = 'Arcade Carnival';
  app.appendChild(title);

  const grid = document.createElement('div');
  grid.className = 'hub-grid';

  for (const game of GAMES) {
    const card = document.createElement('a');
    card.className = 'game-card';
    card.href = `/games/${game.slug}/index.html`;
    card.setAttribute('aria-label', `Play ${game.name}`);
    card.style.setProperty('--accent', game.color);

    card.innerHTML = `
      <div class="card-thumbnail" style="background: ${game.color}20">
        <span>${game.icon}</span>
      </div>
      <div class="card-info">
        <div class="card-title">${game.name}</div>
        <div class="card-genre">${game.genre}</div>
        <span class="card-play">Play</span>
      </div>
    `;

    grid.appendChild(card);
  }

  app.appendChild(grid);
});
```

Add hover glow using CSS custom property `--accent`:
```css
.game-card:hover {
  border-color: var(--accent, #ff00ff);
  box-shadow: 0 0 15px color-mix(in srgb, var(--accent, #ff00ff) 25%, transparent);
}
```

ARIA labels on each card per REQ-01 accessibility expectations (full a11y addressed in Phase 7 per REQ-12, but basic labels go in now).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Arcade Carnival" src/hub.ts
    - grep -q "hub-grid" src/hub.css
    - grep -q "grid" src/hub.css
    - grep -q "safe-cracker" src/hub.ts
    - grep -q "brick-blitz" src/hub.ts
    - grep -q "sky-hopper" src/hub.ts
    - grep -q "crate-catch" src/hub.ts
    - grep -q "type-strike" src/hub.ts
    - grep -q "games/" src/hub.ts
    - grep -q "aria-label" src/hub.ts
    - grep -q "0a0a1a" src/hub.css
    - grep -q "hub.css" index.html
    - grep -q "hub.ts" index.html
  </acceptance_criteria>
  <done>Hub page renders 5 game cards in dark arcade grid. Each card navigates to its game's index.html. ARIA labels present. Neon hover effects with per-game accent colors. pnpm typecheck and pnpm build pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify hub and game placeholders</name>
  <files>src/hub.ts, src/hub.css, index.html</files>
  <action>
User visually verifies hub page and game placeholder navigation.
  </action>
  <what-built>Hub menu page with 5 game cards + placeholder canvas pages for each game</what-built>
  <how-to-verify>
    1. Run `pnpm dev` in project root
    2. Open the URL shown in terminal (likely http://localhost:5173)
    3. Verify: Dark background, "ARCADE CARNIVAL" title with neon glow, 5 game cards in a grid
    4. Hover over cards — each should glow with its accent color and lift slightly
    5. Click "Safe Cracker" card — should navigate to its page showing "Safe Cracker" + "Coming Soon" on dark canvas
    6. Go back, click each of the other 4 game cards — each should show its name + "Coming Soon" with a unique background color
    7. Run `pnpm build` — verify dist/ folder contains hub index.html + 5 game folders
  </how-to-verify>
  <verify>User confirms hub page looks correct and all 5 game links work</verify>
  <done>Hub page visually verified: dark arcade theme, 5 game cards, all navigation works, build produces dist/ with correct structure</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Static HTML/CSS/JS hub page — no external data, no user input processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | T (Tampering) | innerHTML in hub.ts | accept | Data is hardcoded GAMES array — no user input flows into innerHTML. No XSS vector. |
</threat_model>

<verification>
- Hub page shows 5 game cards
- Each card links to correct game path
- Each game placeholder shows name + "Coming Soon"
- `pnpm build` produces complete dist/
- `pnpm typecheck` passes
</verification>

<success_criteria>
Hub menu fully functional with dark arcade theme, 5 clickable game cards, navigation to placeholder game pages. Visual verification by user confirms arcade aesthetic.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
