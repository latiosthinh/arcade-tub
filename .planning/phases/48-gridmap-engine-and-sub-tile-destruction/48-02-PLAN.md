---
phase: 48-gridmap-engine-and-sub-tile-destruction
plan: 02
type: execute
wave: 2
depends_on:
  - "48-01"
files_modified:
  - games/tank-1990/src/stages.ts
  - games/tank-1990/test/GridMap.test.ts
autonomous: true
requirements:
  - GRID-01
  - GRID-02
  - GRID-03
  - GRID-04
  - GRID-05
  - GRID-06
  - GRID-07

must_haves:
  truths:
    - "System encodes all 35 authentic Battle City / Tank 1990 stage map layouts"
    - "Stage loader parses compact stage definitions and populates GridMap cleanly"
    - "Vitest suite verifies 100% of grid engine behavior: bitmask chipping, terrain queries, Eagle HQ, and 35 stage structures"
  artifacts:
    - path: "games/tank-1990/src/stages.ts"
      provides: "35 stage map encodings and stage loader function"
      exports: ["STAGE_MAPS", "loadStage", "TOTAL_STAGES"]
    - path: "games/tank-1990/test/GridMap.test.ts"
      provides: "Comprehensive unit test suite for GridMap and Stage loader"
      min_lines: 100
  key_links:
    - from: "games/tank-1990/src/stages.ts"
      to: "games/tank-1990/src/GridMap.ts"
      via: "imports GridMap, TileType"
      pattern: "import.*GridMap.*from"
    - from: "games/tank-1990/test/GridMap.test.ts"
      to: "games/tank-1990/src/GridMap.ts"
      via: "tests GridMap core methods and stage loader"
      pattern: "import.*GridMap.*from"
---

<objective>
Encode all 35 authentic Tank 1990 / Battle City stage maps, implement the stage loader, and build a comprehensive Vitest unit test suite validating the entire grid engine and destruction subsystem.

Purpose: Provide full campaign map data (Stages 1 through 35) and rigorous test coverage ensuring zero regressions in sub-tile chipping, terrain queries, Eagle HQ mechanics, and level loading.
Output: `games/tank-1990/src/stages.ts`, `games/tank-1990/test/GridMap.test.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-gridmap-engine-and-sub-tile-destruction/48-CONTEXT.md
@.planning/phases/48-gridmap-engine-and-sub-tile-destruction/48-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode 35 authentic stage maps and create stage loader</name>
  <files>games/tank-1990/src/stages.ts</files>
  <action>
Create `games/tank-1990/src/stages.ts`:
1. Define `STAGE_MAPS`: Array or record containing 35 authentic Battle City / Tank 1990 stage layouts (Stage 1 to Stage 35).
   - Use compact string array format (26 rows of 26 characters) or run-length/coordinate encoding for readability and minimal bundle size.
   - Character mapping:
     - `.` or ` ` = `EMPTY` (0)
     - `#` or `B` = `BRICK` (1)
     - `@` or `S` = `STEEL` (2)
     - `~` or `W` = `WATER` (3)
     - `%` or `T` = `TREES` (4)
     - `-` or `I` = `ICE` (5)
     - `E` = `EAGLE` (6) (at row 24-25, col 12-13)
   - Ensure classic Stage 1 layout is exact (brick fortress around base at bottom center, standard brick corridors and steel blocks, spawn points clear at top left, top middle, top right, and player spawn at bottom left-of-center).
   - Include authentic layouts for Stages 2 through 35 with their characteristic river crossings, tree mazes, ice tracks, and brick/steel labyrinths.
2. Constants:
   - `TOTAL_STAGES = 35`
3. Function:
   - `loadStage(gridMap: GridMap, stageNumber: number): boolean`:
     - Clamps/wraps `stageNumber` (1..35).
     - Resets `gridMap` via `gridMap.initEmpty()`.
     - Populates all cells according to stage map characters.
     - Ensures Eagle HQ (col 12..13, row 24..25) is initialized as intact Eagle base.
     - Returns true on successful load.
  </action>
  <verify>
    <automated>npx tsc --noEmit games/tank-1990/src/stages.ts</automated>
  </verify>
  <done>All 35 stage layouts encoded and loadStage parses maps cleanly into GridMap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build comprehensive Vitest unit test suite for GridMap and Stages</name>
  <files>games/tank-1990/test/GridMap.test.ts</files>
  <behavior>
    - GridMap initializes 26x26 cells with 416x416px total dimensions
    - Directional projectile hits chip 8x8px brick quadrants correctly:
      - UP bullet chipping bottom half then top half
      - DOWN bullet chipping top half then bottom half
      - LEFT bullet chipping right half then left half
      - RIGHT bullet chipping left half then right half
    - Tier 4 projectile destroys entire brick cell or steel cell in one hit
    - Water blocks tank rects but does not block bulletSolid queries
    - Ice and Trees flags are correctly detected on overlapping rect queries
    - Eagle HQ occupies 2x2 cells, registers destruction, and triggers isEagleDestroyed()
    - Shovel fortification turns perimeter into steel and restores previous state upon removal
    - All 35 stages load into GridMap with valid cell counts and intact Eagle HQ
  </behavior>
  <action>
Create `games/tank-1990/test/GridMap.test.ts` with comprehensive Vitest tests covering:
1. `Grid Initialization & Bounds`:
   - Grid dimensions (26 cols, 26 rows, 16px cell size).
   - Out-of-bounds safety on getCell/setCell/damage methods.
2. `Sub-Tile Destruction Bitmasks`:
   - Test full brick (0b1111).
   - Test UP bullet: first hit produces 0b0011 (top half intact, bottom removed), second hit destroys cell (0b0000 -> EMPTY).
   - Test DOWN bullet: first hit produces 0b1100 (bottom half intact, top removed), second hit destroys cell.
   - Test LEFT bullet: first hit produces 0b0101 (left half intact, right removed), second hit destroys cell.
   - Test RIGHT bullet: first hit produces 0b1010 (right half intact, left removed), second hit destroys cell.
   - Sub-tile bounding box generation returns matching 8x8px rects for remaining quadrants.
3. `Material & Tier Damage Properties`:
   - Standard shot on Steel returns false, steel remains intact.
   - Tier 4 shot on Steel destroys cell.
   - Tier 4 shot on Brick destroys cell in single blow.
4. `Terrain Queries`:
   - Tank collision query against Brick, Steel, Water, Ice, Trees, Empty.
   - Bullet collision query: Water is non-solid for bullets; Brick/Steel/Eagle are solid.
   - Ice modifier returns `isIce: true`.
   - Trees modifier returns `isTrees: true`.
5. `Eagle HQ Lifecycle & Shovel Fortification`:
   - Eagle starts intact at (12..13, 24..25).
   - Damage to Eagle sets destroyed = true and updates cells.
   - `fortifyEagle(true)` surrounds Eagle with Steel.
   - `fortifyEagle(false)` restores previous terrain without leaving empty voids.
6. `Campaign Stage Loader`:
   - Loads Stage 1 through Stage 35.
   - Validates no NaN or invalid tile types across all 35 stages.
   - Verifies Eagle base is present at bottom center for all stages.
  </action>
  <verify>
    <automated>npx vitest run games/tank-1990/test/GridMap.test.ts</automated>
  </verify>
  <done>All unit tests pass with 100% success rate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stage Map Array → Stage Loader | Malformed stage index or corrupt character string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-03 | Denial of Service / Crash | `loadStage` | mitigate | Bounds-clamp stage numbers, sanitize unknown characters to `EMPTY`, enforce default fallback map |
</threat_model>

<verification>
Execute `npx vitest run games/tank-1990/test/GridMap.test.ts` to verify 100% test coverage and pass rate across all grid behaviors.
</verification>

<success_criteria>
1. 35 authentic stage maps load cleanly into GridMap.
2. All 4-quadrant sub-tile destruction scenarios pass unit testing.
3. Eagle HQ state, fortification, and terrain query checks pass 100%.
</success_criteria>

<output>
After completion, create `.planning/phases/48-gridmap-engine-and-sub-tile-destruction/48-02-SUMMARY.md`
</output>
