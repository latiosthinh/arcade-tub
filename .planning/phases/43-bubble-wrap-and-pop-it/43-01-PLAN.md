---
phase: 43-bubble-wrap-and-pop-it
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - games/bubble-pop/package.json
  - games/bubble-pop/tsconfig.json
  - games/bubble-pop/index.html
  - games/bubble-pop/src/BubbleGrid.ts
  - games/bubble-pop/src/BubbleAudio.ts
  - games/bubble-pop/src/BubbleScene.ts
  - games/bubble-pop/src/main.ts
  - games/bubble-pop/test/BubblePop.test.ts
autonomous: true
requirements:
  - FR-01
must_haves:
  truths:
    - "Interactive 2D grid bubble sheet responds to click and swipe/drag pointer sweeps"
    - "Bubbles change state from unpopped to popped with snappy plastic deformation and particle bursts"
    - "Rare golden rainbow bubbles trigger arpeggiated chord cascade sound harmonies and glitter confetti"
    - "Fresh sheet button resets or re-rolls the grid with infinite continuous sensory popping"
    - "Tactile Web Audio pops generate high-fidelity physical plastic snaps across diverse pitch bands"
    - "100% unit tests pass for BubbleGrid state matrix, sweep collision, and audio pitch triggers"
  artifacts:
    - path: "games/bubble-pop/src/BubbleGrid.ts"
      provides: "Bubble grid matrix data model, swipe collision detection, golden bubble generation, and reload reset"
      exports: ["BubbleGrid", "BubbleCell"]
    - path: "games/bubble-pop/src/BubbleAudio.ts"
      provides: "Tactile procedural bubble pop synthesizer and rainbow chord cascade audio engine"
      exports: ["BubbleAudio"]
    - path: "games/bubble-pop/src/BubbleScene.ts"
      provides: "Canvas rendering, swipe input handler, papercraft bubble visuals, and UI controls"
      exports: ["BubbleScene"]
    - path: "games/bubble-pop/test/BubblePop.test.ts"
      provides: "Unit tests covering grid indexing, swipe sweep bounds, state changes, and audio triggers"
  key_links:
    - from: "games/bubble-pop/src/BubbleScene.ts"
      to: "games/bubble-pop/src/BubbleGrid.ts"
      via: "invokes popAt / sweepPop on pointer move and renders bubble cells"
      pattern: "bubbleGrid"
    - from: "games/bubble-pop/src/BubbleScene.ts"
      to: "games/bubble-pop/src/BubbleAudio.ts"
      via: "triggers pop sound on cell state transition"
      pattern: "bubbleAudio"
---

<objective>
Implement `bubble-pop` (Bubble Wrap Pop Sandbox) with a dynamic papercraft bubble sheet, pointer swipe/sweep popping, rare golden rainbow cascade bubbles, high-fidelity procedural Web Audio pops, fresh sheet reload, and thorough unit tests.

Purpose: Provide an ultra-relaxing, zero-cognitive-load bubble wrap popping sandbox with realistic sensory feedback.
Output: Self-contained game package in `games/bubble-pop/` with passing Vitest unit tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@packages/game-engine/src/AudioSynthesizer.ts
@games/pop-balloon/src/PopAudio.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build BubbleGrid state matrix, swipe collision logic, and unit tests</name>
  <files>
    games/bubble-pop/package.json,
    games/bubble-pop/tsconfig.json,
    games/bubble-pop/src/BubbleGrid.ts,
    games/bubble-pop/test/BubblePop.test.ts
  </files>
  <behavior>
    - BubbleGrid:
      - `constructor(cols, rows, goldenChance = 0.05)`: initializes grid with unpopped bubbles, randomly assigning golden/rainbow status.
      - `getCell(col, row)`: returns bubble state (col, row, popped, isGolden, popProgress, scale).
      - `popAt(x, y, radius)`: pops bubbles intersecting circle coordinates, returning list of newly popped bubbles.
      - `popCell(col, row)`: pops individual cell; returns false if already popped.
      - `sweepLine(x1, y1, x2, y2, radius)`: pops all bubbles intersecting line segment for smooth fast drag/swipe gestures.
      - `reload(cols, rows)`: regenerates fresh grid sheet with new random golden bubbles.
      - `getStats()`: returns total bubbles, popped count, golden count, and percent popped.
    - BubblePop.test.ts:
      - Grid initialization dimensions and cell count match expectations.
      - Popping an unpopped cell flags `popped: true` and triggers event.
      - Popping already popped cell is a no-op.
      - `sweepLine` correctly identifies and pops multiple bubbles along trajectory.
      - Golden bubbles generate at expected frequency and yield golden flag.
      - Reload resets all cells to unpopped state.
  </behavior>
  <action>
    Create `package.json`, `tsconfig.json`, `BubbleGrid.ts` data model with complete spatial indexing and swipe math, and author unit tests in `BubblePop.test.ts`. Run Vitest to verify all tests pass.
  </action>
  <verify>
    <automated>pnpm --filter bubble-pop test || npx vitest run games/bubble-pop/test/BubblePop.test.ts</automated>
  </verify>
  <done>BubbleGrid handles all cell matrix queries, sweep intersections, golden bubble spawns, and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build BubbleAudio procedural synthesizer, BubbleScene visual canvas renderer, and standalone HTML shell</name>
  <files>
    games/bubble-pop/src/BubbleAudio.ts,
    games/bubble-pop/src/BubbleScene.ts,
    games/bubble-pop/src/main.ts,
    games/bubble-pop/index.html
  </files>
  <action>
    1. Implement `BubbleAudio.ts`:
       - `playPop(pitchVariation, isGolden)`: generates realistic multi-layer bubble snap using sine/triangle snap + burst noise click with variable pitch (500Hz - 900Hz).
       - `playRainbowCascade(streak)`: plays resonant pentatonic chime arpeggios for golden bubble bursts.
       - Honors `arcade-carnival-muted` localStorage key.
    2. Implement `BubbleScene.ts`:
       - Renders tactile 2D Papercraft bubble sheet with embossed shadows, translucent glossy highlights, and crimped seal borders.
       - Supports pointerdown, pointermove, touchmove continuous dragging/swiping.
       - Particle burst generator on pop (plastic flecks, rainbow glitter for golden bubbles).
       - UI HUD: Popped count counter, "New Sheet" refresh button, audio mute toggle, back to hub button.
    3. Implement `main.ts` and `index.html`:
       - Standalone playable entry point with full responsive viewport and touch-action: none.
  </action>
  <verify>
    <automated>npx vitest run games/bubble-pop/test/BubblePop.test.ts</automated>
  </verify>
  <done>Complete standalone `bubble-pop` game running with smooth interactive canvas, audio feedback, and clean papercraft visual style.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Pointer Input → Canvas Coordinates | Untrusted client pointer and touch coordinates translated into canvas grid indices |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Denial of Service | `BubbleGrid.ts` | mitigate | Clamp grid indices and coordinate bounds to prevent array out-of-bounds or infinite loops during sweep calculations |
| T-43-02 | Resource Exhaustion | `BubbleScene.ts` | mitigate | Cap maximum active burst particles (max 200) to prevent frame rate drops during continuous rapid swiping |
</threat_model>

<verification>
- `npx vitest run games/bubble-pop/test/BubblePop.test.ts` passes 100%.
- Game runs in browser with responsive canvas and tactile click/drag popping.
</verification>

<success_criteria>
- Bubble wrap game fully playable with satisfying tactile popping, golden bubble cascade, and sheet regeneration.
- Vitest unit tests pass with zero regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/43-bubble-wrap-and-pop-it/43-01-SUMMARY.md`
</output>
