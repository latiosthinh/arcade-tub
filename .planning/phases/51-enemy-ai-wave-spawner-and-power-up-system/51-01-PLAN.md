---
phase: 51-enemy-ai-wave-spawner-and-power-up-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - games/tank-1990/src/types.ts
  - games/tank-1990/src/EnemyTank.ts
  - games/tank-1990/src/EnemySpawner.ts
autonomous: true
requirements:
  - ENEMY-01
  - ENEMY-02
  - ENEMY-03
  - ENEMY-04
must_haves:
  truths:
    - "EnemyTank supports 4 distinct archetypes (BASIC, FAST, POWER, ARMOR) with authentic speeds, bullet specs, points, and 4-hit armor color degradation."
    - "EnemyTank executes grid-node steering AI at integer tile intersections with goal-oriented bias towards Player or Eagle HQ and anti-180deg oscillation turn locks."
    - "EnemySpawner manages a 20-tank wave queue across 3 top spawn portals with a max 4 concurrent active enemy cap."
    - "EnemySpawner flags the 4th, 11th, and 18th spawned tanks as flashing bonus tanks that emit drop events upon taking damage."
  artifacts:
    - path: "games/tank-1990/src/types.ts"
      provides: "Enemy archetypes, powerup types, spawn configs, and combat interfaces"
      contains: "EnemyType"
    - path: "games/tank-1990/src/EnemyTank.ts"
      provides: "EnemyTank entity class, kinematics, grid-node steering AI, shooting logic, and armor damage states"
      min_lines: 180
    - path: "games/tank-1990/src/EnemySpawner.ts"
      provides: "Wave queue manager, 3-portal spawner, active enemy concurrency tracker, and flashing bonus tank tagging"
      min_lines: 100
  key_links:
    - from: "games/tank-1990/src/EnemyTank.ts"
      to: "games/tank-1990/src/GridMap.ts"
      via: "queryRect and getIntersectingCells navigation checks"
      pattern: "grid\\.queryRect"
    - from: "games/tank-1990/src/EnemySpawner.ts"
      to: "games/tank-1990/src/EnemyTank.ts"
      via: "instantiation and active enemy list management"
      pattern: "new EnemyTank"
---

<objective>
Implement EnemyTank entity mechanics with 4 archetypes (Basic, Fast, Power, Armor), grid-node steering AI with anti-oscillation locks and player/base goal-bias, and EnemySpawner managing 20-tank wave queues across 3 top portals with 4 concurrent cap and 4th/11th/18th flashing bonus tank tagging.

Purpose: Deliver authentic Battle City enemy behaviors, diverse tactical combat challenges, and wave progression foundation.
Output: `games/tank-1990/src/types.ts` (updated), `games/tank-1990/src/EnemyTank.ts`, `games/tank-1990/src/EnemySpawner.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-enemy-ai-wave-spawner-and-power-up-system/51-CONTEXT.md
@games/tank-1990/src/types.ts
@games/tank-1990/src/GridMap.ts
@games/tank-1990/src/PlayerTank.ts
@games/tank-1990/src/BulletManager.ts

<interfaces>
From games/tank-1990/src/types.ts:
```typescript
export enum TileType { EMPTY = 0, BRICK = 1, STEEL = 2, WATER = 3, TREES = 4, ICE = 5, EAGLE = 6 }
export type CardinalDirection = 'UP' | 'DOWN' | 'LEFT' | 'RIGHT';
export interface Rect { x: number; y: number; width: number; height: number; }
export interface CombatTankTarget {
  id: string | number;
  x: number;
  y: number;
  width: number;
  height: number;
  isPlayer: boolean;
  isDead: boolean;
  isInvulnerable?: boolean;
  armorHp?: number;
  takeDamage: (damage: number) => boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Enemy & PowerUp Core Types and Enums</name>
  <files>games/tank-1990/src/types.ts</files>
  <action>
    Expand `games/tank-1990/src/types.ts` to add:
    1. `EnemyType` enum:
       - `BASIC` = 'BASIC' (Speed: 48 px/s, bulletSpeed: 160 px/s, HP: 1, 100 pts)
       - `FAST` = 'FAST' (Speed: 96 px/s, bulletSpeed: 192 px/s, HP: 1, 200 pts)
       - `POWER` = 'POWER' (Speed: 64 px/s, bulletSpeed: 280 px/s, HP: 1, 300 pts)
       - `ARMOR` = 'ARMOR' (Speed: 48 px/s, bulletSpeed: 160 px/s, HP: 4, 400 pts)
    2. `ArmorColor` enum/type for armor HP degradation:
       - 4 HP: 'GREEN', 3 HP: 'YELLOW', 2 HP: 'ORANGE', 1 HP: 'WHITE'
    3. `PowerUpType` enum:
       - `STAR` = 'STAR' (tier +1)
       - `SHOVEL` = 'SHOVEL' (fortifies base perimeter with steel for 20s)
       - `GRENADE` = 'GRENADE' (destroys all active enemies on screen)
       - `CLOCK` = 'CLOCK' (freezes all enemies for 10s)
       - `HELMET` = 'HELMET' (invulnerability shield for 10s)
       - `TANK` = 'TANK' (+1 extra life)
       - `GUN` = 'GUN' (instantly promotes to Tier 4)
       - `BOAT` = 'BOAT' (enables crossing water terrain)
    4. `EnemyConfig` interface and `ENEMY_CONFIGS` dictionary per archetype.
    5. `EnemyTankState` interface for serialization, rendering, and AI querying.
    6. `SpawnPortal` definition: 3 top spawn positions (Col 0, Col 12, Col 24 at Row 0).
    7. `PowerUpItem` interface: `id`, `type`, `x`, `y`, `width` (30), `height` (30), `alive`, `duration`, `flashTimer`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>EnemyType, PowerUpType, ArmorColor, ENEMY_CONFIGS, and associated state interfaces are exported and type-check without error.</done>
</task>

<task type="auto">
  <name>Task 2: Implement EnemyTank Class with Grid-Node Steering AI and Armor Degradation</name>
  <files>games/tank-1990/src/EnemyTank.ts</files>
  <action>
    Create `games/tank-1990/src/EnemyTank.ts` implementing `CombatTankTarget`:
    1. Properties:
       - `id`: unique number / string
       - `type`: `EnemyType`
       - `x`, `y`, `width` (28), `height` (28), `direction`: `CardinalDirection`
       - `hp`: number (1 for Basic/Fast/Power, 4 for Armor)
       - `isFlashing`: boolean (bonus tank flag)
       - `isFrozen`: boolean (Clock freeze status)
       - `freezeTimer`: number
       - `isDead`: boolean
       - `isPlayer`: false
       - `fireTimer`: number, `fireInterval`: randomized 1.0s - 2.5s
       - `turnCooldown`: number, `lastTurnNode`: string
    2. Kinematics & Grid-Node AI:
       - Node alignment check: When tank center reaches near integer 16px tile alignments (`Math.abs(x % 16 - 2) < 2` or `Math.abs(y % 16 - 2) < 2`), evaluate steering decisions.
       - Direction selection algorithm:
         - Calculate available unobstructed directions (querying `grid.queryRect` along UP, DOWN, LEFT, RIGHT).
         - Anti-180deg oscillation lock: Filter out immediate reverse direction unless all other 3 directions are blocked by obstacles.
         - Goal-oriented bias: With 60% probability, bias choice towards lowest Manhattan distance to Target (Eagle HQ at col 12, row 24 or PlayerTank location). With 40% probability, pick random unobstructed direction.
         - If obstructed while moving forward, immediately trigger node turn decision.
    3. Armor & Damage Resolution:
       - `takeDamage(amount: number = 1)`: Decrements `hp`. For Armor tank, color transitions: 4 HP (Green) -> 3 HP (Yellow) -> 2 HP (Orange) -> 1 HP (White).
       - If `hp <= 0`, sets `isDead = true` and returns `true`.
       - If `isFlashing` and damaged, emit/flag bonus drop event.
    4. Shooting logic:
       - Count down `fireTimer`. When <= 0 and not frozen, attempt firing bullet via `BulletManager.fire(x, y, direction, 'ENEMY', stats)`.
    5. Freeze handling:
       - `setFreeze(duration: number)`: halts AI movement and firing countdown during Clock effect.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>EnemyTank implements full 4-archetype stats, grid-node steering with anti-oscillation lock, armor HP degradation, shooting logic, and freeze handling.</done>
</task>

<task type="auto">
  <name>Task 3: Implement EnemySpawner Class with Wave Queue and Flashing Tank Bonus Drops</name>
  <files>games/tank-1990/src/EnemySpawner.ts</files>
  <action>
    Create `games/tank-1990/src/EnemySpawner.ts`:
    1. Manages a 20-tank wave queue for a stage:
       - Default queue composition (e.g. 14 Basic, 4 Fast, 1 Power, 1 Armor, or stage-defined composition).
       - Portals: 3 top portals at (Col 0, Row 0), (Col 12, Row 0), (Col 24, Row 0).
       - Portal rotation: Left -> Mid -> Right -> Left.
    2. Concurrency & Timing:
       - Maximum 4 concurrent active enemy tanks on the field simultaneously.
       - Spawn interval / animation delay (e.g., 2.5s spawn delay between spawns when under concurrency cap).
       - Checks spawn portal collision before spawning (does not spawn directly on top of another existing tank if blocked).
    3. Flashing Bonus Tanks (ENEMY-04):
       - Tracks overall spawn counter within current wave (1 to 20).
       - Tanks spawned on the 4th, 11th, and 18th spawns are automatically flagged `isFlashing = true`.
       - When a flashing tank takes damage, triggers `onBonusDrop(tank)` callback providing location `(x, y)` for power-up generation.
    4. Public APIs:
       - `initWave(enemyTypes: EnemyType[])`: Initializes 20-tank queue.
       - `update(dt: number, playerX?: number, playerY?: number)`: Updates spawn timers, active enemy AI, and removes dead tanks.
       - `getActiveEnemies(): EnemyTank[]`
       - `getRemainingCount(): number`: Total remaining in queue + active.
       - `freezeAll(duration: number)`: Calls `setFreeze` on all active enemies.
       - `killAll(awardScoreCallback?: (points: number) => void)`: Destroys all active enemies (Grenade effect) and triggers point awards.
       - `onBonusDrop`: Event listener / callback for powerup spawning.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>EnemySpawner manages 20-tank wave list, 3-portal round-robin, 4 concurrent max limit, 4th/11th/18th flashing bonus tank drops, and screen-wide freeze/kill operations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave Spawner -> Arena Grid | Untrusted or invalid spawn positions could clip outside 416x416 arena or spawn inside solid steel |
| Enemy AI -> Kinematics | Rapid heading switches could create infinite 180-degree jitter oscillations at corridor boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Repudiation / State Sync | EnemySpawner | mitigate | Strict 20-tank queue index tracking; decrement remaining count only when tank is spawned and track active set with unique IDs |
| T-51-02 | Denial of Service | EnemyTank AI | mitigate | Anti-180deg turn locking algorithm with minimum node traversal threshold prevents infinite oscillation loops |
| T-51-03 | Tampering | EnemySpawner Portal Collision | mitigate | Check portal footprint collision before spawning; delay spawn tick if portal is temporarily obstructed by another tank |
</threat_model>

<verification>
Automated type-check and compilation pass:
`npx tsc --noEmit`
</verification>

<success_criteria>
EnemyTank and EnemySpawner classes exist, compile cleanly, implement all 4 archetypes, grid-node steering AI, 20-tank wave queue, 3-portal rotation, 4-tank concurrency limit, and flashing bonus tagging.
</success_criteria>

<output>
After completion, create `.planning/phases/51-enemy-ai-wave-spawner-and-power-up-system/51-01-SUMMARY.md`
</output>
