---
phase: 51-enemy-ai-wave-spawner-and-power-up-system
plan: 02
type: execute
wave: 2
depends_on:
  - 51-01
files_modified:
  - games/tank-1990/src/PowerUpSystem.ts
  - games/tank-1990/test/EnemyTank.test.ts
  - games/tank-1990/test/EnemySpawner.test.ts
  - games/tank-1990/test/PowerUpSystem.test.ts
autonomous: true
requirements:
  - ENEMY-05
  - ENEMY-06
must_haves:
  truths:
    - "PowerUpSystem spawns and manages all 8 tactical powerup items (Star, Shovel, Grenade, Clock, Helmet, Tank, Gun, Boat) with AABB collection detection against PlayerTank."
    - "Collecting Star upgrades player tier by +1; collecting Gun instantly elevates player to Tier 4; collecting Helmet grants 10s invulnerability shield; collecting Tank awards +1 life; collecting Boat enables water crossing."
    - "Collecting Grenade instantly destroys all active enemies and awards 500 bonus points; collecting Clock freezes all enemies for 10s."
    - "Collecting Shovel transforms Eagle HQ perimeter into Steel walls for 20s, caches the prior terrain state, and cleanly restores perimeter tiles upon expiration without leaving empty voids."
    - "Comprehensive Vitest test suite achieves 100% pass rate across EnemyTank, EnemySpawner, and PowerUpSystem modules."
  artifacts:
    - path: "games/tank-1990/src/PowerUpSystem.ts"
      provides: "PowerUpSystem managing all 8 items, collection resolution, shovel terrain caching/restoration, and active timers"
      min_lines: 150
    - path: "games/tank-1990/test/EnemyTank.test.ts"
      provides: "Unit tests verifying all 4 enemy archetypes, kinematics, steering AI, and armor degradation"
      min_lines: 80
    - path: "games/tank-1990/test/EnemySpawner.test.ts"
      provides: "Unit tests verifying 20-tank wave queue, 3-portal rotation, concurrency caps, and 4th/11th/18th bonus drops"
      min_lines: 80
    - path: "games/tank-1990/test/PowerUpSystem.test.ts"
      provides: "Unit tests verifying all 8 powerup effects, AABB pickup detection, and shovel base fortification caching/restoration"
      min_lines: 100
  key_links:
    - from: "games/tank-1990/src/PowerUpSystem.ts"
      to: "games/tank-1990/src/GridMap.ts"
      via: "Base perimeter caching and steel wall fortification"
      pattern: "grid\\.setCell"
    - from: "games/tank-1990/src/PowerUpSystem.ts"
      to: "games/tank-1990/src/PlayerTank.ts"
      via: "Tier upgrades, shield setting, life increments, and boat enablement"
      pattern: "player\\.upgradeTier|player\\.setShield|player\\.addLife"
    - from: "games/tank-1990/src/PowerUpSystem.ts"
      to: "games/tank-1990/src/EnemySpawner.ts"
      via: "Screen-wide killAll and freezeAll invocations"
      pattern: "spawner\\.killAll|spawner\\.freezeAll"
---

<objective>
Implement PowerUpSystem managing all 8 tactical powerup items (Star, Shovel, Grenade, Clock, Helmet, Tank, Gun, Boat), complete Eagle HQ base perimeter caching and 20s steel fortification restoration, and construct comprehensive Vitest unit test suites for EnemyTank, EnemySpawner, and PowerUpSystem.

Purpose: Deliver full tactical powerup mechanics and verify zero-regression robustness with 100% test coverage.
Output: `games/tank-1990/src/PowerUpSystem.ts`, `games/tank-1990/test/EnemyTank.test.ts`, `games/tank-1990/test/EnemySpawner.test.ts`, `games/tank-1990/test/PowerUpSystem.test.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-enemy-ai-wave-spawner-and-power-up-system/51-CONTEXT.md
@.planning/phases/51-enemy-ai-wave-spawner-and-power-up-system/51-01-PLAN.md
@games/tank-1990/src/types.ts
@games/tank-1990/src/GridMap.ts
@games/tank-1990/src/PlayerTank.ts
@games/tank-1990/src/BulletManager.ts
@games/tank-1990/src/EnemyTank.ts
@games/tank-1990/src/EnemySpawner.ts

<interfaces>
From games/tank-1990/src/types.ts:
```typescript
export enum PowerUpType {
  STAR = 'STAR',
  SHOVEL = 'SHOVEL',
  GRENADE = 'GRENADE',
  CLOCK = 'CLOCK',
  HELMET = 'HELMET',
  TANK = 'TANK',
  GUN = 'GUN',
  BOAT = 'BOAT',
}

export interface PowerUpItem {
  id: number;
  type: PowerUpType;
  x: number;
  y: number;
  width: number;
  height: number;
  alive: boolean;
  duration?: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement PowerUpSystem with 8 Items and Shovel Base Perimeter Fortification</name>
  <files>games/tank-1990/src/PowerUpSystem.ts</files>
  <action>
    Create `games/tank-1990/src/PowerUpSystem.ts`:
    1. Manages active powerups on field:
       - `items: PowerUpItem[]` (standard 30x30px collectible item bounding box, clamped within arena 0-386).
       - `spawnRandomPowerUp(x?: number, y?: number): PowerUpItem`: Spawns 1 of 8 powerup types with equal or weighted probability. If (x, y) not specified or out of bounds, selects valid random grid cell.
       - `spawnPowerUp(type: PowerUpType, x: number, y: number): PowerUpItem`.
    2. Shovel Base Fortification & Terrain State Caching (ENEMY-06):
       - Eagle base perimeter coordinates (surrounding Eagle at col 12-13, row 24-25):
         `[{col: 11, row: 23}, {col: 12, row: 23}, {col: 13, row: 23}, {col: 14, row: 23}, {col: 11, row: 24}, {col: 14, row: 24}, {col: 11, row: 25}, {col: 14, row: 25}]`
       - `shovelTimer: number = 0` (20s duration).
       - `cachedBasePerimeter: Map<string, GridCell> | null = null`.
       - When SHOVEL activated:
         - If `cachedBasePerimeter === null`, snapshot current cells (type + mask) for all 8 perimeter coordinates.
         - Overwrite all 8 perimeter cells with `TileType.STEEL, SubTileMask.FULL`.
         - Reset `shovelTimer = 20.0`.
       - During update(dt):
         - Decrement `shovelTimer`.
         - When `shovelTimer <= 0` and `cachedBasePerimeter !== null`: restore all 8 perimeter cells to their cached states and reset cache to null.
    3. Item Pickup & Effect Execution (ENEMY-05):
       - Checks AABB overlap between player bounds and each active `PowerUpItem`.
       - On collision, sets item `alive = false` and applies effect:
         - `STAR`: `player.upgradeTier()`, awards 500 pts.
         - `GUN`: `player.setTier(TankTier.TIER_4)`, awards 500 pts.
         - `HELMET`: `player.setShield(10.0)`, awards 500 pts.
         - `TANK`: `player.addLife(1)`, awards 500 pts.
         - `BOAT`: `player.setBoat(true)`, awards 500 pts.
         - `GRENADE`: calls `spawner.killAll()`, awards 500 pts.
         - `CLOCK`: calls `spawner.freezeAll(10.0)`, awards 500 pts.
         - `SHOVEL`: activates 20s steel perimeter fortification, awards 500 pts.
       - Emits pickup event: `{ type: PowerUpType, points: 500, x: number, y: number }`.
    4. Lifecycle & Updates:
       - `update(dt: number, player: PlayerTank, spawner: EnemySpawner)`: Handles shovel timer, item collection, and removes inactive items.
       - `clear()`: Cleans up items and restores shovel base if active.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>PowerUpSystem handles all 8 items, AABB collection, score events, and shovel 20s base perimeter caching/restoration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Comprehensive Vitest Unit Tests for EnemyTank, EnemySpawner, and PowerUpSystem</name>
  <files>
    games/tank-1990/test/EnemyTank.test.ts,
    games/tank-1990/test/EnemySpawner.test.ts,
    games/tank-1990/test/PowerUpSystem.test.ts
  </files>
  <behavior>
    - EnemyTank tests:
      - 4 archetypes verify correct initial speed, bullet speed, HP, and point values.
      - Armor tank verifies 4-hit color degradation (Green -> Yellow -> Orange -> White) and takes damage properly.
      - Grid-node steering AI verifies obstacle collision avoidance, goal bias towards Eagle/Player, and anti-180deg oscillation lock.
      - Freeze status halts movement and fire timer.
    - EnemySpawner tests:
      - Wave queue initializes with 20 tanks and enforces max 4 concurrent active limit.
      - Spawns cycle through 3 top portals (Left, Center, Right).
      - 4th, 11th, and 18th spawned tanks are flagged with isFlashing = true.
      - killAll() and freezeAll() affect all currently active enemy tanks.
    - PowerUpSystem tests:
      - Spawns all 8 powerup types with valid coordinates.
      - Player collecting STAR upgrades tier; GUN grants Tier 4; HELMET grants 10s shield; TANK adds +1 life; BOAT enables water passing.
      - Collecting GRENADE triggers spawner.killAll(); CLOCK triggers spawner.freezeAll(10).
      - Collecting SHOVEL creates steel perimeter around Eagle HQ, caches previous brick layout, and restores original tiles after 20s timer expires.
  </behavior>
  <action>
    Create complete Vitest unit test suites in `games/tank-1990/test/`:
    1. `EnemyTank.test.ts`: test archetypes, damage degradation, AI steering node alignment, anti-oscillation, freeze, and firing.
    2. `EnemySpawner.test.ts`: test 20-tank wave queue, concurrency capping at 4, 3-portal rotation, flashing bonus tanks on 4th/11th/18th spawn, and bulk freeze/kill.
    3. `PowerUpSystem.test.ts`: test spawning, AABB collection, each of 8 powerup effects, and shovel perimeter terrain caching and timeout restoration.
  </action>
  <verify>
    <automated>npm test -- games/tank-1990</automated>
  </verify>
  <done>All unit tests for EnemyTank, EnemySpawner, and PowerUpSystem pass with 100% success rate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PowerUp -> PlayerTank State | Powerup effects directly modify player tier, shield, lives, and boat flags |
| Shovel -> GridMap Perimeter | Modifying grid cells could corrupt stage map memory if original tiles are not cleanly cached and restored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-04 | State Corruption | Shovel Base Fortification | mitigate | Snapshot deep copy of TileType and bitmask for all 8 perimeter coordinates before replacing with steel; restore exact cached map on timer expiry |
| T-51-05 | Resource Exhaustion | PowerUpSystem Spawn | mitigate | Cap maximum active powerups on field (e.g. 1 active powerup at a time, overwriting previous if uncollected, matching Battle City arcade behavior) |
</threat_model>

<verification>
Execute full Vitest test suite across all Tank 1990 modules:
`npm test -- games/tank-1990`
</verification>

<success_criteria>
All 8 powerup items work as specified, shovel terrain caching/restoration operates cleanly without tile corruption, and all Vitest unit tests pass 100%.
</success_criteria>

<output>
After completion, create `.planning/phases/51-enemy-ai-wave-spawner-and-power-up-system/51-02-SUMMARY.md`
</output>
