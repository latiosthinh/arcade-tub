---
phase: 07-polish-deploy
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/hub.ts
  - src/hub.css
  - index.html
  - games/safe-cracker/index.html
  - games/brick-blitz/index.html
  - games/sky-hopper/index.html
  - games/crate-catch/index.html
  - games/type-strike/index.html
  - games/safe-cracker/src/SafeCrackerScene.ts
  - games/brick-blitz/src/BrickBlitzScene.ts
  - games/sky-hopper/src/SkyHopperScene.ts
  - games/crate-catch/src/CrateCatchScene.ts
  - games/type-strike/src/TypeStrikeScene.ts
autonomous: true
requirements:
  - REQ-10
  - REQ-11
  - REQ-12

must_haves:
  truths:
    - "All 5 games trigger procedural Web Audio sound effects during gameplay interactions"
    - "All game pages include a visible, accessible '← Hub' button and Escape return navigation"
    - "Hub page displays personal high score badges loaded from local storage on each game card"
    - "Hub page supports keyboard navigation (1-5 number keys, arrow keys, Enter) and '?' or 'H' controls overlay"
  artifacts:
    - path: "src/hub.ts"
      provides: "Enhanced hub with high score badges, keyboard navigation, and help overlay"
      min_lines: 80
    - path: "src/hub.css"
      provides: "High score badge styling, accessibility focus rings, and shortcut modal styles"
      min_lines: 100
  key_links:
    - from: "src/hub.ts"
      to: "@arcade-carnival/playables-adapter"
      via: "loadData high score retrieval"
      pattern: "loadData\\("
    - from: "games/*/src/*Scene.ts"
      to: "packages/game-engine/src/AudioSynthesizer.ts"
      via: "audio singleton calls on game events"
      pattern: "audio\\.play"
---

<objective>
Integrate `AudioSynthesizer` procedural sound effects across all 5 games, add universal "← Hub" navigation buttons to all game views, and enhance the Arcade Carnival Hub with live high score badges, keyboard navigation, and controls modal.

Purpose: Bring unified polish, instant audio feedback, seamless back-to-hub routing, and enhanced accessibility across the arcade suite.
Output: Audio-wired game scenes, updated game HTML templates with hub return navigation, and enriched hub script/styles.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-deploy/07-CONTEXT.md
@packages/game-engine/src/AudioSynthesizer.ts
@src/hub.ts
@src/hub.css

<interfaces>
From packages/game-engine/src/index.ts:
```typescript
export { AudioSynthesizer, audio } from './AudioSynthesizer.js';
```

From packages/playables-adapter/src/index.ts:
```typescript
export function loadData(key: string): string | null;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire audio synthesizer into all 5 minigames</name>
  <files>games/safe-cracker/src/SafeCrackerScene.ts, games/brick-blitz/src/BrickBlitzScene.ts, games/sky-hopper/src/SkyHopperScene.ts, games/crate-catch/src/CrateCatchScene.ts, games/type-strike/src/TypeStrikeScene.ts</files>
  <read_first>
    - packages/game-engine/src/AudioSynthesizer.ts
    - games/safe-cracker/src/SafeCrackerScene.ts
    - games/brick-blitz/src/BrickBlitzScene.ts
    - games/sky-hopper/src/SkyHopperScene.ts
    - games/crate-catch/src/CrateCatchScene.ts
    - games/type-strike/src/TypeStrikeScene.ts
  </read_first>
  <action>
    Import `audio` from `@arcade-carnival/game-engine` into each scene and wire relevant sound effects:
    1. **Safe Cracker**:
       - `audio.playClick()` on pick lock attempt / dial movement
       - `audio.playScore()` on target zone hit
       - `audio.playError()` on miss or lock fail
       - `audio.playExplosion()` on timer expiration / game over
    2. **Brick Blitz**:
       - `audio.playBounce()` on paddle or wall deflection
       - `audio.playScore()` on brick destruction
       - `audio.playPowerup()` on bonus brick or extra life
       - `audio.playError()` on ball lost / life drop
       - `audio.playVictory()` on level clear
    3. **Sky Hopper**:
       - `audio.playBounce()` on platform spring/jump
       - `audio.playPowerup()` on rocket/spring item pickup
       - `audio.playExplosion()` on obstacle stomp or projectile hit
       - `audio.playError()` on obstacle hit or player death
       - `audio.playVictory()` on mothership story clear
    4. **Crate Catch**:
       - `audio.playScore()` on catching a falling crate
       - `audio.playPowerup()` on banking stacked crates
       - `audio.playExplosion()` on bomb impact
       - `audio.playError()` on crate loss limit reached / game over
    5. **Type Strike**:
       - `audio.playClick()` on correct keystroke advance
       - `audio.playScore()` on enemy word elimination
       - `audio.playPowerup()` on streak multiplier level-up
       - `audio.playError()` on mistype or shield breach
       - `audio.playExplosion()` on base explosion / game over
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>All 5 game scenes invoke procedural audio methods at key gameplay events with 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add universal Back to Hub navigation and game layout polish</name>
  <files>games/safe-cracker/index.html, games/brick-blitz/index.html, games/sky-hopper/index.html, games/crate-catch/index.html, games/type-strike/index.html</files>
  <read_first>
    - games/safe-cracker/index.html
    - games/brick-blitz/index.html
    - games/sky-hopper/index.html
    - games/crate-catch/index.html
    - games/type-strike/index.html
  </read_first>
  <action>
    Add a retro-styled, accessible "← Hub" navigation button to each game's `index.html`:
    - Position: Fixed floating in top-left corner (`position: absolute; top: 12px; left: 12px; z-index: 1000;`).
    - Styled with arcade neon border, high contrast, pointer cursor, and hover/active states.
    - Semantic HTML `<a href="/" class="hub-btn" aria-label="Return to Arcade Hub">← Arcade Hub</a>` (or `../../index.html` relative path).
    - Also ensure viewport metadata, title naming consistency, and high-contrast styling are applied across all 5 HTML entries.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>All 5 game HTML files render a styled, accessible "← Arcade Hub" link back to root.</done>
</task>

<task type="auto">
  <name>Task 3: Enhance Arcade Hub with high scores, keyboard navigation, and shortcuts modal</name>
  <files>src/hub.ts, src/hub.css, index.html</files>
  <read_first>
    - src/hub.ts
    - src/hub.css
    - index.html
  </read_first>
  <action>
    1. Update `src/hub.ts`:
       - Query stored personal best score for each game via `loadData('{slug}-highscore')` or localStorage fallback and render a retro "🏆 BEST: [score]" badge on each card.
       - Add keyboard navigation:
         - Keys 1-5 immediately launch games 1 through 5.
         - Left / Right / Up / Down arrow keys move card focus.
         - Enter key launches focused card.
         - '?' or 'H' toggles a Help / Controls cheatsheet modal.
         - 'M' toggles audio mute globally (`audio.toggleMute()`).
       - Render sound mute toggle button in top-right of hub header.
    2. Update `src/hub.css`:
       - Add styles for `.card-highscore`, `.hub-controls-modal`, `.hub-sound-toggle`, `.hub-header`, and high-visibility `:focus-visible` focus rings (REQ-12).
    3. Update `index.html` with ARIA landmark roles and descriptive labels.
  </action>
  <verify>
    <automated>pnpm build && pnpm test</automated>
  </verify>
  <done>Hub displays high scores for all games, supports 1-5 number and arrow keyboard navigation, toggles shortcuts modal, and passes build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hub UI -> LocalStorage / Playables Adapter | Reading high score values for display |
| Keyboard Input -> Navigation | Fast keyboard switching between game routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | src/hub.ts | mitigate | Sanitize high score inputs loaded from localStorage/data cache: parseInt and clamp before rendering inside textContent. |
| T-07-04 | Elevation of Privilege | Hub Navigation | accept | Static client-side routing within application bundle. |
</threat_model>

<verification>
Run `pnpm build` and `pnpm typecheck` to ensure full cross-game compilation.
</verification>

<success_criteria>
1. All 5 games invoke audio SFX on actions without errors.
2. Every game has a working "← Hub" navigation link.
3. Hub shows live high score badges and handles keyboard navigation.
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-deploy/07-02-SUMMARY.md`
</output>
