---
phase: 07-polish-deploy
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - scripts/audit-bundle.js
  - package.json
autonomous: true
requirements:
  - REQ-10
  - REQ-11
  - REQ-12

must_haves:
  truths:
    - "Production build generates standalone static assets for hub and all 5 games in dist/"
    - "Every game entry bundle is verified to be under 200KB gzipped"
    - "Full test suite across entire monorepo passes with zero failures"
  artifacts:
    - path: "scripts/audit-bundle.js"
      provides: "Automated gzip bundle size analyzer and threshold validator"
      min_lines: 25
  key_links:
    - from: "package.json"
      to: "scripts/audit-bundle.js"
      via: "pnpm run audit-bundle"
      pattern: "audit-bundle"
---

<objective>
Create automated bundle size auditing script and execute full production build and verification across the monorepo to guarantee REQ-10 (static assets per game), REQ-11 (< 200KB gzipped per game), and REQ-12 (accessible keyboard navigation).

Purpose: Ensure deployment readiness for YouTube Playables hosting with deterministic size budget enforcement and zero build/test regressions.
Output: `scripts/audit-bundle.js`, updated `package.json` scripts, and verified `dist/` directory.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-deploy/07-CONTEXT.md
@package.json
@vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bundle size and production asset audit script</name>
  <files>scripts/audit-bundle.js, package.json</files>
  <read_first>
    - package.json
    - vite.config.ts
    - .planning/phases/07-polish-deploy/07-CONTEXT.md
  </read_first>
  <action>
    1. Create `scripts/audit-bundle.js`:
       - Read built output files in `dist/` and `dist/assets/`.
       - Compute raw and gzip compressed size in bytes for each HTML entry and JS/CSS chunk using Node.js `zlib.gzipSync`.
       - Enforce maximum threshold: every game entry HTML + associated chunks < 200 KB gzipped (REQ-11).
       - Print formatted summary table of all bundles and exit with code 0 if within budget, or code 1 if any bundle exceeds 200KB.
    2. Add `"audit-bundle": "node scripts/audit-bundle.js"` script to root `package.json`.
  </action>
  <verify>
    <automated>pnpm build && node scripts/audit-bundle.js</automated>
  </verify>
  <done>Audit script reports gzip sizes for all minigames and passes 200KB threshold verification.</done>
</task>

<task type="auto">
  <name>Task 2: Monorepo end-to-end test and build verification</name>
  <files>package.json</files>
  <read_first>
    - package.json
    - ROADMAP.md
  </read_first>
  <action>
    Run complete validation suite across all monorepo packages and games:
    1. `pnpm typecheck` — 0 TypeScript errors across workspace.
    2. `pnpm test` — all unit tests pass across `@arcade-carnival/game-engine`, `@arcade-carnival/playables-adapter`, and all 5 minigames.
    3. `pnpm build` — static dist bundle generated successfully.
    4. `node scripts/audit-bundle.js` — bundle sizes validated under 200KB gzipped limit.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test && pnpm build && node scripts/audit-bundle.js</automated>
  </verify>
  <done>All validation commands succeed cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build Output -> Production Hosting | Static asset generation in dist/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Denial of Service | Build Bundle Size | mitigate | Enforce strict <200KB gzipped limit via automated audit script during CI/CD. |
</threat_model>

<verification>
Execute `pnpm typecheck && pnpm test && pnpm build && pnpm run audit-bundle`
</verification>

<success_criteria>
1. `dist/` contains all 5 game builds plus arcade hub.
2. Every bundle stays well below 200KB gzipped limit.
3. Test suite and typechecker pass 100%.
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-deploy/07-03-SUMMARY.md`
</output>
