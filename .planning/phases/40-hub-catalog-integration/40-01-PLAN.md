# Plan: Phase 40-01 - Hub Catalog Integration & Final Milestone Audit

## Task 1: Catalog Registration, SVG Screenshots & Hub Data
- Verify all 27 games (15 existing + 12 new CrazyGames minigames) are registered in `src/data/games.ts` with correct metadata, controls, category, tags, and theme colors.
- Ensure all 27 games have bespoke, handcrafted 2D Papercraft SVG screenshot assets in `src/data/screenshots.ts`.
- Ensure all game HTML shells (`games/*/index.html`) build cleanly in `vite.config.ts`.
- Update `test/shells.test.ts`, `test/data/games.test.ts` (if present), and hub view integration tests.

## Task 2: Build Audit, Comprehensive Test Pass & Milestone Audit
- Run full Vitest suite across hub, components, and all 27 games ensuring 100% pass rate.
- Run `pnpm run build` and production bundle audit (`scripts/audit-bundle.js` / `test/production/bundle-audit.test.ts`).
- Produce `.planning/v6.0-MILESTONE-AUDIT.md`.
- Update `.planning/STATE.md` and `.planning/ROADMAP.md` marking v6.0 complete.
