---
phase: 30-complex-renderers-and-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - games/space-racer/src/WarpRenderer.ts
  - games/space-racer/src/SpaceRacerScene.ts
  - games/virus-defense/src/BioArenaRenderer.ts
  - src/data/games.ts
autonomous: true
requirements:
  - CRAFT-07
  - CRAFT-08
  - CRAFT-09
must_haves:
  truths:
    - "Space Racer renders with construction paper starfield, cardboard asteroid cutouts, paper origami boost gates, papercraft spaceship with cardboard wings, and taped HUD placards"
    - "Virus Defense renders with cardboard bio-arena petri dish, papercut center nucleus, origami pathogen swarms, cardboard laser turret, and paper status cards"
    - "src/data/games.ts catalog metadata (titles, descriptions, icons, theme colors, banner gradients) updated for all remaining games (space-racer, virus-defense, snake-eat, bug-climb, car-race)"
    - "Full test suite (619+ tests) passes 100%, Vite production build succeeds, and bundle size audit confirms <200KB gzipped"
  artifacts:
    - path: "games/space-racer/src/WarpRenderer.ts"
      provides: "Construction paper starfield, cardboard asteroid cutouts, paper origami boost rings, papercraft delta-wing spaceship with cardboard wings"
      contains: "drawAsteroid"
    - path: "games/space-racer/src/SpaceRacerScene.ts"
      provides: "Paper HUD gauges, speedometer, distance placards, sticky note overlays in Patrick Hand and Comfortaa"
      contains: "Patrick Hand"
    - path: "games/virus-defense/src/BioArenaRenderer.ts"
      provides: "Cardboard bio-arena petri dish, papercut nucleus cell, origami pathogen swarms, cardboard laser turret with brass hub, sticky note HUD"
      contains: "Patrick Hand"
    - path: "src/data/games.ts"
      provides: "Complete papercraft metadata overhaul across all 15 catalog games"
      contains: "linear-gradient(135deg, #F4EAD4"
  key_links:
    - from: "games/space-racer/src/SpaceRacerScene.ts"
      to: "games/space-racer/src/WarpRenderer.ts"
      via: "WarpRenderer renderBackground, renderTrack, renderObstacles, renderShip"
      pattern: "this\\.renderer\\.render"
    - from: "games/virus-defense/src/VirusDefenseScene.ts"
      to: "games/virus-defense/src/BioArenaRenderer.ts"
      via: "BioArenaRenderer render pipeline"
      pattern: "this\\.renderer\\.render"
    - from: "src/data/games.ts"
      to: "index.html"
      via: "Hub game catalog representation"
      pattern: "GAMES"
---

<objective>
Convert canvas renderers for `space-racer` and `virus-defense` to 2D Papercraft visuals, finalize metadata in `src/data/games.ts` for all remaining games, and execute the final release audit (bundle size check < 200KB gzipped, 100% test pass verification, and production build).
1. `space-racer`: Replace dark space with deep indigo/inked construction paper starfield, papercut star stickers and stamped constellations, cardboard asteroid cutouts with paper craters, paper origami turbo boost rings, cardboard delta-wing spaceship with layered cardboard wings and paper thruster flame, and taped sticky-note HUD and overlays.
2. `virus-defense`: Replace cyber-neon bio-dish with a layered cardboard bio-arena petri dish (`#C5A880` / `#D8C3A5`), papercut center nucleus with cytoplasmic paper folds, origami pathogen swarms (spiker, speedster, splitter, shield carrier) with ink outlines (`#3E2723`), cardboard laser turret launcher with brass hub, floating paper antibody capsules, and taped sticky-note HUD.
3. `src/data/games.ts`: Complete metadata overhaul for all remaining games (`space-racer`, `virus-defense`, `snake-eat`, `bug-climb`, `car-race`) with papercraft titles, descriptions, theme colors, and banner gradients.
4. Final Release Audit: Execute `npm test`, `npm run build`, and `npm run audit-bundle` to confirm bundle budget < 200KB gzipped and 100% test suite pass.

Purpose: Complete the 2D Papercraft visual transformation across all 15 games in Milestone v5.0 and perform final release audit.
Output: Updated `WarpRenderer.ts`, `SpaceRacerScene.ts`, `BioArenaRenderer.ts`, `src/data/games.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@games/space-racer/src/WarpRenderer.ts
@games/space-racer/src/SpaceRacerScene.ts
@games/virus-defense/src/BioArenaRenderer.ts
@games/virus-defense/src/VirusDefenseScene.ts
@src/data/games.ts
@scripts/audit-bundle.js

<interfaces>
<!-- Papercraft Canvas Theme Tokens -->
const PAPERCRAFT = {
  bgParchment: '#F4EAD4',
  bgCard: '#FFFDF8',
  bgKraft: '#D8C3A5',
  bgCardboard: '#C5A880',
  bgDeepSpace: '#1E1B2E',
  ink: '#3E2723',
  inkLight: 'rgba(62, 39, 35, 0.4)',
  red: '#E11D48',
  blue: '#3B82F6',
  yellow: '#F59E0B',
  green: '#10B981',
  purple: '#8B5CF6',
  pink: '#EC4899',
  tape: 'rgba(255, 248, 220, 0.85)',
  tapeBorder: 'rgba(62, 39, 35, 0.2)',
  fontDisplay: '"Patrick Hand", cursive, sans-serif',
  fontBody: '"Comfortaa", cursive, sans-serif',
  fontSketch: '"Cabin Sketch", cursive, sans-serif',
};
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Space Racer Papercraft Visuals</name>
  <files>games/space-racer/src/WarpRenderer.ts, games/space-racer/src/SpaceRacerScene.ts</files>
  <action>
    Transform Space Racer from cyber-neon warp into a handmade retro-futuristic papercraft orbital racer.
    1. In `games/space-racer/src/WarpRenderer.ts`:
       - `renderBackground`: Render dark indigo construction paper gradient (`#1E1B2E` to `#2D2540`), papercut star stickers and stamped ink star dots with subtle parallax drift.
       - `renderTrack`: Render orbital runway as stitched/dashed ink perspective lane guides (`rgba(255, 248, 220, 0.4)` and `#F59E0B`) on kraft track bed with cardboard horizon boundary.
       - `renderObstacles`:
         - Asteroids: Render as textured cardboard cutouts (`#A67C52` / `#C5A880`) with irregular hand-cut polygonal edges, ink outlines (`#3E2723`, 2px), and layered paper crater indentations (`#8D5B34`).
         - Plasma Mines: Render as papercut spiky hazard discs in warning red/amber (`#E11D48`, `#F59E0B`) with ink outlines.
         - Turbo Boost Rings: Render as vibrant paper origami chevron gate rings (`#3B82F6`, `#8B5CF6`, `#F59E0B`) with paper tape accents and bright inner pass-through arrows.
       - `renderShip`: Render player ship as a folded origami / cardboard delta-wing starfighter (`#FFFDF8` white craft paper body with `#C5A880` cardboard wing stabilizers, `#3E2723` ink crease lines, paper canopy in `#3B82F6`, and layered construction paper thruster flame `#F59E0B` / `#E11D48`).
    2. In `games/space-racer/src/SpaceRacerScene.ts`:
       - `renderHUD`: Replace monospace font with `"Patrick Hand"` and `"Comfortaa"`. Render distance, speed, and score on taped craft paper cards, shield pips as paper shield stamps, and boost gauge as a paper tape ribbon.
       - `renderOverlays`: Render Ready, Paused, and GameOver screens as pinned cardboard cards with tape strips and tactile paper buttons.
  </action>
  <verify>
    <automated>npx vitest run games/space-racer</automated>
  </verify>
  <done>Space Racer renders with construction paper starfield, cardboard asteroids, origami boost gates, papercraft spaceship, and sticky note HUD; all space-racer tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Virus Defense Papercraft Visuals</name>
  <files>games/virus-defense/src/BioArenaRenderer.ts</files>
  <action>
    Transform Virus Defense from cyber-matrix bio-dish into a handmade papercraft cellular biology defense arena.
    1. In `games/virus-defense/src/BioArenaRenderer.ts`:
       - `renderBackground` & `renderMembraneGrid`: Render warm kraft parchment bio-arena desktop (`#F4EAD4` / `#E8DEC8`), concentric dashed ink circular guidelines (`rgba(62, 39, 35, 0.15)`), and faint stamped cellular paper organelles.
       - `renderNucleus`: Render center nucleus as a layered papercut circle (`#3B82F6` / `#10B981` based on HP) with paper membrane ripple folds, crisp `#3E2723` inked borders, and paper drop shadows (`4px 4px 0px rgba(62, 39, 35, 0.25)`).
       - `renderAntibodies`: Render antibodies as papercut medical cross badges in craft blue (`#3B82F6`) with white paper sparkles and ink outlines.
       - `renderPathogens`:
         - Spiker: Render as red papercut virus discs (`#E11D48`) with triangular paper spikes and ink outlines.
         - Speedster: Render as orange papercut capsule pods (`#F97316`) with crinkled paper flagella ribbons.
         - Splitter: Render as green construction paper dividing amoeba blobs (`#10B981`) with dual papercut nuclei.
         - Shield Carrier: Render as purple paper virus (`#8B5CF6`) with a cardboard crescent shield arc and ink borders.
       - `renderTurret` & `renderProjectiles`: Render cannon as a sturdy cardboard launcher (`#C5A880`) with a brass paper fastener pivot hub (`#F59E0B`), dashed ink laser targeting sight line, and paper pellet / ink blast projectiles (`#3B82F6`).
       - `renderHUD` & `renderOverlays`: Render wave, combo multiplier, score, and nucleus HP bar on taped craft paper cards using `"Patrick Hand"` and `"Comfortaa"`. Render Ready, Paused, and GameOver overlays on pinned cardboard cards.
  </action>
  <verify>
    <automated>npx vitest run games/virus-defense</automated>
  </verify>
  <done>Virus Defense renders with cardboard bio-arena petri dish, papercut nucleus, origami pathogen swarms, cardboard laser turret, and paper status placards; all virus-defense tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Complete Catalog Metadata Overhaul & Final Release Audit</name>
  <files>src/data/games.ts</files>
  <action>
    1. Update `src/data/games.ts` metadata for all remaining games to ensure complete catalog-wide papercraft consistency:
       - `space-racer`: Title "Space Racer", description mentioning high-speed papercraft orbital warp, weaving through cardboard asteroid fields, and hitting origami turbo boost gates, update themeColor to `#3B82F6`, and bannerBg to craft paper gradient (`linear-gradient(135deg, #F4EAD4 0%, #D8C3A5 100%)`).
       - `virus-defense`: Title "Virus Defense", description mentioning rotating cardboard laser turret to defend the central papercut cell nucleus from origami pathogen swarms, update themeColor to `#10B981`, and bannerBg to craft paper gradient (`linear-gradient(135deg, #F4EAD4 0%, #E8DEC8 100%)`).
       - `snake-eat`: Title "Storybook Snake", description mentioning guiding the papercut caterpillar across kraft paper grid to eat paper apples and golden honey fruit, update themeColor to `#10B981` / `#4A6D56`, and bannerBg to warm kraft gradient (`linear-gradient(135deg, #F4EAD4 0%, #D8C3A5 100%)`).
       - `bug-climb`: Title "Ladybug Climb", description mentioning tapping left or right to switch sides on the cardboard tree trunk and dodging cardboard branch obstacles, update themeColor to `#E11D48` / `#C85A32`, and bannerBg to warm kraft gradient.
       - `car-race`: Title "Vintage Speedway", description mentioning steering origami roadster across multi-lane kraft parchment highway, throttling speed, and drafting cardboard traffic, update themeColor to `#E11D48` / `#C85A32`, and bannerBg to warm kraft gradient.
    2. Final Release Audit:
       - Execute full test suite (`npm test`) — all 619+ tests must pass with 0 failures.
       - Execute production build (`npm run build`) — Vite static assets generated cleanly in `dist/`.
       - Execute bundle audit (`npm run audit-bundle`) — confirm all game bundles and total distribution are under the 200KB gzipped budget (CRAFT-09).
  </action>
  <verify>
    <automated>npm test && npm run build && npm run audit-bundle</automated>
  </verify>
  <done>src/data/games.ts completely updated for all 15 games; all 619+ tests pass, production build succeeds, and bundle size audit passes <200KB gzipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Build Output & Deployment Pipeline | Static assets emitted to dist/ and hosted on Cloudflare Pages |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Denial of Service | Production Bundle Size | mitigate | Enforce automated bundle audit (<200KB total gzipped) via `scripts/audit-bundle.js` |
| T-30-04 | Tampering | Canvas Coordinates & Transforms | mitigate | Balance `ctx.save()` / `ctx.restore()` across all canvas renders and bound all draw coordinates |
</threat_model>

<verification>
Execute full test suite, build, and bundle size audit:
`npm test && npm run build && npm run audit-bundle`
</verification>

<success_criteria>
1. All 5 complex games (snake-eat, bug-climb, car-race, space-racer, virus-defense) fully converted to 2D papercraft visual identity.
2. Complete catalog metadata in `src/data/games.ts` reflects papercraft theme for all 15 games.
3. 100% of unit tests pass cleanly.
4. Vite build succeeds with bundle size under 200KB gzipped.
</success_criteria>

<output>
After completion, create `.planning/phases/30-complex-renderers-and-audit/30-02-SUMMARY.md`
</output>
