---
phase: 30-complex-renderers-and-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - games/snake-eat/src/SnakeRenderer.ts
  - games/snake-eat/src/SnakeEatScene.ts
  - games/bug-climb/src/TreeRenderer.ts
  - games/car-race/src/HighwayRenderer.ts
  - games/car-race/src/CarRaceScene.ts
autonomous: true
requirements:
  - CRAFT-07
must_haves:
  truths:
    - "Snake Eat renders with kraft paper grid, papercut caterpillar segments with ink outlines, paper apple food, and golden honeycomb bonus"
    - "Bug Climb renders with cardboard tree trunk, papercut ladybug with ink spots, cardboard branch obstacles with tape joints, and paper timer ribbon"
    - "Car Race renders with origami classic roadster, kraft parchment highway with taped lane markers, cardboard traffic vehicles, and paper HUD dashboard"
    - "All unit tests for snake-eat, bug-climb, and car-race pass cleanly"
  artifacts:
    - path: "games/snake-eat/src/SnakeRenderer.ts"
      provides: "Kraft paper grid, papercut caterpillar segments, paper apple and golden honeycomb food, sticky note HUD and overlays"
      contains: "Patrick Hand"
    - path: "games/bug-climb/src/TreeRenderer.ts"
      provides: "Cardboard tree trunk with bark grain, papercut ladybug, cardboard branches with tape joints, paper timer ribbon, taped status cards"
      contains: "Patrick Hand"
    - path: "games/car-race/src/HighwayRenderer.ts"
      provides: "Kraft parchment highway, taped lane markers, origami roadster, cardboard traffic vehicles, paper speedometer and HUD"
      contains: "Patrick Hand"
  key_links:
    - from: "games/snake-eat/src/SnakeEatScene.ts"
      to: "games/snake-eat/src/SnakeRenderer.ts"
      via: "SnakeRenderer render pipeline"
      pattern: "this\\.renderer\\.render"
    - from: "games/bug-climb/src/BugClimbScene.ts"
      to: "games/bug-climb/src/TreeRenderer.ts"
      via: "TreeRenderer render pipeline"
      pattern: "this\\.renderer\\.render"
    - from: "games/car-race/src/CarRaceScene.ts"
      to: "games/car-race/src/HighwayRenderer.ts"
      via: "HighwayRenderer renderRoad, renderPlayer, renderTraffic"
      pattern: "this\\.renderer\\.render"
---

<objective>
Convert canvas renderers and presentation layers for 3 complex games (`snake-eat`, `bug-climb`, `car-race`) to the 2D Papercraft aesthetic.
1. `snake-eat`: Render warm kraft paper background (`#F4EAD4`), inked grid matrix lines (`rgba(62, 39, 35, 0.12)`), papercut caterpillar segments with sage/green construction paper (`#4A6D56`, `#10B981`) and layered paper drop shadows (`rgba(62, 39, 35, 0.2)`), red paper apple with green leaf cutout, golden honeycomb bonus orb with countdown ribbon ring, and taped sticky-note HUD and dialog overlays with `"Patrick Hand"` and `"Comfortaa"` typography.
2. `bug-climb`: Render corrugated cardboard tree trunk (`#C5A880` / `#A67C52`) with inked bark grooves and grain, papercut ladybug (`#E11D48` / `#C85A32`) with distinct ink spots and articulated legs, cardboard branch obstacle limbs (`#8D5B34`) with paper tape joints (`rgba(255, 248, 220, 0.85)`) and leaf tufts, urgent paper countdown ribbon, and taped cardboard score cards.
3. `car-race`: Render kraft parchment highway roadbed (`#F4EAD4` / `#E8DEC8`) with taped lane divider markers (`#F59E0B` / paper tape `rgba(255, 248, 220, 0.85)`), origami/folded paper classic roadster (`#E11D48` / `#C85A32`) with inked creases and cardboard wheels, cardboard traffic trucks, police cars, and sedans, paper dashboard speedometer placard, and sticky-note overlays.

Purpose: Deliver tactile 2D papercraft in-game visuals for the first 3 complex games in Milestone v5.0.
Output: Updated `SnakeRenderer.ts`, `SnakeEatScene.ts`, `TreeRenderer.ts`, `HighwayRenderer.ts`, and `CarRaceScene.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@games/snake-eat/src/SnakeRenderer.ts
@games/snake-eat/src/SnakeEatScene.ts
@games/bug-climb/src/TreeRenderer.ts
@games/bug-climb/src/BugClimbScene.ts
@games/car-race/src/HighwayRenderer.ts
@games/car-race/src/CarRaceScene.ts

<interfaces>
<!-- Papercraft Canvas Theme Tokens -->
const PAPERCRAFT = {
  bgParchment: '#F4EAD4',
  bgCard: '#FFFDF8',
  bgKraft: '#D8C3A5',
  bgCardboard: '#C5A880',
  woodTrunk: '#A67C52',
  woodBranch: '#8D5B34',
  ink: '#3E2723',
  inkLight: 'rgba(62, 39, 35, 0.4)',
  red: '#E11D48',
  blue: '#3B82F6',
  yellow: '#F59E0B',
  green: '#10B981',
  purple: '#8B5CF6',
  pink: '#EC4899',
  tape: 'rgba(255, 248, 220, 0.85)',
  tapeBorder: 'rgba(62, 39, 35, 0.2)',
  fontDisplay: '"Patrick Hand", cursive, sans-serif',
  fontBody: '"Comfortaa", cursive, sans-serif',
  fontSketch: '"Cabin Sketch", cursive, sans-serif',
};
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snake Eat Papercraft Visuals</name>
  <files>games/snake-eat/src/SnakeRenderer.ts, games/snake-eat/src/SnakeEatScene.ts</files>
  <action>
    Transform Snake Eat into a handmade storybook papercraft caterpillar game.
    1. In `games/snake-eat/src/SnakeRenderer.ts`:
       - `renderBackground` and `renderGrid`: Render kraft parchment background (`#F4EAD4`), dashed/stitched grid matrix lines (`rgba(62, 39, 35, 0.1)`), stamped paper corner accents, and thick inked outer border (`#3E2723`, 3px).
       - `renderSnake`: Render caterpillar body segments with alternating construction greens (`#4A6D56`, `#588066`, `#10B981`), inked borders (`#3E2723`, 2px), layered paper drop shadow (`2px 2px 0px rgba(62, 39, 35, 0.25)`), and papercut head with hand-drawn eyes and paper antennae.
       - `renderFood`: Render regular food as a papercut red apple (`#E11D48`) with green leaf paper cutout and ink outline; render bonus food as a golden honeycomb / starfruit disc (`#F59E0B`) with countdown timer ring and paper sparkle shine.
       - `renderHUD`: Replace top bar with taped sticky-note score tag, combo multiplier banner, and best score tag in `"Patrick Hand"` and `"Comfortaa"`.
       - `renderOverlays`: Render Ready, Paused, and GameOver overlays as pinned/taped craft paper modal cards with drop shadows and papercut action buttons.
    2. In `games/snake-eat/src/SnakeEatScene.ts`:
       - Verify particle bursts emit paper shreds / confetti chips (`#E11D48`, `#F59E0B`, `#10B981`) on food consumption and crash.
  </action>
  <verify>
    <automated>npx vitest run games/snake-eat</automated>
  </verify>
  <done>Snake Eat renders with kraft grid, papercut caterpillar segments, paper apple and honeycomb bonus, sticky note HUD and overlays; all snake-eat tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Bug Climb Papercraft Visuals</name>
  <files>games/bug-climb/src/TreeRenderer.ts</files>
  <action>
    Transform Bug Climb into a vertical cardboard tree climb with papercut ladybug.
    1. In `games/bug-climb/src/TreeRenderer.ts`:
       - `renderBackground`: Render parchment background (`#F4EAD4`) with faint silhouette papercut trees in background (`rgba(74, 109, 86, 0.12)`).
       - `renderTrunk`: Render central tree trunk as textured corrugated cardboard (`#C5A880` / `#A67C52`) with vertical bark grain lines, segment crosscuts, and crisp inked borders (`#3E2723`, 3px).
       - `renderBranches`: Render branch obstacle limbs as cardboard planks (`#8D5B34`) reinforced with paper tape strips (`rgba(255, 248, 220, 0.85)` with dashed ink edges) at the trunk joint, topped with papercut leaf clusters (`#4A6D56`, `#10B981`).
       - `renderBug`: Render ladybug hero as a vibrant red paper cutout (`#E11D48` / `#C85A32`) with distinct inked spots (`#3E2723`), center shell crease, paper head with craft eyes, and scurry-animated paper legs.
       - `renderHUD`: Render altitude counter, score, and combo multiplier on taped paper tags using `"Patrick Hand"` and `"Comfortaa"`. Render countdown timer as a colorful paper ribbon gauge that pulses with paper red tape (`#E11D48`) when urgent.
       - `renderOverlays`: Render start controls, pause, and game over screens on cardboard cards with taped edges and craft typography.
  </action>
  <verify>
    <automated>npx vitest run games/bug-climb</automated>
  </verify>
  <done>Bug Climb renders with cardboard tree trunk, papercut ladybug with ink spots, cardboard branch obstacles with tape joints, and paper timer ribbon; all bug-climb tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Car Race Papercraft Visuals</name>
  <files>games/car-race/src/HighwayRenderer.ts, games/car-race/src/CarRaceScene.ts</files>
  <action>
    Transform Car Race into a vintage papercraft highway racing adventure.
    1. In `games/car-race/src/HighwayRenderer.ts`:
       - `renderRoad`: Render kraft parchment highway (`#F4EAD4` roadside, `#E8DEC8` roadbed) with scrolling taped lane divider dashes (`#F59E0B` / `rgba(255, 248, 220, 0.85)`) and inked road shoulder borders (`#3E2723`, 3px).
       - `renderPlayer`: Render player roadster as an origami/folded paper classic sports car (`#E11D48` / `#C85A32`) with ink crease lines, paper windshield (`#FFFDF8`), cardboard wheels (`#3E2723`), and paper brake lights (`#F59E0B`).
       - `renderTraffic`: Render heavy trucks in cardboard ochre (`#F59E0B`), police cars in paper navy (`#2C3E50`) with flashing paper siren bars, and sedans in construction green/blue (`#4A6D56`, `#3B82F6`) with layered drop shadows (`3px 3px 0px rgba(62, 39, 35, 0.25)`).
    2. In `games/car-race/src/CarRaceScene.ts`:
       - Replace top HUD with taped craft paper dashboard placard containing speedometer, distance meter, and score in `"Patrick Hand"` and `"Comfortaa"`.
       - Render Ready, Paused, and GameOver overlays as pinned cardboard placards with tape strips and tactile restart buttons.
  </action>
  <verify>
    <automated>npx vitest run games/car-race</automated>
  </verify>
  <done>Car Race renders with origami roadster, kraft parchment highway with taped lane markers, cardboard traffic vehicles, and paper HUD dashboard; all car-race tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Canvas 2D Transform & Input | User pointer and keyboard events driving canvas rendering and physics step |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | Canvas Rendering Context | mitigate | Enforce balanced `ctx.save()` / `ctx.restore()` across all custom canvas render calls; keep all path coordinates bounded |
| T-30-02 | Denial of Service | Particle & Geometry Calculations | mitigate | Clamp particle generation limits and prevent unconstrained allocations inside the render loop |
</threat_model>

<verification>
Automated unit tests for snake-eat, bug-climb, and car-race pass:
`npx vitest run games/snake-eat games/bug-climb games/car-race`
</verification>

<success_criteria>
All 3 complex games (snake-eat, bug-climb, car-race) render with complete 2D papercraft visual identity (kraft/cardboard backgrounds, papercut vehicles/creatures, ink borders, craft typography, and sticky-note overlays).
</success_criteria>

<output>
After completion, create `.planning/phases/30-complex-renderers-and-audit/30-01-SUMMARY.md`
</output>
