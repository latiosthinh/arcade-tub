---
phase: 19-2048-neon
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - games/game-2048/src/TileRenderer.ts
  - games/game-2048/src/Audio2048.ts
  - games/game-2048/src/Particles.ts
  - games/game-2048/src/Game2048Scene.ts
  - games/game-2048/src/main.ts
  - games/game-2048/index.html
  - vite.config.ts
  - games/game-2048/test/particles.test.ts
autonomous: true
requirements:
  - G2048-02

must_haves:
  truths:
    - "TileRenderer renders dark cybernetic background, rounded neon cell slots, and color-tiered glowing number tiles (2: Cyan, 4: Mint, 8: Yellow, 16: Orange, 32: Coral, 64: Crimson, 128: Magenta, 256: Purple, 512: Indigo, 1024: Gold, 2048: Rainbow Ultra Glow)"
    - "Tile movement smoothly interpolates between source and destination grid positions using slide animations, newly spawned tiles scale up from 0 to 1, and merged tiles execute a pop scale burst (1.0 -> 1.25 -> 1.0)"
    - "Particles system emits neon sparkle bursts when tiles merge with color matched to the resulting merged tile"
    - "Audio2048 synthesizer provides procedural Web Audio effects (slide whoosh, merge pop chord with ascending pitch proportional to tile tier, 2048 victory fanfare, gameover chime) with zero external assets"
    - "Game2048Scene handles touch swipe gesture tracking (swipe angle & threshold detection), keyboard arrow & WASD inputs, undo button clicks, restart, and state transitions"
    - "Vite multi-page rollup config includes games/game-2048/index.html for development and production builds"
  artifacts:
    - path: "games/game-2048/src/TileRenderer.ts"
      provides: "Color-tiered neon tile visuals, slide interpolation, pop scale animations, spawn zoom, HUD score banners, and overlay modals"
      exports: ["TileRenderer", "AnimatedTile", "TILE_THEMES"]
    - path: "games/game-2048/src/Audio2048.ts"
      provides: "Procedural Web Audio sound effects for sliding, tier-scaled merge chords, win fanfare, and gameover"
      exports: ["Audio2048"]
    - path: "games/game-2048/src/Particles.ts"
      provides: "Particle engine for neon merge sparkles and tile pop bursts"
      exports: ["ParticleSystem", "Particle"]
    - path: "games/game-2048/src/Game2048Scene.ts"
      provides: "GameScene implementation integrating Grid2048, GameState, TileRenderer, Audio2048, ParticleSystem, swipe & keyboard input"
      exports: ["Game2048Scene"]
    - path: "games/game-2048/src/main.ts"
      provides: "Entry point mounting Game2048Scene to GameLoop and Playables adapter"
    - path: "games/game-2048/index.html"
      provides: "Standalone canvas HTML page for 2048 Neon"
  key_links:
    - from: "games/game-2048/src/Game2048Scene.ts"
      to: "games/game-2048/src/Grid2048.ts"
      via: "dispatching directional slide and undo requests"
      pattern: "grid\\.slide"
    - from: "games/game-2048/src/Game2048Scene.ts"
      to: "games/game-2048/src/TileRenderer.ts"
      via: "triggering slide and pop merge animations from SlideResult events"
      pattern: "renderer\\.(animateSlide|render)"
    - from: "games/game-2048/src/Game2048Scene.ts"
      to: "games/game-2048/src/Audio2048.ts"
      via: "playing audio effects on slide, merge, win, and gameover"
      pattern: "audio\\.(playSlide|playMerge|playWin|playGameOver)"
    - from: "vite.config.ts"
      to: "games/game-2048/index.html"
      via: "rollupOptions input configuration"
      pattern: "'game-2048': resolve\\(__dirname, 'games/game-2048/index\\.html'\\)"
---

<objective>
Build HTML5 Canvas rendering for 2048 Neon with color-tiered neon tile visuals (2 to 2048+), smooth slide interpolation, spawn zoom, merge pop scale animations, particle sparkles, procedural Web Audio synthesizer (`Audio2048`), touch swipe gesture recognition + keyboard controls, `Game2048Scene` orchestrator, standalone `index.html`, and Vite rollup packaging (G2048-02).

Purpose: Deliver an ultra-responsive, vibrant cyber-arcade puzzle experience with tactile swipe gestures, escalating melodic merge feedback, dazzling neon glows, and seamless Playables integration.
Output: `TileRenderer.ts`, `Audio2048.ts`, `Particles.ts`, `Game2048Scene.ts`, `main.ts`, `index.html`, `vite.config.ts`, and particle unit test.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@packages/game-engine/src/index.ts
@packages/playables-adapter/src/index.ts
@games/game-2048/src/Grid2048.ts
@games/game-2048/src/GameState.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ParticleSystem and Audio2048 procedural synthesizer with tests</name>
  <files>
    games/game-2048/src/Particles.ts,
    games/game-2048/src/Audio2048.ts,
    games/game-2048/test/particles.test.ts
  </files>
  <behavior>
    - ParticleSystem:
      - Particle properties: `x`, `y`, `vx`, `vy`, `radius`, `color`, `alpha`, `life`, `maxLife`, `shape` (`'sparkle'` | `'ring'`).
      - Methods:
        - `emitMergeSparkles(x: number, y: number, color: string, count: number)`: Emits radial burst of sparkling neon particles with velocity damping and alpha fade.
        - `emitWinConfetti(width: number, height: number, count: number)`: Emits rainbow celebratory particle showers across the board.
        - `update(dt: number)`: Updates particle positions, drag, and alpha lifespans; culls dead particles.
        - `render(ctx: CanvasRenderingContext2D)`: Renders glowing particles with canvas blending.
    - Audio2048:
      - Procedural Web Audio synthesis using AudioContext/AudioSynthesizer without external MP3/WAV assets:
        - `playSlide()`: Crisp short frequency-modulated whoosh (`220Hz -> 110Hz` filtered sweep).
        - `playMerge(value: number)`: Harmonic 2-note pop chord where root pitch scales logarithmically with tile tier:
          - 2/4: `C5` (523Hz)
          - 8/16: `E5` (659Hz)
          - 32/64: `G5` (784Hz)
          - 128/256: `C6` (1046Hz)
          - 512/1024: `E6` (1318Hz)
          - 2048+: `G6 -> C7` resonant chord
        - `playWin()`: Victorious multi-tone arpeggio fanfare (`C5 -> E5 -> G5 -> C6 -> E6 -> G6`).
        - `playGameOver()`: Descending minor dissonance chord.
        - `playUndo()`: Quick reverse chime sweep.
  </behavior>
  <action>
    1. Implement `Particles.ts`:
       - `export interface Particle`
       - `export class ParticleSystem` managing merge sparkles and victory particle effects.
    2. Implement `Audio2048.ts`:
       - `export class Audio2048` generating dynamic sound effects with safe audio context unlocking and pitch tier scaling.
    3. Write unit tests in `games/game-2048/test/particles.test.ts` testing:
       - Particle emission quantity, speed distributions, and color assignments.
       - Kinematic updates and lifespan decay.
       - Expiration removal and array bounding.
  </action>
  <verify>
    <automated>npx vitest run games/game-2048/test/particles.test.ts</automated>
  </verify>
  <done>Particles and Audio2048 synthesizer pass all tests and produce procedural effects.</done>
</task>

<task type="auto">
  <name>Task 2: Implement TileRenderer, Game2048Scene, standalone HTML, and Vite build integration</name>
  <files>
    games/game-2048/src/TileRenderer.ts,
    games/game-2048/src/Game2048Scene.ts,
    games/game-2048/src/main.ts,
    games/game-2048/index.html,
    vite.config.ts
  </files>
  <action>
    1. Implement `TileRenderer.ts`:
       - Define color-tiered neon themes (`TILE_THEMES`):
         - 2: `#00f0ff` (Cyan)
         - 4: `#00ffa3` (Mint)
         - 8: `#ffe600` (Yellow)
         - 16: `#ff9900` (Orange)
         - 32: `#ff4d4d` (Coral)
         - 64: `#ff0055` (Crimson)
         - 128: `#ff00a0` (Magenta)
         - 256: `#b537f2` (Purple)
         - 512: `#6a0dad` (Indigo)
         - 1024: `#ffd700` (Gold glow)
         - 2048: `#ffffff` with chromatic rainbow pulsing glow (`#ff0055`, `#00f0ff`, `#ffe600`)
         - >2048: Deep cosmic obsidian with neon white text & dual outer halos
       - `AnimatedTile` class tracking current render coordinates `(x, y)`, scale factor, target grid position, slide animation timer (e.g. 100ms duration), and merge pop pulse `(1.0 -> 1.25 -> 1.0)`.
       - Render routines:
         - Background: Deep cyber carbon grid (`#0a0e17`) with rounded backdrop tile slots (`#151c2e`).
         - Tiles: Rounded rectangular cards with subtle border glow, vibrant gradient fills, drop shadows, and high-contrast bold typography scaled to fit 1 to 5 digits.
         - Top Header / HUD: Title logo "2048 NEON", Score & High Score glassmorphic counters, Undo button with remaining count badge, Restart button.
         - Overlays: Ready modal ("SWIPE OR USE ARROWS TO PLAY"), 2048 Victory fanfare screen ("YOU REACHED 2048! [CONTINUE] [RESTART]"), Game Over screen ("NO MORE MOVES! [TRY AGAIN] [UNDO]").
    2. Implement `Game2048Scene.ts`:
       - Implements `GameScene` (`update(dt)`, `render(ctx)`).
       - Initializes `Grid2048`, `GameState`, `TileRenderer`, `Audio2048`, `ParticleSystem`, and `InputManager`.
       - Touch & Gesture Control:
         - Pointer down/up tracking to compute swipe vectors `(dx, dy)`.
         - Swipe detection: If magnitude > 25px, checks angle to determine primary direction (`up`, `down`, `left`, `right`).
       - Keyboard Input: Arrow keys (`ArrowUp`, `ArrowDown`, `ArrowLeft`, `ArrowRight`) and WASD (`KeyW`, `KeyS`, `KeyA`, `KeyD`) trigger directional moves; `KeyU` / `Backspace` triggers undo; `KeyR` triggers restart.
       - Button click hits: Tests pointer clicks against HUD Undo, Restart, and modal Continue/Restart buttons.
       - Animation Dispatch: On `grid.slide()` result, triggers slide animations in `TileRenderer`, emits merge sparkles in `ParticleSystem`, and plays corresponding `Audio2048` chord.
    3. Create `games/game-2048/src/main.ts`:
       - Initializes Playables adapter (`initPlayables`, `onPause`, `onResume`).
       - Creates canvas, `GameLoop`, and mounts `Game2048Scene`.
    4. Create `games/game-2048/index.html`:
       - Responsive HTML5 canvas container matching Arcade Carnival standard shell.
    5. Update `vite.config.ts`:
       - Add `'game-2048': resolve(__dirname, 'games/game-2048/index.html')` under `build.rollupOptions.input`.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>2048 Neon canvas rendering, swipe gesture controls, animated tiles, and Vite build entry complete and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Swipe/Pointer Input → Game2048Scene | Gesture vector calculations triggering directional slide |
| Game2048Scene → Audio2048 | Audio synthesis node initialization and pitch trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Denial of Service | TileRenderer.ts | mitigate | Cull and recycle completed animation interpolations so active tile object count strictly matches board state |
| T-19-05 | Denial of Service | Audio2048.ts | mitigate | Clean up completed audio oscillator nodes immediately after envelope decay to avoid browser audio node exhaustion |
| T-19-06 | Denial of Service | Particles.ts | mitigate | Cap maximum active merge sparkles (max 150) for smooth 60fps canvas performance on mobile devices |
</threat_model>

<verification>
Run workspace test suite to verify full integration:
`npx vitest run`
</verification>

<success_criteria>
- Color-tiered neon tiles render with glows and clean typography from 2 to 2048+.
- Tile sliding, pop merging, and spawn scaling animate smoothly.
- Swipe gestures and keyboard controls respond crisply in all 4 directions.
- Procedural audio chords scale harmonically with tile merge tier.
- Standalone HTML page and Vite build entry configured.
</success_criteria>

<output>
After completion, create `.planning/phases/19-2048-neon/19-02-SUMMARY.md`
</output>
