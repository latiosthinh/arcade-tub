---
phase: 41-square-bird-timed-blocks-infinity
plan: 02
type: execute
wave: 2
depends_on:
  - 41-01
files_modified:
  - games/square-bird/src/BirdRenderer.ts
  - games/square-bird/src/BirdParticles.ts
  - games/square-bird/src/BirdAudio.ts
  - games/square-bird/src/SquareBirdScene.ts
autonomous: true
requirements:
  - REQ-617
  - REQ-618

must_haves:
  truths:
    - "Egg blocks render visual decay indicators (cracking origami crease lines and pulsation/flash when timer < 1.0s) as they near expiration"
    - "When egg block expires, BirdParticles emits a paper crumble puff and BirdAudio plays a soft paper flutter sound"
    - "Ready screen allows selecting between '[1] LEVELS' and '[2] INFINITE MODE' via on-screen buttons or 1/2 keyboard shortcuts"
    - "HUD displays active game mode, current score, distance/progress, fever gauge, and mode-specific high score"
  artifacts:
    - path: "games/square-bird/src/BirdRenderer.ts"
      provides: "Decay crack rendering on timed blocks, mode-specific HUD and placards"
      contains: "lifeTime"
    - path: "games/square-bird/src/BirdParticles.ts"
      provides: "Egg block expiration crumble particle effect"
      contains: "emitEggExpire"
    - path: "games/square-bird/src/BirdAudio.ts"
      provides: "Audio cue for egg block expiration"
      contains: "playEggExpire"
    - path: "games/square-bird/src/SquareBirdScene.ts"
      provides: "Mode selection UI, touch/keyboard input handling for mode switching, and expiration event wiring"
      contains: "startMode"
  key_links:
    - from: "games/square-bird/src/SquareBirdScene.ts"
      to: "games/square-bird/src/GameState.ts"
      via: "invokes startMode('levels' | 'infinite') based on user choice"
      pattern: "startMode"
    - from: "games/square-bird/src/SquareBirdScene.ts"
      to: "games/square-bird/src/BirdParticles.ts"
      via: "emits crumble particles when onEggExpire is triggered"
      pattern: "emitEggExpire"
---

<objective>
Update `BirdRenderer`, `BirdParticles`, `BirdAudio`, and `SquareBirdScene` to visually depict egg block timer decay (crack lines & pulse), emit crumble particle/audio effects on block expiration, and provide an interactive mode selector (Levels vs Infinite Mode) with updated HUD.

Purpose: Deliver tactile visual/audio feedback for timed block expiration and full UI/UX controls for both normal and infinite gameplay.
Output: Updated `BirdRenderer.ts`, `BirdParticles.ts`, `BirdAudio.ts`, and `SquareBirdScene.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-square-bird-timed-blocks-infinity/41-01-PLAN.md
@games/square-bird/src/BirdPhysics.ts
@games/square-bird/src/GameState.ts
@games/square-bird/src/BirdRenderer.ts
@games/square-bird/src/BirdParticles.ts
@games/square-bird/src/BirdAudio.ts
@games/square-bird/src/SquareBirdScene.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add egg decay visual effects, expiration particles, and audio cues</name>
  <files>
    games/square-bird/src/BirdParticles.ts,
    games/square-bird/src/BirdAudio.ts,
    games/square-bird/src/BirdRenderer.ts
  </files>
  <action>
    1. In `BirdParticles.ts`:
       - Add `emitEggExpire(x: number, y: number, size: number): void`:
         - Spawns 8-10 tiny square kraft paper specks that disperse outward and fall with gravity, fading over 0.4s.
    2. In `BirdAudio.ts`:
       - Add `playEggExpire(): void`:
         - Synthesizes a subtle, crisp white noise paper crumble click via Web Audio (filter envelope 800Hz -> 200Hz, 60ms duration).
    3. In `BirdRenderer.ts`:
       - Update `drawEggStack`:
         - Read `egg.lifeTime` and `egg.maxLifeTime`.
         - When `egg.lifeTime < 1.0`s (or ratio < 0.33):
           - Render origami crack lines across the block (dashed or jagged lines).
           - Add subtle opacity or color pulsation (flashing warm amber/white) indicating imminent expiration.
         - Render a small subtle countdown bar or circular notch at the block top edge if helpful.
  </action>
  <verify>
    <automated>npx vitest run games/square-bird/test/SquareBird.test.ts</automated>
  </verify>
  <done>Egg blocks show dynamic visual wear/cracking as timer elapses, and crumble particles/audio cues are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Update HUD, Ready Overlay Mode Selector, and Input Handlers in SquareBirdScene</name>
  <files>
    games/square-bird/src/BirdRenderer.ts,
    games/square-bird/src/SquareBirdScene.ts
  </files>
  <action>
    1. In `BirdRenderer.ts`:
       - Update `drawHUD`:
         - Display active mode badge: `[LEVELS Stage X]` or `[INFINITE SURVIVAL]`.
         - In levels mode: show stage progress bar to finish line.
         - In infinite mode: show distance counter `DIST: {Math.floor(distanceTraveled)}m` and `BEST: {infiniteHighScore}`.
       - Update overlay rendering:
         - Draw Ready placard with 2 distinct interactive mode buttons:
           - Button 1: "[1] STAGE LEVELS" (Clear progressive obstacle courses)
           - Button 2: "[2] INFINITE MODE" (Endless procedural survival)
           - Space to start active selected mode.
         - Game Over placard: displays final score, distance traveled, and indicates whether a new personal best was set.
    2. In `SquareBirdScene.ts`:
       - Wire `state.bird.onEggExpire` and `state.onEggExpire` to trigger `this.particles.emitEggExpire` and `this.audio.playEggExpire`.
       - Update input handling (`pointerdown` / click coordinates & keyboard `keydown`):
         - Digit 1 / Key L: Select & start 'levels' mode.
         - Digit 2 / Key I: Select & start 'infinite' mode.
         - Space / ArrowUp: Lay egg while playing; start selected mode while on ready screen; restart on gameover screen.
         - Canvas touch/click hit-testing on ready overlay mode buttons to launch chosen mode.
  </action>
  <verify>
    <automated>npx vitest run games/square-bird/test/SquareBird.test.ts</automated>
  </verify>
  <done>SquareBirdScene provides full interactive mode switching, decay feedback, and responsive HUD overlays.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Click / Key Event → Scene Action | Mode selection and restart action execution |
| Web Audio API Context → Speaker Output | Audio synthesizer state management |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04 | Denial of Service | BirdParticles.ts | mitigate | Cap particle array length to 100 max to prevent frame drops during rapid egg expiration bursts |
| T-41-05 | Tampering | SquareBirdScene.ts | mitigate | Validate mode string argument to only allow `'levels' | 'infinite'`, preventing invalid state transitions |
</threat_model>

<verification>
Automated vitest suite passes for all Square Bird tests:
`npx vitest run games/square-bird/test/SquareBird.test.ts`
</verification>

<success_criteria>
- Egg blocks visibly crack and pulse before expiring.
- Block expiration produces crumble paper particles and audio clicks.
- Ready menu supports one-tap / key selection for Levels and Infinite Mode.
- HUD clearly distinguishes mode and tracks infinite high score.
</success_criteria>

<output>
After completion, create `.planning/phases/41-square-bird-timed-blocks-infinity/41-02-SUMMARY.md`
</output>
