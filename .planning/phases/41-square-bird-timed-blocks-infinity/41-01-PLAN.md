---
phase: 41-square-bird-timed-blocks-infinity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - games/square-bird/src/BirdPhysics.ts
  - games/square-bird/src/ObstacleGenerator.ts
  - games/square-bird/src/GameState.ts
  - games/square-bird/test/SquareBird.test.ts
autonomous: true
requirements:
  - REQ-617
  - REQ-618

must_haves:
  truths:
    - "Egg blocks possess a timed lifespan (3.0s default) and automatically expire when timer reaches zero"
    - "When an egg block expires, it is removed from the egg stack and the bird/stack above falls naturally to the ground or obstacle platform"
    - "ObstacleGenerator supports infinite procedural generation streaming obstacles ahead of camera position"
    - "GameState supports both 'levels' and 'infinite' game modes, tracking high score in localStorage via playables adapter"
    - "100% unit tests pass across timed egg expiration, dynamic obstacle streaming, and infinite mode state machine"
  artifacts:
    - path: "games/square-bird/src/BirdPhysics.ts"
      provides: "Egg block lifetime tracking, tick decay, and expiration removal"
      contains: "lifeTime"
    - path: "games/square-bird/src/ObstacleGenerator.ts"
      provides: "Dynamic obstacle chunk and streaming generation ahead of player"
      exports: ["ObstacleGenerator", "Obstacle"]
    - path: "games/square-bird/src/GameState.ts"
      provides: "Game mode switching, infinite runner loop, and storage persistence"
      contains: "mode"
  key_links:
    - from: "games/square-bird/src/GameState.ts"
      to: "games/square-bird/src/BirdPhysics.ts"
      via: "bird.update(dt, groundY) decays egg block timers and resolves falls"
      pattern: "bird\\.update"
    - from: "games/square-bird/src/GameState.ts"
      to: "@arcade-carnival/playables-adapter"
      via: "saveData and loadData for infinite mode high score"
      pattern: "(saveData|loadData)"
---

<objective>
Implement egg block decay lifetime system in `BirdPhysics`, endless procedural obstacle streaming in `ObstacleGenerator`, and dual game mode support ('levels' | 'infinite') with high score persistence in `GameState`, validated by 100% passing unit tests.

Purpose: Allow egg blocks to only appear temporarily before crumbling away, and enable an infinite survival runner mode for Square Bird.
Output: Updated `BirdPhysics.ts`, `ObstacleGenerator.ts`, `GameState.ts`, and unit test suite in `SquareBird.test.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@games/square-bird/src/BirdPhysics.ts
@games/square-bird/src/ObstacleGenerator.ts
@games/square-bird/src/GameState.ts
@games/square-bird/test/SquareBird.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add timed lifespan and expiration physics to EggBlock in BirdPhysics</name>
  <files>
    games/square-bird/src/BirdPhysics.ts,
    games/square-bird/test/SquareBird.test.ts
  </files>
  <behavior>
    - EggBlock interface:
      - `id`: number
      - `y`: number (top Y position)
      - `size`: number (default 36)
      - `lifeTime`: number (seconds remaining, default 3.0s)
      - `maxLifeTime`: number (initial seconds, default 3.0s)
    - Egg decay & expiration:
      - `layEgg(customDuration?: number)`: sets `lifeTime = customDuration ?? 3.0`, `maxLifeTime = lifeTime`.
      - `update(dt: number, groundY: number)`:
        - Decrements `lifeTime` for each egg in `this.eggs` by `dt`.
        - Identifies and removes expired eggs (`lifeTime <= 0`), returning list of expired eggs or invoking `onEggExpire(egg)` callback if registered.
        - When an egg expires from middle or bottom, shifts all eggs above and bird downward, or allows standard gravity/ground resolution to pull bird and remaining stack to proper ground level.
    - Unit tests:
      - Initial egg block creation has `lifeTime = 3.0` and `maxLifeTime = 3.0`.
      - Timers decay proportionally with `dt`.
      - Expired eggs are removed from `bird.eggs` after 3.0 seconds.
      - Bird and remaining egg stack fall down to ground level when egg expires.
  </behavior>
  <action>
    1. Update `EggBlock` interface in `games/square-bird/src/BirdPhysics.ts` to include `lifeTime: number` and `maxLifeTime: number`.
    2. In `BirdPhysics.layEgg()`, initialize `lifeTime` and `maxLifeTime` to 3.0 seconds (or configurable `eggDuration` from `BirdConfig`).
    3. Add `public onEggExpire?: (egg: EggBlock) => void;` callback to `BirdPhysics`.
    4. In `BirdPhysics.update(dt, groundY)`:
       - Iterate over `this.eggs` and decrement `lifeTime -= dt`.
       - Filter out expired eggs (`lifeTime <= 0`), emit `onEggExpire(egg)` for each.
       - Re-align remaining eggs below bird or resolve vertical physics against `groundY`.
    5. Update `games/square-bird/test/SquareBird.test.ts` with tests for egg lifetime decrement, expiration event callback, and post-expiration gravity settling.
  </action>
  <verify>
    <automated>npx vitest run games/square-bird/test/SquareBird.test.ts</automated>
  </verify>
  <done>Egg blocks decay over time and cleanly expire with physics settling, verified by unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dynamic obstacle streaming in ObstacleGenerator and Infinite Mode in GameState</name>
  <files>
    games/square-bird/src/ObstacleGenerator.ts,
    games/square-bird/src/GameState.ts,
    games/square-bird/test/SquareBird.test.ts
  </files>
  <behavior>
    - ObstacleGenerator:
      - Supports `generateAhead(currentX: number, viewDistance?: number)`:
        - Keeps track of `furthestGeneratedX`.
        - Spawns obstacles procedurally ahead of player (up to `currentX + 1800` px) with increasing difficulty as `currentX` increases (higher stacks up to 4 blocks, variable gaps 200–380px).
        - Supports `cullBehind(camX: number)`: removes obstacles where `obs.x + obs.width < camX - 300`.
    - GameState:
      - `mode: 'levels' | 'infinite'` (default `'levels'`).
      - `infiniteHighScore: number` loaded/saved from `square-bird-infinite-highscore` via `loadData` / `saveData`.
      - `startMode(mode: 'levels' | 'infinite', level?: number)`:
        - For `'levels'`: sets `totalDistance = 4000 + level * 1000`, generates level obstacle sequence.
        - For `'infinite'`: sets `totalDistance = Infinity`, resets `distanceTraveled = 0`, calls `generator.reset()`, generates initial streaming chunk.
      - `update(dt: number)`:
        - In infinite mode, calls `generator.generateAhead(distanceTraveled, 1800)` and `generator.cullBehind(distanceTraveled)`.
        - Increments score based on distance and obstacle clears.
        - Updates `infiniteHighScore` and saves via `saveData` on new high score or game over.
        - Never triggers stage victory in infinite mode; continues until collision game over.
    - Unit tests:
      - Obstacle streaming generates new obstacles as player moves forward in infinite mode.
      - Culling removes passed obstacles behind player to prevent memory leaks.
      - Infinite mode high score persists and updates correctly.
      - Mode switching between 'levels' and 'infinite' initializes state properly.
  </behavior>
  <action>
    1. Update `ObstacleGenerator.ts`:
       - Add `furthestGeneratedX: number` property.
       - Implement `generateAhead(currentX: number, bufferDistance: number = 1800): Obstacle[]`.
       - Implement `cullBehind(minX: number): void`.
    2. Update `GameState.ts`:
       - Add `public mode: 'levels' | 'infinite' = 'levels'`.
       - Add `public infiniteHighScore: number = 0`.
       - Add `loadInfiniteHighScore()` and `saveInfiniteHighScore()`.
       - Implement `startMode(mode: 'levels' | 'infinite', level: number = 1): void`.
       - In `update(dt)`: if `mode === 'infinite'`, stream obstacles with `generateAhead`, cull with `cullBehind`, and update infinite score metrics.
    3. Update `games/square-bird/test/SquareBird.test.ts` to test infinite mode obstacle generation, culling, scoring, and high score persistence.
  </action>
  <verify>
    <automated>npx vitest run games/square-bird/test/SquareBird.test.ts</automated>
  </verify>
  <done>Infinite mode obstacle streaming and state management pass 100% of unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Action (Space / Pointer) → Egg Lay | Input rate and stack height bounds |
| GameState → Playables Storage Adapter | Infinite mode high score persistence boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Denial of Service | ObstacleGenerator.ts | mitigate | Cull behind camera (`cullBehind`) and cap generated obstacle list size to prevent unbounded memory growth in infinite mode |
| T-41-02 | Tampering | BirdPhysics.ts | mitigate | Clamp minimum egg lifetime to `0` and clamp maximum stack height to prevent NaN coordinate calculations or negative array indices |
| T-41-03 | Information Disclosure | GameState.ts | mitigate | Validate parsed localStorage values from `loadData('square-bird-infinite-highscore')`, defaulting safely to `0` on corrupt input |
</threat_model>

<verification>
Automated vitest suite passes for all Square Bird model tests:
`npx vitest run games/square-bird/test/SquareBird.test.ts`
</verification>

<success_criteria>
- Egg blocks decay over time (3.0s default) and expire cleanly with gravity physics.
- Obstacle streaming allows endless procedural obstacle generation with automatic memory culling.
- GameState supports both 'levels' and 'infinite' modes with persistent high scores.
- 100% unit tests pass in `SquareBird.test.ts`.
</success_criteria>

<output>
After completion, create `.planning/phases/41-square-bird-timed-blocks-infinity/41-01-SUMMARY.md`
</output>
