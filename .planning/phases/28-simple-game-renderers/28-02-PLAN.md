---
phase: 28-simple-game-renderers
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - games/game-2048/src/TileRenderer.ts
  - games/game-2048/src/Game2048Scene.ts
  - games/pop-balloon/src/BalloonRenderer.ts
  - games/pop-balloon/src/PopBalloonScene.ts
  - src/data/games.ts
autonomous: true
requirements:
  - CRAFT-05
  - CRAFT-08
must_haves:
  truths:
    - "2048 renders tiles as numbered papercut tiles with craft color palette, cardboard board frame, and paper score notes"
    - "Pop Balloon renders balloons as papercut silhouettes with string ribbons, paper spike bombs, kraft sky background, and taped HUD"
    - "src/data/games.ts metadata (titles, descriptions, icons, theme colors, banners) updated for all 5 simple games to match papercraft theme"
    - "Full test suite (619+ tests) passes and Vite production build succeeds"
  artifacts:
    - path: "games/game-2048/src/TileRenderer.ts"
      provides: "Craft paper tile color palette, layered drop shadow tiles, ink typography with Patrick Hand and Comfortaa"
      contains: "Patrick Hand"
    - path: "games/pop-balloon/src/BalloonRenderer.ts"
      provides: "Papercut balloon shapes with ink outlines, ribbon strings, paper hazard bombs"
      contains: "BalloonRenderer"
    - path: "src/data/games.ts"
      provides: "Updated catalog entries for safe-cracker, memory-cards, memory-boxes, game-2048, and pop-balloon"
      contains: "2048 Paper"
  key_links:
    - from: "games/game-2048/src/Game2048Scene.ts"
      to: "games/game-2048/src/TileRenderer.ts"
      via: "TileRenderer render and animations"
      pattern: "this\\.renderer\\.render"
    - from: "games/pop-balloon/src/PopBalloonScene.ts"
      to: "games/pop-balloon/src/BalloonRenderer.ts"
      via: "BalloonRenderer renderBalloon calls"
      pattern: "this\\.renderer\\.renderBalloon"
    - from: "src/data/games.ts"
      to: "index.html"
      via: "Hub game card presentation"
      pattern: "GAMES"
---

<objective>
Convert canvas renderers for `game-2048` and `pop-balloon` to papercraft visuals, update metadata in `src/data/games.ts` for all 5 simple games, and execute full build & test verification.
1. `game-2048`: Replace neon tile palette with rich construction paper and craft stock values (`2` through `2048+`), layered cardboard board backing (`#C5A880` / `#D8C3A5`), ink-drawn borders (`#3E2723`), craft typography (`"Patrick Hand"`, `"Comfortaa"`), sticky-note score/best cards, and paper victory/game-over placards.
2. `pop-balloon`: Replace cyber dark sky with kraft/sky parchment background (`#F4EAD4` / soft craft sky gradient), papercut colored balloons with inked contours and hand-drawn string ribbons, paper origami spike bombs, taped score board, and paper confetti bursts.
3. `src/data/games.ts`: Update titles, descriptions, theme colors, and banner gradients for `safe-cracker`, `memory-cards`, `memory-boxes`, `game-2048`, and `pop-balloon` to reflect the 2D papercraft theme.
4. Verify tests and build integrity across the entire project.

Purpose: Complete the papercraft transition for all 5 simple games in Milestone v5.0 and update catalog metadata.
Output: Updated `TileRenderer.ts`, `Game2048Scene.ts`, `BalloonRenderer.ts`, `PopBalloonScene.ts`, `src/data/games.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@games/game-2048/src/TileRenderer.ts
@games/game-2048/src/Game2048Scene.ts
@games/pop-balloon/src/BalloonRenderer.ts
@games/pop-balloon/src/PopBalloonScene.ts
@src/data/games.ts

<interfaces>
<!-- Papercraft 2048 Tile Palette Contract -->
export const TILE_THEMES: Record<number, { bg: string; text: string; border: string; shadow: string }> = {
  2:    { bg: '#FFFDF8', text: '#3E2723', border: '#3E2723', shadow: 'rgba(62,39,35,0.2)' },
  4:    { bg: '#FDF6E2', text: '#3E2723', border: '#3E2723', shadow: 'rgba(62,39,35,0.2)' },
  8:    { bg: '#F59E0B', text: '#FFFDF8', border: '#3E2723', shadow: 'rgba(62,39,35,0.25)' },
  16:   { bg: '#F97316', text: '#FFFDF8', border: '#3E2723', shadow: 'rgba(62,39,35,0.25)' },
  32:   { bg: '#E11D48', text: '#FFFDF8', border: '#3E2723', shadow: 'rgba(62,39,35,0.25)' },
  64:   { bg: '#BE123C', text: '#FFFDF8', border: '#3E2723', shadow: 'rgba(62,39,35,0.25)' },
  128:  { bg: '#10B981', text: '#FFFDF8', border: '#3E2723', shadow: 'rgba(62,39,35,0.25)' },
  256:  { bg: '#059669', text: '#FFFDF8', border: '#3E2723', shadow: 'rgba(62,39,35,0.25)' },
  512:  { bg: '#3B82F6', text: '#FFFDF8', border: '#3E2723', shadow: 'rgba(62,39,35,0.25)' },
  1024: { bg: '#8B5CF6', text: '#FFFDF8', border: '#3E2723', shadow: 'rgba(62,39,35,0.25)' },
  2048: { bg: '#EC4899', text: '#FFFDF8', border: '#3E2723', shadow: 'rgba(62,39,35,0.3)' },
};
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 2048 Papercraft Visuals</name>
  <files>games/game-2048/src/TileRenderer.ts, games/game-2048/src/Game2048Scene.ts</files>
  <action>
    Convert 2048 from neon grid into a tactile papercraft tile board game.
    1. In `games/game-2048/src/TileRenderer.ts`:
       - Update `TILE_THEMES` to use the papercraft color palette with crisp `#3E2723` inked borders and cardboard drop shadows (`3px 3px 0px rgba(62,39,35,0.25)`).
       - In `render`: Render the 4x4 board container as a sturdy cardboard tray (`#C5A880` with layered shadow and stitched/dashed inner border) with recessed paper cell slots (`#E8DEC8`).
       - Render tiles with rounded corner papercut geometry, solid ink outlines, and craft typography (`"Patrick Hand"` for large numbers, `"Comfortaa"` for 4-digit+ numbers).
       - Smooth slide, pop merge scale, and spawn animations with paper spring dynamics.
       - Render HUD elements (Score, Best, Undo, New Game buttons) as taped sticky-notes with paper drop shadows.
       - Render Win / Game Over overlays as pinned craft paper modal cards.
    2. In `games/game-2048/src/Game2048Scene.ts`:
       - Render parchment / craft paper background (`#F4EAD4`) and adjust particle sparkles to paper confetti.
  </action>
  <verify>
    <automated>npx vitest run games/game-2048</automated>
  </verify>
  <done>2048 renders with cardboard board tray, papercut numbered tiles, sticky note HUD placards, and paper win/loss overlays; all game-2048 unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Pop Balloon Papercraft Visuals</name>
  <files>games/pop-balloon/src/BalloonRenderer.ts, games/pop-balloon/src/PopBalloonScene.ts</files>
  <action>
    Convert Pop Balloon from neon/dark sky to a handmade papercraft balloon popping carnival.
    1. In `games/pop-balloon/src/BalloonRenderer.ts`:
       - `renderNeonBalloon`: Render balloons as construction paper silhouettes (`#E11D48`, `#3B82F6`, `#F59E0B`, `#10B981`, `#8B5CF6`, `#EC4899`) with crisp `#3E2723` inked contours, crescent paper cutout highlights, paper knot triangles, and swaying crinkled string ribbons.
       - `renderSpikeBomb`: Render hazard bombs as dark brown papercut discs with paper triangular spikes, ink outlines, and a paper warning skull/exclamation mark.
    2. In `games/pop-balloon/src/PopBalloonScene.ts`:
       - Replace dark gradient background with a bright craft sky parchment (`#F4EAD4` with subtle soft paper clouds or dotted grid).
       - `renderHUD`: Render score, time gauge, combo multiplier, and lives on taped note cards using `"Patrick Hand"` and `"Comfortaa"`.
       - Update floating scores and particle pops to paper bursts/shreds.
       - Update start, pause, and game over overlays into taped craft paper cards.
  </action>
  <verify>
    <automated>npx vitest run games/pop-balloon</automated>
  </verify>
  <done>Pop Balloon renders papercut balloons with string ribbons, paper spike bombs, craft sky background, and taped sticky-note HUD; all pop-balloon unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Catalog Metadata Update & Project Verification</name>
  <files>src/data/games.ts</files>
  <action>
    Update metadata for the 5 simple games in `src/data/games.ts` to align with the papercraft theme:
    1. `safe-cracker`: Title "Safe Cracker", update description to mention cracking the cardboard vault tumbler zones, update theme color to `#F59E0B` (craft yellow), and bannerBg to warm kraft gradient (`linear-gradient(135deg, #F4EAD4 0%, #D8C3A5 100%)`).
    2. `memory-cards`: Title "Memory Cards", update description to mention matching papercraft card pairs and craft glyphs, update theme color to `#3B82F6` (craft blue), and bannerBg to craft paper gradient (`linear-gradient(135deg, #F4EAD4 0%, #E8DEC8 100%)`).
    3. `memory-boxes`: Title "Memory Boxes", update description to mention paper puzzle sequence matrix and color tones, update theme color to `#8B5CF6` (craft purple), and bannerBg to craft paper gradient.
    4. `game-2048`: Title "2048 Paper" (or "2048"), description mentioning sliding numbered paper tiles to reach 2048, update theme color to `#F59E0B`, and bannerBg to warm paper gradient.
    5. `pop-balloon`: Title "Pop Balloon", description mentioning popping paper balloons and dodging paper spike bombs, theme color `#E11D48`, and bannerBg to craft paper gradient.
    6. Run entire Vitest test suite and Vite build to confirm 100% clean passes.
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <done>src/data/games.ts updated with papercraft titles, descriptions, theme colors, and banner gradients for all 5 simple games; all tests pass and production build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Hub Catalog Data | Static JSON/TypeScript metadata loaded by Hub UI and adapters |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03 | Information Disclosure | Metadata Schema | accept | Public static catalog data only; no secrets or private keys |
| T-28-04 | Tampering | Canvas Rendering | mitigate | Clamp animation progress values and sanitize score bounds before canvas drawing |
</threat_model>

<verification>
Full test suite and production build pass:
`npm test && npm run build`
</verification>

<success_criteria>
All 5 simple games (safe-cracker, memory-cards, memory-boxes, game-2048, pop-balloon) fully converted to papercraft visuals with updated catalog metadata, 100% passing tests, and successful production build.
</success_criteria>

<output>
After completion, create `.planning/phases/28-simple-game-renderers/28-02-SUMMARY.md`
</output>
