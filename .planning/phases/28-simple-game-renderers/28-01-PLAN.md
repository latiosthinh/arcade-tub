---
phase: 28-simple-game-renderers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - games/safe-cracker/src/Dial.ts
  - games/safe-cracker/src/SafeCrackerScene.ts
  - games/memory-cards/src/CardRenderer.ts
  - games/memory-cards/src/MemoryCardsScene.ts
  - games/memory-boxes/src/BoxRenderer.ts
  - games/memory-boxes/src/MemoryBoxesScene.ts
autonomous: true
requirements:
  - CRAFT-05
must_haves:
  truths:
    - "Safe Cracker renders with cardboard dial plate, kraft paper background, paper cutout pointer, and sticky note instructions"
    - "Memory Cards renders cards as craft card stock with paper cutout glyphs, cardboard backing pattern, and taped note HUD"
    - "Memory Boxes renders construction paper box grid with ink outlines, cardboard frame, and paper status cards"
    - "Safe Cracker, Memory Cards, and Memory Boxes unit tests pass without regressions"
  artifacts:
    - path: "games/safe-cracker/src/SafeCrackerScene.ts"
      provides: "Papercraft canvas background, cardboard dial bezel, paper pointer needle, sticky note overlays and HUD"
      contains: "Patrick Hand"
    - path: "games/memory-cards/src/CardRenderer.ts"
      provides: "Craft card stock rendering, paper glyphs, torn-edge/wobble shadows, inked outlines"
      contains: "Patrick Hand"
    - path: "games/memory-boxes/src/BoxRenderer.ts"
      provides: "Construction paper colored boxes, cardboard grid frame, ink outline borders, paper glow pulses"
      contains: "Comfortaa"
  key_links:
    - from: "games/safe-cracker/src/SafeCrackerScene.ts"
      to: "games/safe-cracker/src/Dial.ts"
      via: "Dial state and target zones"
      pattern: "this\\.dial"
    - from: "games/memory-cards/src/MemoryCardsScene.ts"
      to: "games/memory-cards/src/CardRenderer.ts"
      via: "CardRenderer instance and grid dimensions"
      pattern: "CardRenderer\\.getGridDimensions"
    - from: "games/memory-boxes/src/MemoryBoxesScene.ts"
      to: "games/memory-boxes/src/BoxRenderer.ts"
      via: "BoxRenderer render methods"
      pattern: "this\\.renderer\\.render"
---

<objective>
Convert canvas renderers and scene presentations for 3 simple games (`safe-cracker`, `memory-cards`, `memory-boxes`) to the 2D Papercraft aesthetic.
1. `safe-cracker`: Replace dark metallic dial/rivets with kraft paper background (`#F4EAD4`), cardboard dial face (`#D8C3A5` / `#C5A880`), ink tick marks (`#3E2723`), construction paper colored target arcs (`#F59E0B` score, `#3B82F6` time bonus), papercut pointer needle with ink outline, and sticky-note HUD and overlay panels.
2. `memory-cards`: Update card front and back rendering to use craft card stock (`#FFFDF8`), cardboard inner diamond pattern, construction paper glyph cutouts with ink borders, kraft paper background, and sticky-note score/timer HUD panels.
3. `memory-boxes`: Update box renderer and scene to use construction paper colored boxes (`#E11D48`, `#3B82F6`, `#F59E0B`, `#10B981`, `#8B5CF6`, `#EC4899`), crisp `#3E2723` inked borders, cardboard mounting frame, kraft paper background, and taped paper status overlays.

Purpose: Deliver tactile 2D papercraft in-game visuals for the first 3 simple games in Milestone v5.0.
Output: Updated `SafeCrackerScene.ts`, `Dial.ts`, `CardRenderer.ts`, `MemoryCardsScene.ts`, `BoxRenderer.ts`, and `MemoryBoxesScene.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@games/safe-cracker/src/SafeCrackerScene.ts
@games/safe-cracker/src/Dial.ts
@games/memory-cards/src/CardRenderer.ts
@games/memory-cards/src/MemoryCardsScene.ts
@games/memory-boxes/src/BoxRenderer.ts
@games/memory-boxes/src/MemoryBoxesScene.ts

<interfaces>
<!-- Papercraft Canvas Theme Tokens -->
const PAPERCRAFT = {
  bgParchment: '#F4EAD4',
  bgCard: '#FFFDF8',
  bgKraft: '#D8C3A5',
  bgCardboard: '#C5A880',
  ink: '#3E2723',
  inkLight: 'rgba(62, 39, 35, 0.4)',
  red: '#E11D48',
  blue: '#3B82F6',
  yellow: '#F59E0B',
  green: '#10B981',
  purple: '#8B5CF6',
  pink: '#EC4899',
  tape: 'rgba(255, 248, 220, 0.85)',
  tapeBorder: 'rgba(62, 39, 35, 0.2)',
  fontDisplay: '"Patrick Hand", cursive, sans-serif',
  fontBody: '"Comfortaa", cursive, sans-serif',
  fontSketch: '"Cabin Sketch", cursive, sans-serif',
};
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Safe Cracker Papercraft Visuals</name>
  <files>games/safe-cracker/src/Dial.ts, games/safe-cracker/src/SafeCrackerScene.ts</files>
  <action>
    Transform Safe Cracker from dark industrial vault into a handmade papercraft cardboard safe.
    1. In `games/safe-cracker/src/SafeCrackerScene.ts`:
       - Replace `renderBackground` and rivet border with warm kraft paper background (`#F4EAD4`), decorative stitched/dashed border (`#3E2723`), and paper tape corner reinforcements instead of metal rivets.
       - In `renderDial`: Replace metallic gradients with layered cardboard circles (outer cardboard ring `#C5A880` with layered drop shadow `rgba(62,39,35,0.25)`, inner kraft face `#D8C3A5`), inked tick marks (`#3E2723`), construction yellow target arc (`#F59E0B`) and blue time arc (`#3B82F6`) with solid paper appearance and inked borders.
       - Render rotating pointer needle as a papercut arrow cutout with `#3E2723` outline and central brass paper fastener/brad hub (`#F59E0B` with small shine).
       - In `renderHUD`: Use craft fonts (`"Patrick Hand"`, `"Comfortaa"`), sticky-note score tags, warm paper colors (`#E11D48`, `#3B82F6`, `#3E2723`), and paper pill containers.
       - In `renderOverlays` (Ready, Paused, GameOver): Render dialogs as taped cardboard/paper sticky-notes with drop shadows, craft typography, and papercut restart buttons with pressed states.
    2. Ensure `games/safe-cracker/src/Dial.ts` remains pure in logic and retains full hit-detection accuracy.
  </action>
  <verify>
    <automated>npx vitest run games/safe-cracker</automated>
  </verify>
  <done>Safe Cracker renders with cardboard dial, kraft paper background, paper cutout pointer, sticky note HUD and dialogs; all safe-cracker unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Memory Cards Papercraft Visuals</name>
  <files>games/memory-cards/src/CardRenderer.ts, games/memory-cards/src/MemoryCardsScene.ts</files>
  <action>
    Transform Memory Cards from neon/cyber grid into a papercraft card matching game.
    1. In `games/memory-cards/src/CardRenderer.ts`:
       - `renderCardBack`: Render cards with layered craft stock (`#FFFDF8`), cardboard tan outline (`#C5A880`), inked diamond grid pattern (`#3E2723`), and central paper star/acorn seal with papercraft shadows (`3px 3px 0px rgba(62,39,35,0.2)`).
       - `renderCardFront`: Render front as clean paper stock with construction paper colored glyphs (`#E11D48`, `#3B82F6`, `#F59E0B`, `#10B981`, `#8B5CF6`, `#EC4899`) cut out with thick ink outlines (`#3E2723`) and subtle paper emboss.
       - Match & Flip effects: Render confetti/paper sparkles on matched pairs.
    2. In `games/memory-cards/src/MemoryCardsScene.ts`:
       - Replace background with warm parchment/kraft table texture (`#F4EAD4` with subtle crosshatch/dotted grid).
       - `renderHUD`: Replace digital readout with sticky-note score and timer placards using `"Patrick Hand"` and `"Comfortaa"` fonts, tape accents, and streak multipliers on paper flags.
       - `renderOverlay`: Render victory/gameover screens on pinned craft cards with tape strips and tactile paper buttons.
  </action>
  <verify>
    <automated>npx vitest run games/memory-cards</automated>
  </verify>
  <done>Memory Cards renders craft stock cards, papercut glyphs, sticky note HUDs, and paper victory modals; all memory-cards unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Memory Boxes Papercraft Visuals</name>
  <files>games/memory-boxes/src/BoxRenderer.ts, games/memory-boxes/src/MemoryBoxesScene.ts</files>
  <action>
    Transform Memory Boxes from dark neon matrix into a colorful construction paper grid game.
    1. In `games/memory-boxes/src/BoxRenderer.ts`:
       - `renderBox`: Render each box as a vibrant construction paper tile (`#E11D48`, `#3B82F6`, `#F59E0B`, `#10B981`, `#8B5CF6`, `#EC4899`, `#14B8A6`, `#F97316`, `#6366F1`) with tactile 3D cardboard bevel/shadow (`4px 4px 0px rgba(62,39,35,0.25)`), inked outline (`#3E2723`, 2.5px), and inner paper texture.
       - Active/Light-up state: When a box illuminates during playback or player press, give it a bright paper punch highlight (white inner cutout, elevated paper pop offset `-2px, -2px`, and warm luminous paper glow).
    2. In `games/memory-boxes/src/MemoryBoxesScene.ts`:
       - Background: Render kraft paperboard desktop (`#F4EAD4`) with a cardboard tray/frame surrounding the box grid.
       - HUD: Render sequence counter, score, and lives (paper heart cutouts or stamps) on taped sticky notes using `"Patrick Hand"` and `"Comfortaa"`.
       - Overlays: Render start and game over screens on cardboard cards with taped edges and paper craft buttons.
  </action>
  <verify>
    <automated>npx vitest run games/memory-boxes</automated>
  </verify>
  <done>Memory Boxes renders construction paper grid tiles with ink outlines, cardboard mounting tray, paper HUD note cards, and life stamps; all memory-boxes unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Canvas 2D API | Untrusted input coordinates crossing into canvas interaction handlers |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | Canvas Rendering | mitigate | Ensure coordinate transforms (`ctx.save()`, `ctx.restore()`, clipping paths) are strictly balanced and bounded to canvas dimensions |
| T-28-02 | Denial of Service | Particle & Animation Loop | mitigate | Cap particle counts and avoid unbounded loops or heavy canvas filter allocations in frame render cycles |
</threat_model>

<verification>
Automated unit tests for safe-cracker, memory-cards, and memory-boxes pass:
`npx vitest run games/safe-cracker games/memory-cards games/memory-boxes`
</verification>

<success_criteria>
All 3 simple games (safe-cracker, memory-cards, memory-boxes) render with complete 2D papercraft visual identity (kraft/cardboard backgrounds, papercut sprites/tiles, ink borders, craft typography, and sticky-note overlays).
</success_criteria>

<output>
After completion, create `.planning/phases/28-simple-game-renderers/28-01-SUMMARY.md`
</output>
