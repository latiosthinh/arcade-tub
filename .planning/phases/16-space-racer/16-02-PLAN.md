---
phase: 16-space-racer
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - games/space-racer/index.html
  - games/space-racer/src/WarpRenderer.ts
  - games/space-racer/src/RacerAudio.ts
  - games/space-racer/src/Particles.ts
  - games/space-racer/src/SpaceRacerScene.ts
  - games/space-racer/src/main.ts
  - games/space-racer/test/particles.test.ts
  - vite.config.ts
autonomous: true
requirements:
  - SR-02

must_haves:
  truths:
    - "Canvas renders pseudo-3D perspective warp starfield with streaks, grid horizon lines, and moving speed tunnel"
    - "Canvas renders spaceship with neon thruster flame, dynamic bank tilt, shield glow aura, and damage flash"
    - "Canvas renders approaching 3D asteroids with textured rock facets, plasma mines with pulsing energy rings, and turbo boost rings with neon glow"
    - "Procedural Web Audio synthesizes continuous engine whine pitch-mapped to speed, turbo boost boom/whistle, near-miss whoosh, shield damage crunch, and game over chord"
    - "Particle system emits thruster flame trails, turbo burst sparks, collision debris, and near-miss shockwave rings"
    - "SpaceRacerScene orchestrates HUD (Score, High Score, Distance, Speedometer, Shield Bar, Boost Timer), screen shake, ready/gameover overlays, pause, and restart"
    - "Vite multi-page rollup config and standalone index.html build cleanly for space-racer"
  artifacts:
    - path: "games/space-racer/index.html"
      provides: "Standalone game HTML host"
      contains: "canvas id=\"game\""
    - path: "games/space-racer/src/WarpRenderer.ts"
      provides: "Pseudo-3D starfield, tunnel lines, ship, asteroid, mine, and boost gate rendering"
      exports: ["WarpRenderer"]
    - path: "games/space-racer/src/RacerAudio.ts"
      provides: "Synthesized dynamic engine audio, boost sound, impact crunch, and gate chime"
      exports: ["RacerAudio"]
    - path: "games/space-racer/src/Particles.ts"
      provides: "Thruster trail, explosion debris, warp streaks, and near-miss particles"
      exports: ["ParticleSystem"]
    - path: "games/space-racer/src/SpaceRacerScene.ts"
      provides: "GameScene implementation coordinating rendering, audio, input, and state"
      exports: ["SpaceRacerScene"]
    - path: "games/space-racer/src/main.ts"
      provides: "Entrypoint wiring GameLoop and Playables adapter"
  key_links:
    - from: "games/space-racer/src/SpaceRacerScene.ts"
      to: "games/space-racer/src/WarpRenderer.ts"
      via: "rendering 3D space track, ship, and obstacles"
      pattern: "WarpRenderer"
    - from: "games/space-racer/src/SpaceRacerScene.ts"
      to: "games/space-racer/src/RacerAudio.ts"
      via: "audio engine and impact SFX"
      pattern: "RacerAudio"
    - from: "vite.config.ts"
      to: "games/space-racer/index.html"
      via: "rollup input registration"
      pattern: "'space-racer': resolve"
---

<objective>
Build pseudo-3D warp starfield rendering, spaceship graphics with thruster trails and banking tilt, asteroid/mine/boost gate visuals, procedural audio synthesizer (engine pitch, boost boom, impact crunch), particle explosion/warp systems, SpaceRacerScene orchestrator, standalone HTML entrypoint, and Vite rollup wiring for Space Racer minigame (SR-02).

Purpose: Deliver an exhilarating arcade pseudo-3D racer experience with dynamic speed sensations, engine sounds, neon visuals, and high score persistence.
Output: `index.html`, `WarpRenderer.ts`, `RacerAudio.ts`, `Particles.ts`, `SpaceRacerScene.ts`, `main.ts`, `particles.test.ts`, and updated `vite.config.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-space-racer/16-01-PLAN.md
@packages/game-engine/src/index.ts
@packages/playables-adapter/src/index.ts
@games/space-racer/src/Ship.ts
@games/space-racer/src/TrackHazardManager.ts
@games/space-racer/src/HighwaySpeedPhysics.ts
@games/space-racer/src/GameState.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement WarpRenderer, RacerAudio, ParticleSystem, and unit tests</name>
  <files>
    games/space-racer/src/WarpRenderer.ts,
    games/space-racer/src/RacerAudio.ts,
    games/space-racer/src/Particles.ts,
    games/space-racer/test/particles.test.ts
  </files>
  <behavior>
    - WarpRenderer:
      - 3D Projection math: project (x, z) where z is 1.0 (horizon) down to 0.0 (near camera/bottom) into 2D canvas coordinates (800x600).
      - Perspective: horizon at `y = 220`, camera plane at `y = 540`, FOV expansion scaling factor `scale = 1 / (z * 4.0 + 0.6)`.
      - Renders:
        - Starfield & Warp Streaks: background stars radiating outwards from vanishing point (400, 220).
        - Track Grid / Highway Rails: neon perspective lines outlining track edges and lane dividers with animated scrolling dashed lines.
        - Spacecraft: sleek delta-wing jet at bottom center (`y = 520`), horizontal position according to `ship.x`, banking tilt angle (`ship.tilt`), thruster flame with flicker, glowing cyan shield bubble (when active), and red damage flash.
        - Asteroids: faceted 3D rotating rock polygons with shaded craters and rim lighting, sized by depth `scale`.
        - Plasma Mines: glowing red/orange tetrahedral spike core with pulsing energy shields.
        - Turbo Boost Rings: glowing cyan/magenta rectangular or circular energy gates with passing perspective animation.
    - RacerAudio:
      - Continuous engine drone oscillator: pitch modulated by `speed / maxSpeed` (100Hz -> 380Hz).
      - Sound effects:
        - `playBoost()`: ascending whoosh and explosive bass burst.
        - `playHit()`: metallic crunch and low-pass noise rumble.
        - `playNearMiss()`: high-speed Doppler whoosh.
        - `playGatePass()`: bright synth chime chord.
        - `playGameOver()`: descending minor chord sweep.
      - Respects master mute flag.
    - ParticleSystem:
      - Thruster flame particles behind ship.
      - Asteroid debris chunks on collision.
      - Warp star streaks accelerating at high speed.
      - Boost speed lines radiating from horizon.
    - Tests verify particle lifecycle, emission, and cleanup.
  </behavior>
  <action>
    1. Implement `WarpRenderer.ts` with clean 3D-to-2D perspective projection functions and canvas draw routines.
    2. Implement `RacerAudio.ts` with Web Audio API nodes, safe context resumption, and engine pitch modulation.
    3. Implement `Particles.ts` supporting engine trails, explosions, warp speed streaks, and spark bursts.
    4. Write unit tests in `games/space-racer/test/particles.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run games/space-racer/test/particles.test.ts</automated>
  </verify>
  <done>WarpRenderer, RacerAudio, and ParticleSystem implemented and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Build SpaceRacerScene, HTML page, main entrypoint, and Vite config rollup registration</name>
  <files>
    games/space-racer/index.html,
    games/space-racer/src/SpaceRacerScene.ts,
    games/space-racer/src/main.ts,
    vite.config.ts
  </files>
  <action>
    1. Create `games/space-racer/index.html` with 800x600 canvas container, title "Space Racer - Arcade Carnival", and script `./src/main.ts`.
    2. Implement `SpaceRacerScene.ts` (implements `GameScene`):
       - Coordinates `GameState`, `Ship`, `TrackHazardManager`, `HighwaySpeedPhysics`, `WarpRenderer`, `RacerAudio`, and `ParticleSystem`.
       - Input handling:
         - Keyboard: Left/Right arrows or A/D for steering, Space for start/restart, Esc for pause.
         - Touch / Mouse drag: Maps pointer horizontal position directly to ship target X across the canvas.
       - Game loop:
         - Updates speed, distance, ship kinematics, obstacle stream, near-misses, and particle emitters.
         - Triggers audio and visual shake on hits or near-misses.
         - Screen shake during asteroid hits.
       - HUD:
         - Top-left: Distance traveled & Current Speedometer (KM/H or Warp Factor).
         - Top-center: Score & Multiplier badge (e.g. 2.5x Speed Bonus).
         - Top-right: High Score & Shield HP Bar (3 cyan pips).
         - Bottom: Boost gauge / Active boost countdown timer.
         - Overlays: Ready screen with controls diagram, Pause screen, Game Over summary (Distance, Asteroids Dodged, Gates Cleared, Final Score, High Score).
    3. Create `games/space-racer/src/main.ts`:
       - Entrypoint setting up `GameLoop`, `InputManager`, `SpaceRacerScene`, and Playables SDK lifecycle hooks.
    4. Update `vite.config.ts`:
       - Register `'space-racer': resolve(__dirname, 'games/space-racer/index.html')` under `rollupOptions.input`.
    5. Run full workspace typecheck, test suite, and Vite build test.
  </action>
  <verify>
    <automated>npm run typecheck && npm run test && npm run build</automated>
  </verify>
  <done>Space Racer minigame runs at 60fps with full pseudo-3D visuals, responsive ship controls, dynamic audio, and builds cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser Input Events → Ship Steering | Pointer/keyboard inputs sanitized to valid range [-1.0, 1.0] |
| Web Audio API → AudioContext Output | Controlled oscillator frequencies and master gain preventing spikes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Denial of Service | WarpRenderer.ts / Particles.ts | mitigate | Cap star counts (max 100) and particles (max 200) to maintain 60fps |
| T-16-05 | Tampering | SpaceRacerScene.ts | mitigate | Ignore steering inputs during game over or pause states |
| T-16-06 | Elevation of Privilege | index.html | mitigate | Standard standalone HTML with no untrusted external scripts |
</threat_model>

<verification>
Full workspace verification:
1. Type check passes: `npm run typecheck`
2. All unit tests pass: `npm run test`
3. Multi-page production build succeeds: `npm run build`
</verification>

<success_criteria>
- Player can steer spaceship horizontally across high-speed warp track.
- Pseudo-3D starfield, track rails, asteroids, plasma mines, and turbo boost rings render smoothly on canvas.
- Passing boost rings triggers turbo boost and shield recovery; hitting asteroids causes damage and screen shake.
- Procedural audio generates responsive engine pitch and impact SFX.
- 100% test pass rate and clean multi-page Vite build.
</success_criteria>

<output>
After completion, create `.planning/phases/16-space-racer/16-02-SUMMARY.md`
</output>
