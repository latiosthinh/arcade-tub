---
phase: 54-mobile-virtual-controls-and-responsive-viewport
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - games/tank-1990/src/types.ts
  - games/tank-1990/src/TouchControls.ts
  - games/tank-1990/src/ViewportManager.ts
autonomous: true
requirements:
  - MOBILE-01
  - MOBILE-02
  - MOBILE-03
must_haves:
  truths:
    - "TouchControls provides an on-screen 4-way virtual D-Pad with angular hysteresis (45-degree quadrant boundaries and deadzone threshold) and a dedicated Fire button."
    - "TouchControls tracks and isolates multiple active pointer IDs independently, allowing holding a D-Pad direction while repeatedly tapping Fire without stutter."
    - "TouchControls binds pointerdown, pointermove, pointerup, and pointercancel with touch-action: none to suppress default gesture zoom and browser scrolling."
    - "ViewportManager scales the 416×416 arena (plus side/bottom HUD margins) across portrait, landscape, and desktop viewports while maintaining integer/crisp canvas pixel ratios."
    - "ViewportManager transforms client/screen pointer coordinates into game arena canvas coordinates accurately."
  artifacts:
    - path: "games/tank-1990/src/types.ts"
      provides: "Touch control state interfaces, pointer mappings, and viewport layout dimensions"
      contains: "TouchControlState"
    - path: "games/tank-1990/src/TouchControls.ts"
      provides: "Multi-touch isolated virtual D-Pad with angular hysteresis and dedicated Fire button"
      min_lines: 150
    - path: "games/tank-1990/src/ViewportManager.ts"
      provides: "Aspect ratio letterbox scaling and pointer-to-canvas coordinate projection"
      min_lines: 120
  key_links:
    - from: "games/tank-1990/src/TouchControls.ts"
      to: "games/tank-1990/src/types.ts"
      via: "Emitting CardinalDirection and fire button active state"
      pattern: "CardinalDirection|TouchControlState"
    - from: "games/tank-1990/src/ViewportManager.ts"
      to: "games/tank-1990/src/GridMap.ts"
      via: "Scaling target ARENA_SIZE 416x416"
      pattern: "ARENA_SIZE"
---

<objective>
Implement `TouchControls` (virtual 4-way D-Pad with angular hysteresis and dedicated Fire button) and `ViewportManager` (responsive aspect ratio preservation and coordinate projection) for Tank 1990.

Purpose: Deliver seamless mobile touch controls with zero diagonal jitter, multi-touch isolation, and crisp aspect-ratio scaling across all device viewports.
Output: `games/tank-1990/src/types.ts` (updated), `games/tank-1990/src/TouchControls.ts`, `games/tank-1990/src/ViewportManager.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-mobile-virtual-controls-and-responsive-viewport/54-CONTEXT.md
@games/tank-1990/src/types.ts
@games/tank-1990/src/GridMap.ts
@games/tank-1990/src/PlayerTank.ts

<interfaces>
From games/tank-1990/src/types.ts:
```typescript
export type CardinalDirection = 'UP' | 'DOWN' | 'LEFT' | 'RIGHT';
export interface Rect { x: number; y: number; width: number; height: number; }
export const TANK_SIZE = 28;
export const ARENA_SIZE = 416;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Touch & Viewport Interfaces in types.ts</name>
  <files>games/tank-1990/src/types.ts</files>
  <behavior>
    - Add `TouchControlState`, `DPadBounds`, `FireButtonBounds`, `ViewportTransform`, and `ViewportMetrics` interfaces.
    - Export constants for D-Pad base radius, deadzone radius, button dimensions, and default virtual control layout zones.
  </behavior>
  <action>
    Update `games/tank-1990/src/types.ts`:
    1. Define `TouchControlState`: `{ direction: CardinalDirection | null; isFiring: boolean; dpadActive: boolean; fireActive: boolean; rawVector: { x: number; y: number } }`.
    2. Define `DPadConfig`: `{ centerX: number; centerY: number; radius: number; deadzone: number; hysteresisAngleDeg?: number }`.
    3. Define `FireButtonConfig`: `{ centerX: number; centerY: number; radius: number }`.
    4. Define `ViewportMetrics`: `{ scale: number; offsetX: number; offsetY: number; canvasWidth: number; canvasHeight: number; gameWidth: number; gameHeight: number; isLandscape: boolean }`.
    5. Define `TOTAL_CANVAS_WIDTH = 512` (416 arena + 96 side HUD) and `TOTAL_CANVAS_HEIGHT = 448` (416 arena + 32 top/bottom margins).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Touch and Viewport types exported with zero TypeScript compilation errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TouchControls with Angular Hysteresis & Multi-Touch Isolation</name>
  <files>games/tank-1990/src/TouchControls.ts</files>
  <behavior>
    - Pointer down in D-Pad region assigns `dpadPointerId` and calculates cardinal direction based on 45-degree sector slices.
    - Pointer down in Fire button region assigns `firePointerId` and sets `isFiring = true`.
    - Angular hysteresis prevents rapid switching near sector boundaries (requires angle delta beyond hysteresis threshold).
    - Multi-touch isolation allows holding D-Pad direction with one finger while tapping Fire with another finger.
    - Pointer up/cancel cleanly releases corresponding pointer ID without affecting other active pointers.
    - Supports dynamic custom placement or fixed layout zones.
    - Provides `render(ctx: CanvasRenderingContext2D)` for drawing tactile papercraft-styled on-screen D-Pad and Fire buttons.
  </behavior>
  <action>
    Create `games/tank-1990/src/TouchControls.ts`:
    1. Implement class `TouchControls`:
       - Properties: `dpadPointerId: number | null`, `firePointerId: number | null`, `dpadConfig: DPadConfig`, `fireConfig: FireButtonConfig`, `state: TouchControlState`, `currentAngle: number | null`, `lastDirection: CardinalDirection | null`.
       - Quadrant calculation: Compute angle from center `Math.atan2(dy, dx) * 180 / Math.PI` (normalized 0..360 or -180..180).
       - Deadzone check: If distance `< deadzone`, direction is `null`.
       - Angular hysteresis: When direction is active, transitioning to an adjacent cardinal direction requires crossing boundary by >= 10 degrees buffer (e.g. 45° boundary requires 55° or 35°).
       - Cardinal boundaries:
         * RIGHT: 315° to 45° (-45° to 45°)
         * DOWN: 45° to 135°
         * LEFT: 135° to 225°
         * UP: 225° to 315° (-135° to -45°)
       - Attach DOM listeners (`pointerdown`, `pointermove`, `pointerup`, `pointercancel`) on container/canvas element.
       - Isolate pointers by pointerId: Fire button pointer never overrides DPad pointer; DPad pointer never overrides Fire pointer.
       - Implement `destroy()` to cleanly remove event listeners.
       - Implement `render(ctx)` to draw cardboard D-Pad cross with directional arrows and circular Fire button with tactile press feedback.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>TouchControls handles multi-touch pointer events, calculates cardinal directions with angular hysteresis, and draws virtual controls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement ViewportManager for Aspect-Ratio Scaling & Coordinate Projection</name>
  <files>games/tank-1990/src/ViewportManager.ts</files>
  <behavior>
    - Computes uniform aspect-ratio scale to fit virtual game canvas (e.g., 512×448 or 416×416 arena) inside parent window/container.
    - Centers canvas with letterbox/pillarbox margins (`offsetX`, `offsetY`).
    - Projects window client coordinates `(clientX, clientY)` into internal virtual game canvas coordinates `(virtualX, virtualY)`.
    - Handles resize events and devicePixelRatio adjustments for crisp rendering (`image-rendering: pixelated`).
  </behavior>
  <action>
    Create `games/tank-1990/src/ViewportManager.ts`:
    1. Implement class `ViewportManager`:
       - Constructor accepts `canvas: HTMLCanvasElement`, virtual width (default 512), virtual height (default 448).
       - Method `resize(containerWidth: number, containerHeight: number): ViewportMetrics`:
         * Scale = `Math.min(containerWidth / virtualWidth, containerHeight / virtualHeight)`.
         * OffsetX = `(containerWidth - virtualWidth * scale) / 2`.
         * OffsetY = `(containerHeight - virtualHeight * scale) / 2`.
         * Applies CSS transform or direct canvas style width/height and centering.
       - Method `clientToVirtual(clientX: number, clientY: number, containerRect?: DOMRect): { x: number; y: number }`:
         * Converts screen coordinates to virtual game space: `virtualX = (clientX - rect.left - offsetX) / scale`.
         * Clamps coordinates to `[0, virtualWidth]` and `[0, virtualHeight]`.
       - Method `getMetrics(): ViewportMetrics`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>ViewportManager scales game canvas preserving aspect ratio and accurately maps touch coordinates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM Pointer Events -> TouchControls | Untrusted user touch/mouse inputs crossing into game kinematics |
| Window Resize -> ViewportManager | Arbitrary window and container dimension inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Denial of Service | TouchControls | mitigate | Clamp coordinates, validate finite numbers, ignore NaN/Infinity from malformed pointer events. |
| T-54-02 | Elevation of Privilege | Pointer Events | mitigate | Isolate pointerId tracking and ensure pointercancel and pointerup always reset state to prevent stuck movement keys. |
| T-54-03 | Denial of Service | ViewportManager | mitigate | Guard against zero or negative container dimensions by enforcing minimum 1px bounds and non-zero scale factor (Math.max(scale, 0.001)). |
</threat_model>

<verification>
`npx tsc --noEmit` passes with 0 type errors across `TouchControls.ts` and `ViewportManager.ts`.
</verification>

<success_criteria>
`TouchControls.ts` and `ViewportManager.ts` implement 4-way angular hysteresis, multi-touch isolation, and aspect-ratio scaling with full TypeScript compliance.
</success_criteria>

<output>
After completion, create `.planning/phases/54-mobile-virtual-controls-and-responsive-viewport/54-01-SUMMARY.md`
</output>
