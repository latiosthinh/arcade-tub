---
phase: 54-mobile-virtual-controls-and-responsive-viewport
plan: 02
type: tdd
wave: 2
depends_on:
  - 54-01
files_modified:
  - games/tank-1990/test/TouchControls.test.ts
  - games/tank-1990/test/ViewportManager.test.ts
autonomous: true
requirements:
  - MOBILE-01
  - MOBILE-02
  - MOBILE-03
must_haves:
  truths:
    - "TouchControls test suite verifies cardinal 4-way direction calculation across all angular sectors (UP, DOWN, LEFT, RIGHT)."
    - "TouchControls test suite verifies deadzone clamping where sub-threshold pointer distances return null direction."
    - "TouchControls test suite verifies angular hysteresis preventing rapid direction jitter across 45-degree quadrant boundaries."
    - "TouchControls test suite verifies multi-touch pointer ID isolation allowing independent D-Pad holding and Fire tapping."
    - "ViewportManager test suite verifies uniform aspect-ratio scaling, centering offsets (letterbox/pillarbox), and client-to-virtual coordinate transformations."
    - "All test suites achieve 100% pass rate in Vitest with zero regressions."
  artifacts:
    - path: "games/tank-1990/test/TouchControls.test.ts"
      provides: "Unit test suite for virtual D-Pad angles, deadzone, hysteresis, and multi-pointer events"
      min_lines: 120
    - path: "games/tank-1990/test/ViewportManager.test.ts"
      provides: "Unit test suite for aspect ratio calculations, resize metrics, and coordinate mappings"
      min_lines: 80
  key_links:
    - from: "games/tank-1990/test/TouchControls.test.ts"
      to: "games/tank-1990/src/TouchControls.ts"
      via: "Simulating PointerEvents and asserting TouchControlState"
      pattern: "touchControls\\.getState|touchControls\\.handlePointer"
    - from: "games/tank-1990/test/ViewportManager.test.ts"
      to: "games/tank-1990/src/ViewportManager.ts"
      via: "Asserting resize metrics and clientToVirtual mappings"
      pattern: "viewportManager\\.resize|viewportManager\\.clientToVirtual"
---

<objective>
Implement comprehensive Vitest unit test suites for `TouchControls` and `ViewportManager`.

Purpose: Guarantee rock-solid touch control responsiveness, angular hysteresis stability, multi-pointer isolation, and aspect-ratio coordinate projection accuracy.
Output: `games/tank-1990/test/TouchControls.test.ts`, `games/tank-1990/test/ViewportManager.test.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-mobile-virtual-controls-and-responsive-viewport/54-CONTEXT.md
@.planning/phases/54-mobile-virtual-controls-and-responsive-viewport/54-01-PLAN.md
@games/tank-1990/src/types.ts
@games/tank-1990/src/TouchControls.ts
@games/tank-1990/src/ViewportManager.ts

<interfaces>
From games/tank-1990/src/TouchControls.ts:
```typescript
export class TouchControls {
  constructor(container: HTMLElement | Window, options?: Partial<TouchControlOptions>);
  public getState(): TouchControlState;
  public handlePointerDown(e: PointerEvent | MockPointerEvent): void;
  public handlePointerMove(e: PointerEvent | MockPointerEvent): void;
  public handlePointerUp(e: PointerEvent | MockPointerEvent): void;
  public handlePointerCancel(e: PointerEvent | MockPointerEvent): void;
  public calculateDirection(dx: number, dy: number): CardinalDirection | null;
  public destroy(): void;
}
```

From games/tank-1990/src/ViewportManager.ts:
```typescript
export class ViewportManager {
  constructor(canvas: HTMLCanvasElement, virtualWidth?: number, virtualHeight?: number);
  public resize(containerWidth: number, containerHeight: number): ViewportMetrics;
  public clientToVirtual(clientX: number, clientY: number, containerRect?: DOMRect): { x: number; y: number };
  public getMetrics(): ViewportMetrics;
}
```
</interfaces>
</context>

<feature>
  <name>TouchControls & ViewportManager Comprehensive Test Suite</name>
  <files>games/tank-1990/test/TouchControls.test.ts, games/tank-1990/test/ViewportManager.test.ts</files>
  <behavior>
    - Deadzone test: Distance < deadzone (e.g. 10px from center when deadzone=15) yields `direction: null`.
    - Quadrant angles:
      * (dx: 30, dy: 0) -> 0° -> 'RIGHT'
      * (dx: 0, dy: 30) -> 90° -> 'DOWN'
      * (dx: -30, dy: 0) -> 180° -> 'LEFT'
      * (dx: 0, dy: -30) -> 270°/-90° -> 'UP'
    - Angular hysteresis test:
      * Moving from 0° (RIGHT) to 48° (just over 45° boundary) stays 'RIGHT' when hysteresis buffer = 10°.
      * Moving past 55° (45° + 10°) switches direction to 'DOWN'.
    - Multi-touch isolation test:
      * Pointer ID 1 touches D-Pad -> `dpadActive: true`, `direction: 'UP'`.
      * Pointer ID 2 touches Fire button -> `fireActive: true`, `isFiring: true`, `direction: 'UP'`.
      * Pointer ID 2 lifted (pointerup) -> `fireActive: false`, `isFiring: false`, but `dpadActive: true` and `direction: 'UP'` preserved.
      * Pointer ID 1 lifted -> `dpadActive: false`, `direction: null`.
    - Viewport aspect ratio scaling test:
      * 1024×896 container with 512×448 game yields scale 2.0, offsetX 0, offsetY 0.
      * 1200×896 container yields scale 2.0, offsetX 88 (pillarbox/letterbox centering), offsetY 0.
      * 800×1000 container yields scale 1.5625, offsetX 0, offsetY 150.
    - Coordinate conversion test:
      * `clientToVirtual` properly scales and translates mouse/touch click points back to virtual canvas coordinates.
  </behavior>
  <implementation>
    Write Vitest test suites simulating pointer events and container resizing without requiring browser canvas rendering context.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Unit Tests for TouchControls</name>
  <files>games/tank-1990/test/TouchControls.test.ts</files>
  <behavior>
    - Test cardinal direction resolution across 0°, 90°, 180°, 270°.
    - Test deadzone clamping for sub-threshold distance.
    - Test hysteresis angular buffer on diagonal boundary transitions.
    - Test multi-touch pointer ID isolation (holding D-Pad + tapping Fire).
    - Test pointer cancellation and release cleanup.
  </behavior>
  <action>
    Create `games/tank-1990/test/TouchControls.test.ts`:
    1. Construct test fixtures with mock DOM container element or direct handler dispatch.
    2. Write tests for:
       - Default state `{ direction: null, isFiring: false, dpadActive: false, fireActive: false }`.
       - Deadzone: Pointer at center (radius < deadzone) returns null direction.
       - Cardinal sectors: Exact and near-center angles for RIGHT, DOWN, LEFT, UP.
       - Hysteresis: Direction holds near 45°, 135°, 225°, 315° boundaries until hysteresis angle is exceeded.
       - Multi-pointer isolation: Pointer 1 on DPad, Pointer 2 on Fire button. Releasing Pointer 2 does not release Pointer 1.
       - Pointer cancel: Reset state when `pointercancel` fires.
  </action>
  <verify>
    <automated>npx vitest run games/tank-1990/test/TouchControls.test.ts</automated>
  </verify>
  <done>All TouchControls tests pass in Vitest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Unit Tests for ViewportManager</name>
  <files>games/tank-1990/test/ViewportManager.test.ts</files>
  <behavior>
    - Test aspect ratio scale calculations for landscape, portrait, and square viewports.
    - Test centering letterbox / pillarbox offset calculations.
    - Test client-to-virtual coordinate transformations and boundary clamping.
    - Test zero and negative dimension resilience.
  </behavior>
  <action>
    Create `games/tank-1990/test/ViewportManager.test.ts`:
    1. Mock `HTMLCanvasElement` with basic style and dimensions.
    2. Write tests for:
       - Exact 1:1 fit (512×448 -> scale: 1, offset: 0, 0).
       - Integer 2x scale (1024×896 -> scale: 2, offset: 0, 0).
       - Wide desktop aspect ratio (letterbox on left/right).
       - Tall mobile portrait aspect ratio (pillarbox on top/bottom).
       - `clientToVirtual` coordinate projection accuracy: center, corners, out-of-bounds clicks clamped.
       - Edge cases: 0x0 container size handled gracefully with fallback scale.
  </action>
  <verify>
    <automated>npx vitest run games/tank-1990/test/ViewportManager.test.ts</automated>
  </verify>
  <done>All ViewportManager tests pass in Vitest.</done>
</task>

<task type="auto">
  <name>Task 3: Execute Full Tank 1990 Vitest Test Suite</name>
  <files>games/tank-1990/test/</files>
  <action>
    Run the entire Vitest test suite for `games/tank-1990/test/` to ensure zero regressions across GridMap, PlayerTank, BulletManager, EnemyTank, EnemySpawner, PowerUpSystem, ScoreManager, GameFlow, ParticleEmitter, TankAudio, TouchControls, and ViewportManager.
  </action>
  <verify>
    <automated>npx vitest run games/tank-1990/test/</automated>
  </verify>
  <done>100% of all Tank 1990 test suites pass with 0 failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Mocks -> Vitest Test Runner | Mock event payloads and container bounds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-04 | Tampering | Test Fixtures | mitigate | Use deterministic mock pointer events and explicit assertions to prevent flaky tests across platforms. |
</threat_model>

<verification>
`npx vitest run games/tank-1990/test/` passes 100% with no errors.
</verification>

<success_criteria>
All tests for TouchControls, ViewportManager, and prior subsystems pass completely.
</success_criteria>

<output>
After completion, create `.planning/phases/54-mobile-virtual-controls-and-responsive-viewport/54-02-SUMMARY.md`
</output>
