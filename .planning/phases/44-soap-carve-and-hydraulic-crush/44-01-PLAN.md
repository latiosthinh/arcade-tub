---
phase: 44-soap-carve-and-hydraulic-crush
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - games/soap-carve/package.json
  - games/soap-carve/tsconfig.json
  - games/soap-carve/index.html
  - games/soap-carve/src/SoapBlock.ts
  - games/soap-carve/src/PeelParticles.ts
  - games/soap-carve/src/FigurineDiscovery.ts
  - games/soap-carve/src/CarveAudio.ts
  - games/soap-carve/src/SoapCarveScene.ts
  - games/soap-carve/src/main.ts
  - games/soap-carve/test/SoapCarve.test.ts
autonomous: true
requirements:
  - FR-02
must_haves:
  truths:
    - "User can carve layered pastel soap bars by dragging cutter tool across surface"
    - "Shaving blade generates curling ribbon peel particles with physics simulation and trajectory"
    - "Carving depth progressively reveals a 3D papercraft origami collectible figurine underneath"
    - "Figurine is collected with victory flourish and added to unlocked showcase when fully uncovered (>80% revealed)"
    - "Blade scrape Web Audio generates realistic ASMR slicing and peeling sound effects that scale with cut speed and depth"
    - "100% unit tests pass for depth grid shaving math, cutter sweep intersection, and figurine reveal percentage"
  artifacts:
    - path: "games/soap-carve/src/SoapBlock.ts"
      provides: "2D heightfield/depth grid for soap layers, blade sweep cutting math, and surface regeneration"
      exports: ["SoapBlock", "SoapLayer", "SOAP_PALETTES"]
    - path: "games/soap-carve/src/PeelParticles.ts"
      provides: "Curling spiral shaving particle generator and physics simulation"
      exports: ["PeelParticleSystem", "PeelParticle"]
    - path: "games/soap-carve/src/FigurineDiscovery.ts"
      provides: "Hidden origami figurine models (Swan, Fox, Frog, Lotus), reveal depth thresholds, and collection state"
      exports: ["FigurineDiscovery", "OrigamiFigurine", "FIGURINES"]
    - path: "games/soap-carve/src/CarveAudio.ts"
      provides: "Procedural ASMR blade scraping, peeling crinkles, and discovery chime Web Audio"
      exports: ["CarveAudio", "carveAudio"]
    - path: "games/soap-carve/src/SoapCarveScene.ts"
      provides: "Canvas renderer with beveled soap layer isometric views, cutter blade dragging, particle rendering, and UI"
      exports: ["SoapCarveScene"]
    - path: "games/soap-carve/test/SoapCarve.test.ts"
      provides: "Unit tests covering heightfield shaving, layer peeling, figurine reveal calculation, and palette generation"
  key_links:
    - from: "games/soap-carve/src/SoapCarveScene.ts"
      to: "games/soap-carve/src/SoapBlock.ts"
      via: "invokes carveSlice on drag pointer events and renders shaved depth layers"
      pattern: "soapBlock"
    - from: "games/soap-carve/src/SoapCarveScene.ts"
      to: "games/soap-carve/src/FigurineDiscovery.ts"
      via: "checks discovery progress and triggers unlock when figurine is uncovered"
      pattern: "figurineDiscovery"
    - from: "games/soap-carve/src/SoapCarveScene.ts"
      to: "games/soap-carve/src/CarveAudio.ts"
      via: "plays continuous scraping friction audio during blade drag motion"
      pattern: "carveAudio"
---

<objective>
Implement `soap-carve` (ASMR Soap & Wood Carver Sandbox) featuring a 2D heightfield depth grid, dragging slice cutter, realistic curling ribbon peel particles, progressive discovery of hidden origami collectible figurines, ASMR blade scraping Web Audio, and complete unit tests.

Purpose: Provide a soothing, tactile sensory carving sandbox with satisfying progressive reveals and relaxing ASMR audio feedback.
Output: Self-contained game package in `games/soap-carve/` with passing Vitest unit tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@packages/game-engine/src/AudioSynthesizer.ts
@games/layers-roll/src/LayersParticles.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build SoapBlock depth grid, PeelParticles simulation, FigurineDiscovery manager, and unit tests</name>
  <files>
    games/soap-carve/package.json,
    games/soap-carve/tsconfig.json,
    games/soap-carve/src/SoapBlock.ts,
    games/soap-carve/src/PeelParticles.ts,
    games/soap-carve/src/FigurineDiscovery.ts,
    games/soap-carve/test/SoapCarve.test.ts
  </files>
  <behavior>
    - SoapBlock:
      - `constructor(cols = 40, rows = 30, maxDepth = 6)`: initializes discrete heightfield grid where each cell has depth 0 to maxDepth.
      - `carveSlice(x1, y1, x2, y2, bladeWidth, cutDepth)`: calculates grid line rasterization; increments cut depth for intersecting cells up to maxDepth; returns count of newly carved voxels and layer colors shaved off.
      - `getDepth(col, row)`: returns depth level (0 to maxDepth) at cell.
      - `getOverallCarvedPercentage()`: returns percentage of block shaved down to target base level.
      - `reset(paletteIndex)`: resets all grid depths to 0 with chosen pastel soap palette (Lavender Mint, Rose Sunset, Citrus Lime, Ocean Wave).
    - PeelParticles:
      - `spawnPeel(startX, startY, vx, vy, color, length)`: generates curling spiral peel ribbon particles that roll up, rotate with angular velocity, and flutter down with gravity and air resistance.
      - `update(dt)`: updates positions, curl angles, and fades opacity before removal.
    - FigurineDiscovery:
      - Contains definitions for hidden papercraft origami figures (`swan`, `fox`, `frog`, `lotus`, `dragon`).
      - `checkReveal(soapBlock)`: overlays figurine bounding mask with soapBlock depth matrix; calculates uncover percentage; returns true if reveal threshold (>80%) reached.
      - `unlockFigurine(id)`: flags figurine as discovered and saves to local collection state.
    - SoapCarve.test.ts:
      - Initial soap grid has uniform depth 0 and 0% carved.
      - Carving along line modifies depth values within blade radius.
      - Shaving beyond max depth clamps safely without overflow.
      - Peel particles update position, curl angle, and decay life correctly.
      - Figurine reveal percentage increases as overlying soap voxels are removed.
      - Reaching 80%+ threshold triggers figurine unlock state.
  </behavior>
  <action>
    Create `package.json`, `tsconfig.json`, `SoapBlock.ts`, `PeelParticles.ts`, and `FigurineDiscovery.ts` with comprehensive math for rasterized slicing, spiral peel physics, and mask-based reveal verification. Author thorough unit tests in `SoapCarve.test.ts` and ensure all tests pass cleanly via Vitest.
  </action>
  <verify>
    <automated>pnpm --filter soap-carve test || npx vitest run games/soap-carve/test/SoapCarve.test.ts</automated>
  </verify>
  <done>SoapBlock heightfield, PeelParticles physics, and FigurineDiscovery logic implemented and verified by passing unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Build CarveAudio ASMR synthesizer, SoapCarveScene interactive canvas renderer, and standalone HTML shell</name>
  <files>
    games/soap-carve/src/CarveAudio.ts,
    games/soap-carve/src/SoapCarveScene.ts,
    games/soap-carve/src/main.ts,
    games/soap-carve/index.html
  </files>
  <action>
    1. Implement `CarveAudio.ts`:
       - `startScrape(speed, depth)`: continuous filtered white/brown noise with resonant high-pass filter modulated by drag velocity to simulate sharp metal blade cutting through waxy soap.
       - `updateScrape(speed)`: dynamic volume and filter frequency ramping based on pointer cutting speed.
       - `stopScrape()`: smooth gain fade-out when drag stops.
       - `playCurlSnap()`: subtle crispy snap when a shaving ribbon curls off the edge.
       - `playDiscoveryChime()`: celebratory pentatonic music box chime when a hidden origami figurine is fully revealed.
       - Honors `arcade-carnival-muted` setting.
    2. Implement `SoapCarveScene.ts`:
       - Isometric/angled top-down canvas rendering of pastel soap block with layered color bands visible along cut contours.
       - Interactive cutter tool following pointer drag path with angled blade graphic.
       - Real-time rendering of curling peel particles twisting away from cutting edge.
       - Shaded relief rendering for exposed origami figurine beneath shaved areas.
       - UI Overlay: Carving progress bar (% shaved), Figurine Discovery popup modal, "New Soap Bar" palette selector, Mute button, Back to Hub button.
    3. Implement `main.ts` and `index.html`:
       - Standalone responsive HTML shell with Patrick Hand / Cabin Sketch / Comfortaa fonts, papercraft styling, and viewport touch locking.
  </action>
  <verify>
    <automated>npx vitest run games/soap-carve/test/SoapCarve.test.ts</automated>
  </verify>
  <done>Complete playable `soap-carve` game with tactile blade slicing, curling peel ribbon particles, hidden origami discovery, ASMR audio, and passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Pointer Slicing Coordinates → Grid Heightfield Indices | Continuous drag events mapped to discrete 2D rasterized voxel grid indices |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Denial of Service | `SoapBlock.ts` | mitigate | Clamp rasterization line coordinates within [0, cols-1] and [0, rows-1] to prevent array bounds out-of-range errors or infinite stepping in Bresenham rasterizer |
| T-44-02 | Resource Exhaustion | `PeelParticles.ts` | mitigate | Hard-cap maximum concurrent peel particles (max 150) and recycle older particles on rapid continuous slicing |
</threat_model>

<verification>
- `npx vitest run games/soap-carve/test/SoapCarve.test.ts` passes 100%.
- Game runs in standalone browser with responsive canvas and tactile blade dragging.
</verification>

<success_criteria>
- Soap carving game fully playable with layer shaving, curly peel particles, origami figurine discoveries, and ASMR audio.
- Vitest unit tests pass with zero regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/44-soap-carve-and-hydraulic-crush/44-01-SUMMARY.md`
</output>
