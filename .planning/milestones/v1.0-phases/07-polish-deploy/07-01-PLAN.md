---
phase: 07-polish-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/game-engine/src/AudioSynthesizer.ts
  - packages/game-engine/src/index.ts
  - packages/game-engine/test/audio.test.ts
autonomous: true
requirements:
  - REQ-10
  - REQ-11
  - REQ-12

must_haves:
  truths:
    - "AudioSynthesizer creates synthesized sound effects via Web Audio API oscillators without external asset files"
    - "AudioSynthesizer supports click, score, bounce, explosion, powerup, error, and victory sound presets"
    - "AudioSynthesizer provides mute/unmute state toggle persisted in localStorage"
  artifacts:
    - path: "packages/game-engine/src/AudioSynthesizer.ts"
      provides: "Procedural Web Audio synthesizer singleton and sound presets"
      exports: ["AudioSynthesizer", "audio"]
    - path: "packages/game-engine/test/audio.test.ts"
      provides: "Unit tests verifying synthesized sound triggers, mute toggling, and storage fallback"
      min_lines: 30
  key_links:
    - from: "packages/game-engine/src/index.ts"
      to: "packages/game-engine/src/AudioSynthesizer.ts"
      via: "export { AudioSynthesizer, audio }"
      pattern: "export.*AudioSynthesizer"
---

<objective>
Implement procedural Web Audio API sound synthesizer singleton in `packages/game-engine` with zero external audio assets, supporting standard game sound effects (`playClick`, `playScore`, `playBounce`, `playExplosion`, `playPowerup`, `playError`, `playVictory`), master volume control, and persistent mute toggle.

Purpose: Provide shared, zero-overhead audio capabilities across all 5 arcade minigames without introducing audio asset download latency or licensing issues.
Output: `packages/game-engine/src/AudioSynthesizer.ts`, updated exports in `packages/game-engine/src/index.ts`, and test suite in `packages/game-engine/test/audio.test.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-deploy/07-CONTEXT.md
@packages/game-engine/src/index.ts

<interfaces>
From packages/game-engine/src/index.ts:
```typescript
export { GameLoop } from './GameLoop.js';
export type { GameScene } from './GameLoop.js';
export { InputManager } from './InputManager.js';
export { SceneManager } from './SceneManager.js';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement AudioSynthesizer and audio presets in game-engine</name>
  <files>packages/game-engine/src/AudioSynthesizer.ts, packages/game-engine/src/index.ts, packages/game-engine/test/audio.test.ts</files>
  <read_first>
    - packages/game-engine/src/index.ts
    - .planning/phases/07-polish-deploy/07-CONTEXT.md
  </read_first>
  <behavior>
    - AudioSynthesizer singleton initializes Web Audio context lazily on first user interaction.
    - Sound presets: `playClick()` (short 800Hz sine blip), `playScore()` (rising arpeggio 523Hz -> 659Hz -> 784Hz), `playBounce()` (pitch-dropped triangle 300Hz -> 100Hz), `playExplosion()` (noise/low square pitch sweep with exponential decay), `playPowerup()` (rapid major scale glissando), `playError()` (buzzing sawtooth low 120Hz tone), `playVictory()` (fanfare chords).
    - `setMuted(muted: boolean)` and `toggleMute()` update internal muted status and persist to `localStorage.setItem('arcade-carnival-muted', String(muted))`.
    - `isMuted()` returns current mute status, defaulting to stored value or false.
    - Gracefully handles environments without Web Audio API or blocked autoplay policies (unlocks on first call).
  </behavior>
  <action>
    1. Create `packages/game-engine/src/AudioSynthesizer.ts`:
       - Class `AudioSynthesizer` with lazy `AudioContext` / `webkitAudioContext` initialization.
       - Master `GainNode` for volume and mute control.
       - Audio parameter automation using `setValueAtTime`, `exponentialRampToValueAtTime`, `linearRampToValueAtTime`.
       - Helper `resumeContext()` to handle browser user gesture auto-resume.
       - Export class `AudioSynthesizer` and global default singleton `export const audio = new AudioSynthesizer();`.
    2. Export `AudioSynthesizer` and `audio` in `packages/game-engine/src/index.ts`.
    3. Write unit tests in `packages/game-engine/test/audio.test.ts` mocking `AudioContext` to verify mute toggles, localStorage sync, and preset execution without runtime errors.
  </action>
  <verify>
    <automated>pnpm test -- packages/game-engine/test/audio.test.ts</automated>
  </verify>
  <done>AudioSynthesizer compiles cleanly, exports through @arcade-carnival/game-engine, and passes all unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Browser -> Web Audio API | AudioContext creation and localStorage access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Denial of Service | AudioSynthesizer.ts | mitigate | Cap overlapping active audio voices / nodes; disconnect oscillators immediately on playback completion (`onended` event). |
| T-07-02 | Information Disclosure | localStorage key storage | accept | Only storing boolean 'arcade-carnival-muted' preferences. No sensitive user data. |
</threat_model>

<verification>
Run `pnpm test` and `pnpm typecheck` to confirm AudioSynthesizer integration into the monorepo.
</verification>

<success_criteria>
1. `AudioSynthesizer` provides procedural SFX for all 7 game sound events.
2. Mute toggle persists across sessions via localStorage.
3. Unit test coverage passes 100%.
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-deploy/07-01-SUMMARY.md`
</output>
