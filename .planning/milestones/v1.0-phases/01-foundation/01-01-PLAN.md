---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.json
  - tsconfig.base.json
  - vite.config.ts
  - packages/playables-adapter/package.json
  - packages/playables-adapter/tsconfig.json
  - packages/playables-adapter/src/index.ts
  - packages/game-engine/package.json
  - packages/game-engine/tsconfig.json
  - packages/game-engine/src/index.ts
  - games/safe-cracker/package.json
  - games/safe-cracker/tsconfig.json
  - games/safe-cracker/index.html
  - games/safe-cracker/src/main.ts
  - games/brick-blitz/package.json
  - games/brick-blitz/tsconfig.json
  - games/brick-blitz/index.html
  - games/brick-blitz/src/main.ts
  - games/sky-hopper/package.json
  - games/sky-hopper/tsconfig.json
  - games/sky-hopper/index.html
  - games/sky-hopper/src/main.ts
  - games/crate-catch/package.json
  - games/crate-catch/tsconfig.json
  - games/crate-catch/index.html
  - games/crate-catch/src/main.ts
  - games/type-strike/package.json
  - games/type-strike/tsconfig.json
  - games/type-strike/index.html
  - games/type-strike/src/main.ts
  - index.html
  - .gitignore
autonomous: true
requirements:
  - REQ-02
  - REQ-08
  - REQ-10

must_haves:
  truths:
    - "pnpm install succeeds with all workspaces resolved"
    - "pnpm typecheck passes with zero errors"
    - "pnpm build produces dist/ with hub and per-game folders"
    - "pnpm dev launches dev server serving root index.html"
    - "Each game folder has independent entry point (index.html + src/main.ts)"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace definition"
      contains: "packages/*"
    - path: "tsconfig.base.json"
      provides: "Shared TS config with strict mode"
      contains: "strict"
    - path: "vite.config.ts"
      provides: "Multi-page Vite build config"
      contains: "rollupOptions"
    - path: "packages/playables-adapter/src/index.ts"
      provides: "Adapter stub exports"
      exports: ["initPlayables", "reportScore", "saveData", "loadData", "onPause", "onResume"]
    - path: "packages/game-engine/src/index.ts"
      provides: "Engine stub exports"
      exports: ["GameLoop", "InputManager", "SceneManager"]
  key_links:
    - from: "games/*/package.json"
      to: "packages/playables-adapter"
      via: "workspace dependency"
      pattern: "@arcade-carnival/playables-adapter"
    - from: "games/*/package.json"
      to: "packages/game-engine"
      via: "workspace dependency"
      pattern: "@arcade-carnival/game-engine"
---

<objective>
Scaffold pnpm monorepo with TypeScript strict, Vite 7 multi-page build, workspace packages, and 5 game folders with placeholder entry points.

Purpose: Establish project structure so all subsequent phases have a working build pipeline and folder conventions.
Output: Buildable monorepo with `pnpm dev`, `pnpm build`, `pnpm typecheck` all passing.
</objective>

<execution_context>
@.planning/phases/01-foundation/01-01-PLAN.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold monorepo root and workspace config</name>
  <files>package.json, pnpm-workspace.yaml, tsconfig.json, tsconfig.base.json, vite.config.ts, .gitignore, index.html</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md
    - .planning/PROJECT.md
  </read_first>
  <action>
Create root `package.json`:
```json
{
  "name": "arcade-carnival",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "typescript": "^5.8.0"
  }
}
```

Create `pnpm-workspace.yaml`:
```yaml
packages:
  - "packages/*"
  - "games/*"
```

Create `tsconfig.base.json` — shared strict config:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "outDir": "./dist",
    "rootDir": "./src"
  }
}
```

Create `tsconfig.json` at root with project references to all packages and games:
```json
{
  "files": [],
  "references": [
    { "path": "packages/playables-adapter" },
    { "path": "packages/game-engine" },
    { "path": "games/safe-cracker" },
    { "path": "games/brick-blitz" },
    { "path": "games/sky-hopper" },
    { "path": "games/crate-catch" },
    { "path": "games/type-strike" }
  ]
}
```

Create `vite.config.ts` with multi-page build:
```typescript
import { defineConfig } from 'vite';
import { resolve } from 'path';

export default defineConfig({
  build: {
    rollupOptions: {
      input: {
        hub: resolve(__dirname, 'index.html'),
        'safe-cracker': resolve(__dirname, 'games/safe-cracker/index.html'),
        'brick-blitz': resolve(__dirname, 'games/brick-blitz/index.html'),
        'sky-hopper': resolve(__dirname, 'games/sky-hopper/index.html'),
        'crate-catch': resolve(__dirname, 'games/crate-catch/index.html'),
        'type-strike': resolve(__dirname, 'games/type-strike/index.html'),
      },
    },
  },
});
```

Create root `index.html` — minimal shell (hub content added in Plan 03):
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Arcade Carnival</title>
</head>
<body>
  <div id="app"></div>
  <script type="module" src="/src/hub.ts"></script>
</body>
</html>
```

Create `src/hub.ts` as empty placeholder:
```typescript
// Hub menu — implemented in Plan 03
document.addEventListener('DOMContentLoaded', () => {
  const app = document.getElementById('app');
  if (app) app.textContent = 'Arcade Carnival — Loading...';
});
```

Create `.gitignore`:
```
node_modules/
dist/
*.local
.vite/
```

Run: `pnpm install`
  </action>
  <verify>
    <automated>pnpm install && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "strict" tsconfig.base.json
    - grep -q "packages/\*" pnpm-workspace.yaml
    - grep -q "games/\*" pnpm-workspace.yaml
    - grep -q "rollupOptions" vite.config.ts
    - grep -q "arcade-carnival" package.json
    - grep -q "tsc -b" package.json
  </acceptance_criteria>
  <done>Root monorepo scaffolded. pnpm install resolves. pnpm typecheck passes. vite.config.ts defines multi-page build with hub + 5 game entries.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared packages and 5 game folder stubs</name>
  <files>
    packages/playables-adapter/package.json, packages/playables-adapter/tsconfig.json, packages/playables-adapter/src/index.ts,
    packages/game-engine/package.json, packages/game-engine/tsconfig.json, packages/game-engine/src/index.ts,
    games/safe-cracker/package.json, games/safe-cracker/tsconfig.json, games/safe-cracker/index.html, games/safe-cracker/src/main.ts,
    games/brick-blitz/package.json, games/brick-blitz/tsconfig.json, games/brick-blitz/index.html, games/brick-blitz/src/main.ts,
    games/sky-hopper/package.json, games/sky-hopper/tsconfig.json, games/sky-hopper/index.html, games/sky-hopper/src/main.ts,
    games/crate-catch/package.json, games/crate-catch/tsconfig.json, games/crate-catch/index.html, games/crate-catch/src/main.ts,
    games/type-strike/package.json, games/type-strike/tsconfig.json, games/type-strike/index.html, games/type-strike/src/main.ts
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md
    - package.json
    - tsconfig.base.json
    - pnpm-workspace.yaml
  </read_first>
  <action>
**Shared packages — stub exports only (implementations come in Plan 02):**

`packages/playables-adapter/package.json`:
```json
{
  "name": "@arcade-carnival/playables-adapter",
  "version": "0.0.1",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": ["src"]
}
```

`packages/playables-adapter/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "composite": true, "outDir": "./dist", "rootDir": "./src" },
  "include": ["src"]
}
```

`packages/playables-adapter/src/index.ts` — STUB exports (real implementation Plan 02):
```typescript
export function initPlayables(): void { /* stub */ }
export function reportScore(_score: number): void { /* stub */ }
export function saveData(_key: string, _value: string): void { /* stub */ }
export function loadData(_key: string): string | null { return null; }
export function onPause(_cb: () => void): void { /* stub */ }
export function onResume(_cb: () => void): void { /* stub */ }
```

`packages/game-engine/package.json`:
```json
{
  "name": "@arcade-carnival/game-engine",
  "version": "0.0.1",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": ["src"]
}
```

`packages/game-engine/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "composite": true, "outDir": "./dist", "rootDir": "./src" },
  "include": ["src"]
}
```

`packages/game-engine/src/index.ts` — STUB exports (real implementation Plan 02):
```typescript
export class GameLoop {
  constructor(_canvas: HTMLCanvasElement) { /* stub */ }
  start(): void { /* stub */ }
  stop(): void { /* stub */ }
}

export class InputManager {
  isDown(_key: string): boolean { return false; }
  justPressed(_key: string): boolean { return false; }
  justReleased(_key: string): boolean { return false; }
}

export class SceneManager {
  push(_scene: string): void { /* stub */ }
  pop(): void { /* stub */ }
  current(): string | null { return null; }
}
```

**5 game folders — each gets package.json, tsconfig.json, index.html, src/main.ts:**

For each game (`safe-cracker`, `brick-blitz`, `sky-hopper`, `crate-catch`, `type-strike`):

`games/{name}/package.json`:
```json
{
  "name": "@arcade-carnival/{name}",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "dependencies": {
    "@arcade-carnival/playables-adapter": "workspace:*",
    "@arcade-carnival/game-engine": "workspace:*"
  }
}
```

`games/{name}/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "noEmit": true
  },
  "include": ["src"],
  "references": [
    { "path": "../../packages/playables-adapter" },
    { "path": "../../packages/game-engine" }
  ]
}
```

`games/{name}/index.html`:
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{Display Name} — Arcade Carnival</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { background: #1a1a2e; display: flex; justify-content: center; align-items: center; height: 100vh; }
    canvas { display: block; }
  </style>
</head>
<body>
  <canvas id="game"></canvas>
  <script type="module" src="./src/main.ts"></script>
</body>
</html>
```
Use display names: "Safe Cracker", "Brick Blitz", "Sky Hopper", "Crate Catch", "Type Strike".

`games/{name}/src/main.ts` — placeholder canvas showing game name + "Coming Soon":
```typescript
const canvas = document.getElementById('game') as HTMLCanvasElement;
canvas.width = 800;
canvas.height = 600;

const ctx = canvas.getContext('2d');
if (ctx) {
  ctx.fillStyle = '{color}';
  ctx.fillRect(0, 0, canvas.width, canvas.height);
  ctx.fillStyle = '#ffffff';
  ctx.font = 'bold 48px sans-serif';
  ctx.textAlign = 'center';
  ctx.fillText('{Display Name}', canvas.width / 2, canvas.height / 2 - 30);
  ctx.font = '24px sans-serif';
  ctx.fillText('Coming Soon', canvas.width / 2, canvas.height / 2 + 30);
}
```
Use unique background colors per game: safe-cracker=#2d3436, brick-blitz=#0984e3, sky-hopper=#6c5ce7, crate-catch=#00b894, type-strike=#d63031.

After creating all files, run `pnpm install` to resolve workspace deps.
  </action>
  <verify>
    <automated>pnpm install && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "initPlayables" packages/playables-adapter/src/index.ts
    - grep -q "reportScore" packages/playables-adapter/src/index.ts
    - grep -q "saveData" packages/playables-adapter/src/index.ts
    - grep -q "loadData" packages/playables-adapter/src/index.ts
    - grep -q "onPause" packages/playables-adapter/src/index.ts
    - grep -q "onResume" packages/playables-adapter/src/index.ts
    - grep -q "GameLoop" packages/game-engine/src/index.ts
    - grep -q "InputManager" packages/game-engine/src/index.ts
    - grep -q "SceneManager" packages/game-engine/src/index.ts
    - grep -rq "@arcade-carnival/playables-adapter" games/safe-cracker/package.json
    - grep -rq "@arcade-carnival/game-engine" games/safe-cracker/package.json
    - test -f games/brick-blitz/src/main.ts
    - test -f games/sky-hopper/src/main.ts
    - test -f games/crate-catch/src/main.ts
    - test -f games/type-strike/src/main.ts
    - grep -q "Coming Soon" games/safe-cracker/src/main.ts
  </acceptance_criteria>
  <done>All 5 game folders created with placeholder canvas pages. Both shared packages have stub exports. pnpm install resolves workspace deps. pnpm typecheck and pnpm build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 1 is scaffold only — no user input, no network calls, no data persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | package.json deps | accept | Dev-only deps, no runtime exposure. Lock via pnpm-lock.yaml |
</threat_model>

<verification>
- `pnpm install` succeeds
- `pnpm typecheck` passes with 0 errors
- `pnpm build` produces `dist/` with hub + 5 game folders
- `pnpm dev` serves root index.html
</verification>

<success_criteria>
Monorepo scaffold complete. All workspace packages resolve. TypeScript strict mode enforced. Build pipeline produces per-game static bundles.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
